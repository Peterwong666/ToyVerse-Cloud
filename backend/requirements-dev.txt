# ============================================================
# ToyVerse Cloud — 开发依赖
# 安装：pip install -r requirements-dev.txt
# ============================================================

-r requirements.txt

# ---------- 测试 ----------
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<0.25.0
pytest-cov>=6.0.0,<7.0.0
pytest-html>=4.1.0,<5.0.0

# ---------- 代码质量 ----------
ruff>=0.7.0,<0.8.0
mypy>=1.13.0,<2.0.0

# ---------- 类型存根 ----------
types-python-dateutil>=2.9.0

# ---------- 前端算法验证 ----------
# 用于 scripts/verify_qrcode.py 与前端手写二维码编码器做逐模块比对
# （前端为零依赖方案，二维码编码器为手写实现，需要独立实现交叉验证）
qrcode>=8.0,<9.0

# ---------- 演示二维码渲染 ----------
# `qrcode` 库只负责编码，**出图需要一个图像后端**（Pillow 或 pypng）。
# 缺了它 `scripts/gen_qrcodes.py` 只能输出文本清单——脚本会明确降级并提示，
# 但「生成可打印的演示二维码」是本阶段的交付项，因此把 Pillow 放进开发依赖。
# 不加进 requirements.txt：生产镜像不需要渲染二维码，多一个 C 扩展依赖
# 只会让镜像变大、构建变慢。
pillow>=10.0
