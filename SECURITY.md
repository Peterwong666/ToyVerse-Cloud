# 安全策略

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| 0.1.x | ✅ 支持 |

---

## 报告安全漏洞

**请不要通过公开 Issue 报告安全漏洞。**

请通过以下方式私下报告：

1. 使用 GitHub 的 [Private Security Advisory](../../security/advisories/new) 功能（推荐）
2. 或发送邮件至项目维护者

报告中请包含：

- 漏洞类型与影响范围
- 复现步骤（最小可复现示例）
- 受影响的版本
- 如有可能，附上修复建议

我们会在 **3 个工作日内**确认收到，并在修复发布后致谢（如你愿意署名）。

---

## 安全设计说明

本项目在设计上遵循以下安全原则：

### 1. 多租户隔离

商户端所有数据查询在 **repository 层**强制注入 `tenant_id` 条件，而非依赖路由层手写过滤。架构上只有一处收口，避免遗漏导致越权。

所有商户端与工厂端端点都配有参数化的**租户隔离测试**（`tests/integration/test_tenant_isolation.py`），断言租户 A 无法读取租户 B 的资源。

### 2. 密钥管理

- 云服务商 `AccessKey` / `SecretKey` **加密存储**（AES 通过 `cryptography` 库）
- 任何 API 响应**永不返回** `SecretKey`
- 所有密钥通过环境变量注入，代码中**无任何硬编码默认值**
- `.env` 已被 `.gitignore` 排除
- 提供 `SECURITY.md`（本文件）与 `.env.example`（仅含占位符）

### 3. 认证与会话

- 密码使用 **bcrypt** 哈希（cost 可配置，默认 12）
- 使用 **JWT** 无状态鉴权，含 `tenantId` 声明用于租户隔离
- 支持 refresh token 轮换
- **登录失败锁定**：连续 5 次失败锁定 15 分钟
- **首次登录强制改密**（可配置）
- 密码最小长度可配置（默认 8）

### 4. 启动期安全校验

服务启动时执行以下校验，**不通过则拒绝启动**：

- `JWT_SECRET_KEY` 不得为占位符或短于 32 字节
- 管理员初始密码必须已设置且满足强度要求
- 生产环境下 `DEBUG` 必须为 `false`

本项目**不预置任何默认口令**。历史版本中曾出现过硬编码弱口令的问题，当前版本已彻底移除。

### 5. 工厂端数据脱敏

工厂端接口通过独立的序列化器（字段白名单）输出数据，**服务端完成脱敏**：

- 客户名：首字符 + 中间星号 + 尾字符（`中国移动` → `中**动`）
- 金额、联系方式、邮箱：**完全不下发**

### 6. 厂商接入的安全失败

未配置密钥的云服务商：
- `health_check` 返回 `DOWN`
- 相关端点返回 `VENDOR_UNAVAILABLE`
- **绝不返回伪造的成功结果**

避免给使用者造成「已接入」的错误认知。

### 7. 输入校验与注入防护

- 所有请求体通过 Pydantic 严格校验
- 数据库访问全部走 SQLAlchemy ORM 参数化查询，无字符串拼接 SQL
- 前端渲染使用统一的转义工具 `shared/ui/dom.js`，避免 XSS
- 静态文件服务做路径规范化，防止目录穿越

### 8. 其他

- CSRF：API 使用 Bearer Token 而非 Cookie，天然免疫 CSRF
- CORS：白名单可配置，默认仅允许本地开发地址
- 上传限制：单文件大小上限 64MB（可在 Nginx 层调整）
- 审计：所有关键操作写入 `audit_logs`，含 `traceId` 便于追溯

---

## 生产部署安全清单

上线前请逐项确认：

- [ ] `JWT_SECRET_KEY` 已替换为强随机值（≥32 字节），生成方式：
      `python -c "import secrets;print(secrets.token_urlsafe(48))"`
- [ ] `QR_SIGN_SECRET` 已替换为强随机值
- [ ] 三个管理员账号密码均已设置为强密码
- [ ] `DEBUG=false`，`APP_ENV=production`
- [ ] 数据库使用 PostgreSQL 并设置强密码（`POSTGRES_PASSWORD` 无默认值）
- [ ] 已启用 HTTPS，且 HTTP 自动跳转
- [ ] `CORS_ALLOWED_ORIGINS` 已收敛为实际域名，不含 `localhost`
- [ ] `/docs` 与 `/openapi.json` 已在生产环境关闭或加访问控制
- [ ] 已配置日志采集与告警
- [ ] 数据库已完成定期备份并演练过恢复流程
- [ ] 依赖已执行安全扫描：`pip list --outdated` 与 `pip-audit`
- [ ] 已确认仓库中不含任何真实密钥（`git log -p | grep -i "secret\|password"`）

---

## 儿童数据合规边界（自查清单）

> **定位**：本节是代码层面的自查，不是法律意见。生产面向真实儿童用户前，必须由法务/合规团队评估并补齐缺口。

### 涉及儿童个人信息的范围

| 数据类型 | 位置 | 说明 |
|---|---|---|
| 手机号 | `end_users.phone` | 终端用户（家长/孩子）登录账号 |
| 昵称 / 头像 | `end_users.nickname` / `avatar` | 可选个人信息 |
| 语音对话 | `dialogue_messages` | 音频转文本后的对话内容 |
| 设备绑定 | `device_bindings` | 设备与终端用户的归属关系 |
| 位置 / 地域 | `devices.region` | 设备注册时按 IP 或上报填充 |
| 操作审计 | `audit_logs` | 含 IP、UA、操作类型，不含原始对话内容 |

### 代码层面已实现的保护措施

| 保护项 | 实现位置 | 说明 |
|---|---|---|
| 终端用户与管理端 JWT 隔离 | `app/core/security.py` | `issue_end_user_token` 与后台 token 分 claims 写入，类型双向隔离 |
| 设备归属校验 | `app/services/miniapp_service.py` | 所有小程序端端点先校验 `device_bindings.end_user_id`，越权统一返回 404 |
| 对话内容安全过滤 | `app/services/dialogue_service.py` | 关键词级过滤，命中后替换为安全话术，`safety_flag` 落库并写 `CONTENT_BLOCKED` 审计 |
| 审计日志 | `app/services/audit_service.py` | 登录、绑定、解绑、设置变更、内容拦截均落库，含 `traceId` |
| 传输加密 | `deploy/nginx.conf` | 支持 TLS 终止；生产部署需启用 HTTPS |
| 数据隔离 | 多租户作用域 | 商户端数据按 `tenant_id` 隔离，避免 A 品牌读取 B 品牌数据 |
| 密钥加密 | `app/core/crypto.py` | 云厂商密钥 AES 加密存储，响应只回掩码 |

### 尚未实现、生产前必须补齐的项

| 缺口 | 风险 | 建议补齐方式 |
|---|---|---|
| **年龄识别与监护人同意** | 无法确认用户是否为儿童及是否获得家长授权 | 注册/首次登录流程增加「出生日期 + 监护人手机号 + 短信确认」或「家长端小程序授权」 |
| **儿童专属隐私政策** | 缺少面向儿童的个人信息处理说明 | 补充儿童隐私政策，明确收集目的、保存期限、共享范围、儿童权利 |
| **数据保留期限与自动删除** | 对话记录/审计日志永久保留，无到期清理 | 按法规要求配置保留期（如对话 90 天、审计 1 年），到期自动匿名化或删除 |
| **删除权入口** | 用户无法申请删除账号及关联数据 | 提供「注销账号」接口，级联删除 `end_users` / `device_bindings` / `dialogue_sessions` / `dialogue_messages` |
| **数据跨境/存储声明** | 未声明数据是否出境、存储位置 | 在隐私政策中声明服务器位置、云服务商及跨境传输安排 |
| **第三方共享清单** | 未明确向火山/集贤/百度等厂商传输哪些数据 | 建立数据共享清单，仅在必要字段传输，避免回传完整对话 |
| **内容安全词表** | 仅关键词级过滤，覆盖有限 | 引入 LLM/模型复核机制，定期审计漏放与误伤 |
| **合规评估报告** | 无第三方评估 | 邀请法务或第三方机构出具 PIPL / COPPA 合规评估 |

### 当前版本的合规结论

- **作为技术作品集 / 学习项目**：当前保护措施（隔离、审计、内容过滤、传输加密）已满足「演示与自学」场景，风险可控。
- **面向真实儿童终端用户提供服务**：上述 8 项缺口必须补齐，并获取法务/合规团队的书面确认。

---

## 已知的安全边界

本项目作为作品集与学习项目，以下内容**未实现**，生产使用前需自行补齐：

| 项目 | 说明 |
|---|---|
| 儿童数据合规 | 详见上节「儿童数据合规边界」。涉及儿童用户语音数据，**未实现** PIPL / COPPA 等法规要求的家长同意、数据保留期限、删除权等机制。 |
| 真实厂商协议 | 集贤、京东云 JoyInside、火山引擎、百度智能云的适配器为**可运行骨架**，未经过真实协议联调与安全评估。 |
| 设备身份认证 | 设备侧凭证（`device_credentials`）已建模，但完整的设备证书签发、轮换、吊销流程未实现。 |
| 风控与限流 | 未实现接口限流、异常行为检测、IP 黑名单等。 |
| 渗透测试 | 未经过第三方安全渗透测试。 |
