# ToyVerse Cloud 项目进度

> **项目**：多租户 AI 智能玩具 SaaS 平台
> **目录**：`/home/peter/py/saas_ai_toy_platform`
> **配套文件**：[`todolist.md`](./todolist.md)（详细任务清单）
> **最后更新**：2026-09-24（新增「项目整体复盘与质量评估」章节：P0–P11 及硬件/协议验证的整体质量评估、遗留事项闭环分析与分级改进方案。**纯复盘，未改动任何代码**）

---

## 总体进度

```
P0  ████████████████████  100%   脚手架与进度文件          已完成
P1  ████████████████████  100%   后端地基                  已完成
P2  ████████████████████  100%   前端地基                  已完成
P3  ████████████████████  100%   目录域                    已完成
P4  ████████████████████  100%   订单与设备生成            已完成
P5  ████████████████████  100%   设备/分配/绑定            已完成
P6  ████████████████████  100%   烧录工厂端                已完成
P7  ████████████████████  100%   AI 抽象与模拟引擎         已完成
P8  ████████████████████  100%   终端用户小程序端          已完成
P9  ████████████████████  100%   AI 配置与运营看板         已完成
P10 ████████████████████  100%   部署与质量保障            已完成
P11 ████████████████████  100%   文档/学习手册/复盘        已完成
```

| 指标 | 数值 |
|---|---|
| 阶段总数 | 12（P0 – P11） |
| 已完成阶段 | **12（P0–P11）** |
| 进行中阶段 | 0 |
| 阻塞阶段 | 0 |
| **整体完成度** | **100%**（12/12 阶段全部完成） |
| 测试总数 | **828**（567 个测试函数 / 27 个文件；09-22 新增代注册 14 条 + 09-24 门禁修复） |
| 代码覆盖率 | ⚠️ **68%**（10490 语句 / 3344 未覆盖；代注册与签名新代码拉低 2 点，仍高于 CI 门槛 65） |
| 后端源文件（mypy 口径） | 95 |
| 文档 | **16 篇专业文档**（`docs/README` + `01`–`16`，15/16 为硬件速查与火山标准协议方案）+ 1 个文档一致性门禁 |
| 学习手册 | **20 个文件 / 8177 行**（`learning/`，**不上传 GitHub**）+ 1 个结构校验脚本（不入库） |
| 截图 | **14 张**（`docs/assets/`，2.2MB）：登录页 + 平台端 6 + 商户端 4 + 工厂端 2 + 小程序 1 |

> **口径说明（2026-09-24）**：上表「整体完成度 100%」指 **12 个阶段的验收标准全部达成**（判据见各阶段验收记录），**不等于功能全量交付**——已知功能开口（工厂端批次查询页、13 个未实现菜单项、语音前端等）统一登记在下方「遗留事项登记表」；README 的「已知局限」小节是同一口径的对外版本。

---

## 状态图例

| 状态 | 含义 |
|---|---|
| 未开始 | 尚未动工 |
| 进行中 | 正在执行 |
| 已完成 | 已通过该阶段验收标准 |
| 阻塞 | 遇到阻塞，需决策或依赖 |

---

## 主进度表

| 阶段 | 任务 | 状态 | 开始日期 | 完成日期 | 备注 |
|---|---|---|---|---|---|
| P0 | 创建目录骨架 | 已完成 | 2026-09-16 | 2026-09-16 | 38 个目录 |
| P0 | 编写 `.gitignore` | 已完成 | 2026-09-16 | 2026-09-16 | 已排除 `learning/`、`.env`、`data/`、`*.jar` |
| P0 | 编写 `.env.example` | 已完成 | 2026-09-16 | 2026-09-16 | 十一大类变量，含中文注释，无真实密钥 |
| P0 | 编写 `Makefile` | 已完成 | 2026-09-16 | 2026-09-16 | 30+ 命令，含 `help/setup/dev/test/migrate/seed/up/down` |
| P0 | 添加 `LICENSE` | 已完成 | 2026-09-16 | 2026-09-16 | MIT |
| P0 | 产出 `todolist.md` | 已完成 | 2026-09-16 | 2026-09-16 | 含 P0–P11 全量任务 + 3 个附录 |
| P0 | 产出 `项目进度.md` | 已完成 | 2026-09-16 | 2026-09-16 | 本文件 |
| P0 | 编写 `README.md` 骨架 | 已完成 | 2026-09-16 | 2026-09-16 | 含简介、四端、快速开始、架构图、技术栈 |
| P0 | 编写 `backend/requirements.txt` | 已完成 | 2026-09-16 | 2026-09-16 | FastAPI/SQLAlchemy 2.0/PyJWT/bcrypt 等 |
| P0 | 编写 `backend/requirements-dev.txt` | 已完成 | 2026-09-16 | 2026-09-16 | pytest/ruff/mypy |
| P0 | 编写 `backend/pyproject.toml` | 已完成 | 2026-09-16 | 2026-09-16 | ruff + mypy(strict) + pytest + coverage |
| P0 | 编写 `docker-compose.yml` 草案 | 已完成 | 2026-09-16 | 2026-09-16 | SQLite 默认 + PostgreSQL 可选 profile |
| P0 | 编写 `CONTRIBUTING.md` / `SECURITY.md` / `CHANGELOG.md` | 已完成 | 2026-09-16 | 2026-09-16 | CONTRIBUTING 含八条架构红线 |
| P0 | `git init` 与首次提交 | 已完成 | 2026-09-16 | 2026-09-16 | 分支 main |
| P0 | 验证：`git status` 干净、`learning/` 被忽略、`make help` 可用 | 已完成 | 2026-09-16 | 2026-09-16 | 三项全通过 |
| P1 | 后端地基（全部子任务） | 已完成 | 2026-09-16 | 2026-09-16 | 详见下方验收记录 |
| P2 | 前端地基（全部子任务） | 已完成 | 2026-09-16 | 2026-09-16 | 40 个文件、13567 行；详见下方验收记录 |
| P3 | 目录域（全部子任务） | 已完成 | 2026-09-16 | 2026-09-16 | 迁移 0005/0006、平台端 17 端点、前端 6 页；详见下方验收记录 |
| P4 | 订单与设备生成 | 已完成 | 2026-09-16 | 2026-09-16 | 迁移 0008–0010、平台端 20 端点、前端 5 页；测试 395→397；详见下方验收记录 |
| P5 | 设备/分配/绑定 | 已完成 | 2026-09-16 | 2026-09-16 | 迁移 0011、平台端 5 + 商户端 6 + 设备侧 1 = 13 个新端点、前端 4 新页 + 1 改造；测试 397→450；详见下方验收记录 |
| P6 | 烧录工厂端 | 已完成 | 2026-09-16 | 2026-09-17 | 迁移 0012+0013、工厂端 9 + 平台端 6 = 15 个新端点、前端 6 新页 + 1 页；测试 456→737；详见下方验收记录 |
| P7 | AI 抽象与离线模拟引擎 | 已完成 | 2026-09-16 | 2026-09-16 | 迁移 0007、42 测试；详见下方验收记录 |
| P8 | 终端用户小程序端 | 已完成 | 2026-09-17 | 2026-09-17 | 迁移 0014、小程序 16 + WS/SSE = 18 个端点、前端 9 屏（拆分为独立模块）；测试 737→757；详见下方验收记录 |
| P9 | AI 配置与运营看板 | 已完成 | 2026-09-17 | 2026-09-17 | 迁移 0015、34 个新端点、前端 6 新页 + 1 页；测试 758→804；详见下方验收记录 |
| P10 | 部署与质量保障 | 已完成 | 2026-09-17 | 2026-09-17 | Docker/nginx/compose 全链路真跑、冒烟 57/57、敏感信息扫描 0 命中、CI 五 job；**由真跑发现并修复 12 个「文档写了但跑不通」的问题**；详见下方验收记录 |
| P11 | 文档集/学习手册/复盘 | 已完成 | 2026-09-17 | 2026-09-17 | **A/B/C 全部完成**：14 篇文档 + 文档门禁、学习手册 20 文件 8177 行、14 张截图、README 终版；两项 🔑 最终验证通过；**并修正 README 快速开始漏了 3 项密钥的真实缺陷**；详见下方验收记录 |
| P11-D | 收官验证（压测 / TLS / 多实例）+ **推送与 CI 首次实跑** | 已完成 | 2026-09-17 | 2026-09-17 | ✅ 三项**全部真跑**：负载测试（SQLite vs PG）、Nginx TLS、多实例负载均衡；★ **顺带抓到一个「改一个账号名就让部署起不来」的真缺陷**；三条 ⚠️ 局限消除；✅ **已推送 GitHub 并跑通 CI**（run #1 五 job 全绿），★ **首跑又抓到「触发分支写成 master → 永不触发」**；详见下方验收记录 |

---

## 当前节点与最新验收记录

**★ 12 个阶段（P0–P11）全部完成；里程碑 M0–M5 全部达成。**

> **公开前整理（2026-09-17）**：用户提出「公开前的待办也帮我处理」，六项逐一处理完毕
> （完整记录见 `todolist.md` 的「公开前整理」小节）：
> ① **CI**——把能本地验的都验了（YAML 可解析、引用的脚本与路径全在、命令与 Makefile 一致），
>    并修掉一处真实冗余（`pytest -q` 已含 e2e 却又单独跑一次，**每次 CI 白等约 5 分钟**）；
> ② **`CHANGELOG` 已切 `[1.0.0] - 2026-09-17`** + 本地 tag；
> ③ **`docs/12` 三处过期表述已同步**；
> ④ **AI 未联调 / 儿童数据合规未实现**——无法真正实现，改为**让它不可错过**（README 顶部显著提示）；
> ⑤ **CI 新增覆盖率下限 `--cov-fail-under=65`**（实测 70，留余量）——本项目到 P11 才发现
>    「804 用例全绿但覆盖率只有 70%」而**没有任何机制会提醒**；
> ⑥ ★ **PostgreSQL 已真跑验证**（迁移 `0015` + 种子 + 应用 healthy + **冒烟 57/57**），
>    并**因此抓到一个真实缺陷**（见下）。
>
> **P11-D 收官验证（2026-09-17）**：用户选择「A 收官验证」，把上表 ②③ 两条 ⚠️ 真的跑掉了——
> **Nginx TLS、多实例负载均衡、负载测试三项全部实测**，并顺带抓到 1 个真缺陷
> （初始账号播种不幂等 → 改一次 `.env` 里的账号名就会让已有库的部署无限重启）。
> 详见下方 **P11-D 验收记录**。
>
> **★ 已推送并跑通 CI（2026-09-17）**：仓库推到了 `https://github.com/Peterwong666/ToyVerse-Cloud`
> （`main` + tag `v1.0.0`）。★ **首次真跑就抓到一个真缺陷**：`ci.yml` 的触发分支写的是 `master`、
> 而默认分支是 `main` → **推送根本不触发 CI**（查 API 得 `total_count = 0`）；修复后 run #1 **五 job 全绿**。
>
> **剩余待办（诚实记录，1 条）**：
> ① **真实厂商联调（ESP32 真机激活）与儿童数据合规未实现**。
> 多实例 + PostgreSQL 组合已于 2026-09-18 验证完成。
> 另有若干「做了但不够」的项（压测只有一轮短测、连接池未调优、证书为自签、
> CI 不构建镜像且环境与本机不同）见 `docs/09` 附录与 `docs/11` 的 `## 5.2` / `## 8.5`。
>
> **P11-A / P11-B / P11-C / P11-D 的其余局限**见各自验收记录。
>
> **2026-09-18 复盘与待决项梳理**：针对「昨天进度较慢」的感受，重新核对 `项目进度.md` 与 `todolist.md` 后得出：
> 代码交付本身已完整，剩余瓶颈均为**外部依赖型任务**——真实厂商联调（含 ESP32 刷机）、儿童数据合规、多实例 + PostgreSQL 组合验证。
> 已产出两份本地工作文档：
> [`分析_进度瓶颈与待决项.md`](./分析_进度瓶颈与待决项.md)、
> [`解决方案_后续待决项执行计划.md`](./解决方案_后续待决项执行计划.md)（均不上传 GitHub）。
> 同时发现 `todolist.md` P7 中 `ai/base.py`、`ai/registry.py`、`ai/mock/*` 三项复选框仍为 `[ ]`，与代码实际完成状态不一致，已修正。
>
> **2026-09-18 ESP32 云端链路自检完成**：用户在平台端「云服务商配置」录入火山引擎 AccessKey / SecretKey 后，
> 调用 `POST /api/v1/platform/clouds/cloud-volcano-hw/test` 返回 **SUCCESS / ok=true / latencyMs=758**，
> 消息为「网络可达（HTTP 400）——该结论仅代表网络连通，密钥有效性将在实际调用厂商接口时验证」。
> ★ **意义**：火山引擎服务端接口网络可达，平台侧凭证已落库，**云端链路这一层的不确定性已清零**。
> 下一步（待用户有时间刷机时）：ESP32 设备端配网/激活，让平台侧看到「设备已激活」。
>
> **2026-09-18 多实例 + PostgreSQL 组合验证完成**：
> ① 单实例 PG 冒烟 **57/57 通过**；
> ② 移除 `app` 服务的 `container_name`、把 Nginx `depends_on` 由 `condition: service_healthy` 改为简单依赖后，
> `docker compose --profile postgres --profile nginx up -d --scale app=2` 成功启动 2 个应用实例 + 1 Nginx + 1 PG；
> ③ 经 Nginx 冒烟 **57/57 通过**；
> ④ 读压测（`/api/v1/health`，20 并发 × 15 秒）：**142.5 QPS、0 失败**；
> ⑤ 写压测（`POST /api/v1/platform/clouds/cloud-volcano-hw/test`，20 并发 × 15 秒）：**66.2 QPS、1000/1000 成功**；
> ⑥ 日志统计请求分发：**健康检查 1097/1095，写请求 503/502**，基本均衡；
> ⑦ WebSocket 经 Nginx（`ws://localhost:8080/ws/miniapp/chat`）握手成功并以 **4401** 关闭，与预期一致。
> ★ **结论**：**多实例 + PostgreSQL 组合可用**，且写并发无 500；单实例 SQLite 多实例无提升的结论在 PG 下被推翻。
>
> **2026-09-18 儿童数据合规自查完成**：在 `SECURITY.md` 新增「儿童数据合规边界（自查清单）」小节，
> 列出涉及儿童个人信息的 6 类数据、7 项代码层面已实现的保护措施、8 项生产前必须补齐的缺口，
> 并明确当前版本作为「技术作品集 / 学习项目」风险可控，**面向真实儿童用户前必须经法务评估**。
> 同步更新 `docs/11-测试与质量保障.md`：`## 8.5` 的「未测多实例 + PG」改为「已测」，并新增 `## 8.3.2` 记录 PG 多实例数字。

> **2026-09-20 ESP32-S3 Sense 真机刷机完成（硬件侧）**：板子已刷入第三方开源语音固件并**实测通过音频链路**，
> 过程中定位并修复了一个**上游固件在「无屏板型」上的缺陷**。
> ★ 注意边界：设备当前激活在**第三方语音平台**上，**尚未接入本项目自己的后端**，故
> 「真实厂商联调（ESP32 真机激活）」这条待办**只完成了一半**（硬件与固件可跑通，平台侧激活仍未做）。
> 完整分析过程见下方 **硬件联调记录**；速查与回滚见 [`docs/15`](./docs/15-重要信息快查及回滚手册.md)。
>
> **2026-09-20 同时定案了真机接自家后端的路线**：官方「标准协议自定义接入（WebSocket）」规格已取回并整理为
> [`docs/16`](./docs/16-火山标准协议设备端接入方案与改动清单.md)，文档门禁与敏感信息扫描均通过。
> **两项决策已定**：① 走「复用已验证固件、只换协议层」；② **后端代注册**（`product_secret` 不出后端）。
> ★ 关键发现：该协议音频为 **`pcm16`**，与我们的 PDM 麦克风 + 功放天然匹配，**不需要 Opus 编解码**。
> **下一步（已完成 ✅，2026-09-22 P-A / P-B PC 端验证）**：
>
> ① **采样率已确认**：上行 16kHz / 16bit / mono，下行 24kHz（原「待向技术支持确认项」就此闭环）。
> ② **P-A 注册 + 建连通过**：PC 端 Python 脚本（`scripts/pa_verify.py`）走通「注册 + 建连」全流程，判据达成（收到 `session.created`）；过程中发现并修复 4 个问题（`random_num` 应为 int、DynamicRegister 响应 payload 字段为小写、缺 `bot_id` 配置、缺 Query 参数）。
> ③ **后端代注册接口落地**：`POST /api/v1/device/register`——设备用平台 SN + 密钥换取火山 `device_secret`，`product_secret` 不出后端（Route B 定案的落地），配 14 条单元测试；**火山 Provider 的 AK/SK HMAC-SHA256 签名亦已实现**（闭环 P7 已知局限①，见 `docs/08` §5.3）。
> ④ **P-B 文本验证通过**：发送「你好」→ 收到智能体回复，双向通信打通；音频 ASR 上行需真机麦克风，PC 端无法验证。
>
> ★ **当前实际节点：ESP32 移植前夜**。剩余三步：**P-B 音频上行（需真机麦克风）** → 固件协议层移植（按 `docs/16` 改动清单；调试期**复用**已注册设备 `pa-test-6333c399` 的 `device_secret`，见 B-01）→ 新设备注册前在控制台给目标产品分配未占用的 License（账号尚余 **6 个未分配**，**无需扩容**，见 B-01 实查结论）。
>
> **2026-09-24 上午插记（P-B 启动前状态）**：用户开始 P-B 音频上行真机调试，发现 ESP32-S3 与 MAX98357 间杜邦线全部脱落。已按固件板型配置（`~/esp/xiaozhi-esp32/main/boards/seeed/xiao-esp32s3-sense/config.h`：SPK BCLK=GPIO4 / LRC=GPIO5 / DIN=GPIO6）给出 5 根线的重接指导（3 信号 + 5V→VIN + GND→GND，GAIN/SD 悬空），并写入 `ESP32-S3/接线文字描述.txt`（该目录不入 git）。★ 同时确认：**音频上行走板载 PDM 麦（GPIO41/42），无需任何接线**——功放只影响 TTS 播放。**下午计划**：接线 → 通电 3 步验证（提示音 → 唤醒应答，板上仍是已验证的小智固件）→ 进入 P-B 音频上行调试。
> ⚠️ 诚实记录：09-22 的这批工作当时**未同步本文件、未跑门禁即提交**——远端 CI run #4 因此失败且两天无人发现，2026-09-24 执行复盘方案时才修复（门禁修复 + 快照/文档同步）并推送，见变更记录。

### 硬件联调记录（2026-09-20）— ESP32-S3 Sense 刷机、音频验证与一处上游缺陷定位

**目标**：把搁置已久的「真机联调」推进到「设备能听会说」这一层，为后续接入本项目后端扫清硬件侧不确定性。

#### 1. 环境搭建（本机，可复现）

| 项 | 结果 |
|---|---|
| 系统 | Ubuntu 24.04 / x86_64，4 核，磁盘可用 361 GB |
| ESP-IDF | **v6.1**（`~/esp/esp-idf`；工具链 4.8 GB 在 `~/.espressif`） |
| 装法 | `git clone -b v6.1 --single-branch --depth 1 --shallow-submodules --recursive` + `./install.sh esp32s3` |
| 加速 | `IDF_GITHUB_ASSETS=dl.espressif.cn/github_assets` + 国内 PyPI 镜像 |
| 激活 | `~/.bashrc` 写入 `alias get_idf` |
| 验证 | `idf.py --version` → `ESP-IDF v6.1`；官方 `hello_world` 编译通过 |

★ **实测网络结论**：`git clone` 走 github 协议很快（26 MB 的固件源码 6 秒），
但 `codeload.github.com` 的 tarball 直连**超时**——即「能 clone 不能下 zip」，排障时别误判成网络全断。

#### 2. 板型适配：上游没有这块板子

先确认硬件身份（用户确认 + 工具链读取）：

- 芯片 ESP32-S3 rev v0.2，**内嵌 8 MB PSRAM**，Flash **8 MB**（GigaDevice GD25Q64）
- eFuse 中 Flash 类型为 **quad（4 数据线）** → 固件按 QIO 配置正确
- 板载 PDM 数字麦克风，引脚经 Seeed 官方 wiki 交叉确认：**DATA=GPIO41 / CLK=GPIO42**
- 外接 MAX98357 功放：**BCLK=GPIO4 / LRC=GPIO5 / DIN=GPIO6**（与麦克风不冲突）

上游固件共 151 个板型，**没有**这块板子的配置，因此自建板型并本地提交（未推上游）：

- 新增 `main/boards/seeed/xiao-esp32s3-sense/`（`config.h` / `config.json` / 板级 `.cc`）
- 另需改 `main/Kconfig.projbuild` 与 `main/CMakeLists.txt` —— 这是新增板型的**固定三处改动**
- 8 MB Flash 用 `partitions/v2/8m.csv`；走 PDM 通道的 `NoAudioCodecSimplexPdm`
- ★ **踩到的校验规则**：`config.json` 里写了 `manufacturer` 就必须放在
  `main/boards/<manufacturer>/<board>/` 子目录，否则构建脚本直接拒绝

#### 3. ★ 分析过程：配网成功后「必崩」的定位

**现象**：刷入后开机正常、能进配网模式、能播提示音；但**一旦配网成功就崩溃并进入启动循环**，
每次开机都在同一位置重演。

**定位过程（四步，可复用）**：

1. **先排除硬件**：日志开头即显示 `Found 8MB PSRAM device` / `Speed: 80MHz` /
   `Adding pool of 8192K of PSRAM memory to heap allocator` → PSRAM 正常，不是内存不足。
2. **读崩溃类型**：`Guru Meditation Error: Core 1 panic'ed (LoadProhibited)`，
   `EXCVADDR = 0x00000014` → **空指针 + 偏移 0x14**，即「访问了某个为 NULL 的结构体成员」，
   且崩溃点紧跟 `MODEL_LOADER: Successfully load srmodels` 之后。
3. **解栈**（关键一步）：把日志里的 `Backtrace:` 地址交给
   `xtensa-esp32-elf-addr2line -pfiaC -e build/xiaozhi.elf <地址...>`，直接得到调用链：

   ```
   Application::CheckAssetsVersion()            (application.cc:437)
     → Assets::Apply(bool)                      (assets.cc:54)
       → Assets::LvglStrategy::Apply(...)       (assets.cc:291)   ← 加载字体
         → std::make_shared<LvglCBinFont>(ptr)
           → cbin_font_create → lv_malloc
             → lv_tlsf_malloc → search_suitable_block              ← 空指针在这一层
   ```

4. **根因**：LVGL 的 TLSF 内存池**只由显示驱动创建**（`lv_init()` 仅出现在
   `lcd_display.cc` / `epd_display.cc` 等显示相关文件里）。**本板型没有屏幕**，
   `lv_init()` 永远不会被调用 → 池指针为 NULL → 任何 `lv_malloc` 都崩。

**顺带发现两处上游设计盲区（已记录，可作为上游 issue 素材）**：

- `Board::GetDisplay()` 默认返回 `&NoDisplay`，**永远不是 `nullptr`**；
  而 `assets.cc:291` 处写着 `display == nullptr` 的兜底判断 ——
  对无屏板型而言这是**死代码**，救不了场。
- 真正的「无屏开关」是 `CONFIG_USE_EMOTE_MESSAGE_STYLE`（打开后 `HAVE_LVGL` 不再定义、
  走另一套策略），但它的 `depends on` 白名单只列了几个官方板型，
  **自定义板型根本选不中** —— 等于把新板型堵在门外。

**修复**：不引入新代码，采用上游构建脚本**自己已有的规则**——
在板型 `sdkconfig_append` 中加入

```
CONFIG_LV_USE_BUILTIN_MALLOC=n
CONFIG_LV_USE_CLIB_MALLOC=y
```

让 `lv_malloc` 走普通堆，不再依赖那个永不创建的池。
（同一条规则上游原本用于 `CONFIG_SPIRAM=n` 的板型，属于「用上游自己的答案解上游的盲区」。）

#### 4. 修复后验证

| 检查项 | 结果 |
|---|---|
| 崩溃 | `Guru Meditation` 计数 **0** |
| 状态机 | `activating -> idle` 正常进入 |
| 字体加载 | 由崩溃降级为 `W Assets: Ignoring invalid text font asset ...` —— **优雅降级** |
| 网络 | 连上 Wi-Fi，协议握手成功，进入待机监听 |
| 唤醒词引擎 | `wn9_nihaoxiaozhi_tts` 正常初始化，AFE 单麦管线就绪 |

#### 5. 音频链路实测结论（★ 硬件侧不确定性清零）

| 环节 | 结论 | 依据 |
|---|---|---|
| 板载 PDM 麦克风 | ✅ **可用** | 50 cm 外可唤醒，唤醒词引擎实时消费麦克风 PCM |
| 外接 MAX98357 功放 | ✅ **可用** | 配网提示音实际出声 |
| 唤醒词触发 | ✅ **可用** | 状态机随唤醒词推进 |

**一次误判的复盘（值得记）**：配网模式下按 BOOT 会进入内置「录音 10 秒 → 回放」自测，
但当时**录满 10 秒却听不到回放**，一度被判断为「麦克风无信号」。
后续在正常流程（进 idle、唤醒词引擎接管）下实测麦克风灵敏，
说明**「自测回环无声」不能等同于「麦克风坏」**——
该模式下录音队列被填满只能证明输入任务在跑，**不能证明有信号**。
教训：判断音频链路要在**正常业务流程**里验，不要只依赖调试模式。

#### 6. 本次遗留（诚实记录）

- ① 设备当前激活在**第三方语音平台**上，**本项目后端接入未做**——这正是待办
  「真实厂商联调（ESP32 真机激活）」中**尚未完成的那一半**。
- ② 板载摄像头（OV2640）**未启用**（本版刻意最小化，先跑通语音）。
- ③ 用户 LED 极性按「低电平点亮」配置，**未实测**（不影响主链路）。
- ④ 已备份刷机前的原厂固件，回滚路径见 `docs/15`。

---

### P11-D 验收记录（2026-09-17）— 收官验证：负载测试 / Nginx TLS / 多实例

**目标**：把 `docs/09` 里登记过、但一直没有数字的三条 ⚠️ 变成实测——
① 「**从未做过压测**」、②「**Nginx TLS 未验证**」、③「**多实例未验证**」。

**新增固定资产**：

| 文件 | 说明 |
|---|---|
| `scripts/load_test.py` | 标准库 + `httpx` 的 asyncio 压测器；**不引入 ab / wrk / hey**。守卫：非 GET 必须显式 `--allow-write`；只读方法给了 `--body` 直接拒绝 |
| `backend/tests/integration/test_seed_admin_users.py` | **5 条**回归守卫（新建 / 重跑空操作保密码 / **改名同步** / 改名后第三次仍幂等 / 名字被别的 id 占用跳过） |

**① 负载测试（20 并发 × 15 秒，同一脚本打两种后端）**

| 指标 | SQLite | PostgreSQL |
|---|---|---|
| 读 QPS | **73.6** | 72.0 |
| 读 p50 / p95（ms） | 257 / **436** | **149** / 994 |
| 写 QPS | 30.7 | **93.0** |
| 写 p50 / p95（ms） | 210 / 2946 | 151 / **606** |
| 写失败 | **4 / 487**（`database is locked` → `500`） | **0 / 1414** |

★ **结论修正了本项目自己的假设**：**读路径上 SQLite 并不输**（吞吐持平，尾延迟还更好），
PG 的优势**完全集中在写路径**（×3 吞吐、失败归零）。
→ 选型的准确表述是「**只有需要并发写时才必须上 PG**」，而不是「PG 什么都更快」。

**② Nginx TLS（自签证书，四项全过）**

- ① HTTPS 提供 API → `200`；② HTTPS 提供 SPA 与静态资源（4 端 + `/login` + `api.js`）→ 全 `200`
- ③ HTTP → HTTPS → **`301`**；④ ★ **`wss://` 握手成功**，服务端以 **close code 4401** 关闭（期望值）
- 佐证：`openssl s_client` → `New, TLSv1.3, Cipher is TLS_AES_256_GCM_SHA384`
- ★ ④ 最值钱：**TLS 终止与 WS upgrade 同时正确**——这是「配了 TLS 之后 WS 静默失效」的常见坑

**③ 多实例（2 实例 + 1 Nginx 轮询）**

- ① 分发：**31 / 29**（各打 60 次，两边都 > 0）；② WS 经 Nginx → 4401
- ③ ★ **共享同一 SQLite 卷并发写**：**31.4 QPS + 2×500**，两实例各 3 条写冲突
- ★ **反面结论（本次最重要的部署结论）**：单实例 30.7 QPS → 双实例 31.4 QPS（**无提升**），
  冲突却翻倍 → **加实例救不了 SQLite 的写并发；多实例必须配 PostgreSQL**

**④ ★ 顺带抓到的真缺陷（由 ③ 的实验炸出，价值高于任何 QPS 数字）**

- **现象**：复用已有数据卷启动容器 → 应用**启动即崩、无限重启**（`Restarting (1)`）
- **根因**：`_seed_admin_users` **按 `account` 查重、却按固定 `id` 插入**——两个判据不一致
- **触发**：`.env` 里把管理员**登录账号换成了另一个号码** → 按新名字查不到 → 拿同一个 `id` 再插 →
  `IntegrityError: UNIQUE constraint failed: user_accounts.id`
- **为什么严重**：入口脚本「先播种、再起服务」→ **一个配置项就能让已有库的部署进入无限重启**；
  且**新库 / 测试库 / CI 全部复现不出来**
- **修复与验证**：查重改按 **`id`**（三条路径：同名空操作保密码 / 改名同步登录名并告警 / 名字被别的 id 占用则跳过并告警）；
  ① 在**真实故障卷**上实测：`Restarting (1)` → `healthy`、日志出现改名告警、`password_hash` 不变、重启幂等；
  ② 新增 5 条回归测试，**改名那条在修复前的实现上会失败**（已实测输出 `UNIQUE constraint failed`）

**⑤ 同步更新的文档**（把「未验证」改写为实测，并保留仍未证明的部分）

| 文档 | 改动 |
|---|---|
| `docs/09` | `## 2.` 重写为 `2.1` 并发基线（实测表）+ `2.2` 业务阈值；§3.2 HTTPS 行；§4 兼容性三行；附录 3/4/7/11/13 |
| `docs/11` | **新增 `## 8. 性能与部署验证`**（8.1 压测 / 8.2 TLS / 8.3 多实例 / 8.4 播种缺陷 / **8.5 仍未证明的事**）；附录 6、8 改写；测试计数 814 / 553 |
| `docs/03` | 附录 1–4 |
| `docs/01` | 附录 1、2、5；§9.3 多实例条目 |
| `docs/07` | 附录 7（多实例） |
| `docs/10` | 附录 9（明确「压测衡量的是 HTTP 设备/订单接口，与对话链路无关」） |
| `docs/12` | 新增 `4.13`（账号改名即崩）与 `4.14`（被实测推翻的「PG 读更快」假设）；§5.1 加性能基线行；§6.1/6.2 同步 |
| `README.md` | 顶部提示、「已知局限」数据库/部署/性能三行、测试徽章与规模 |

**⑥ 诚实保留的局限**（新增 `docs/11` 的 `## 8.5`）

⚠️ 不是容量规划依据（单机、默认配置、仅 15 秒）｜⚠️ 未调连接池与 `--workers`｜
⚠️ 无长时间稳定性 / 劣化曲线｜⚠️ **未验证「多实例 + PostgreSQL」**｜
⚠️ WebSocket 长连接容量未测｜⚠️ 写路径只压了 1 个轻量端点（批量导入 / 分配执行未压）

**⑦ 门禁**：`make check` 全绿 —— **814 passed in 256.49s** + lint / mypy（95 文件）/ OpenAPI 契约 / **docs-check（8 类）**

**⑧ 推送与 CI 首次实跑（2026-09-17）**

| 项 | 结果 |
|---|---|
| 仓库 | `https://github.com/Peterwong666/ToyVerse-Cloud`（public）；推送 `main` + 附注 tag `v1.0.0` |
| 远端原有内容 | 一个**较旧的快照**（20 提交 / 280 文件，止于「账号中立化」），与本地**无共同祖先**（本机做过作者重写）→ 强推；旧历史留为本地分支 `backup/origin-main-b6c1f06` |
| 推送前核对 | `--dry-run` 演练；远端独有文件只有 `SUMMARY.md`（用户已决定不保留）；本地独有 3 个新文件 |
| ★ 首跑抓到的真缺陷 | `ci.yml` 的 `on.push.branches` 写的是 **`master`**，而默认分支是 **`main`** → **推送不触发任何 CI**（查 API：`total_count = 0`）。修复后重推 |
| **run #1 结果**（commit `8aeea28`） | 代码规范 **43s ✅** / 测试（pytest）**2m42s ✅** / 契约一致性 **25s ✅** / 前端导入契约 **5s ✅** / 敏感信息扫描 **6s ✅** —— **无步骤被跳过** |
| 独立复核 | 把仓库 `git clone` 到空目录（**无 `.env`、无 `venv`**，与 CI 条件一致），逐条重跑 CI 的 5 条命令 → 全部通过（`813 passed`，覆盖率 **69.92%** ≥ 65 门槛） |

> ★ 这条与 P10 的 12 个问题同类：**本地能验的全验过了（YAML 可解析、命令与 Makefile 逐字一致），
> 唯独验不了「它会不会被触发」**。教训：**「配置存在」不等于「配置生效」**——
> 对声明式配置，唯一有效的验收是**推一次，然后去查它有没有真的跑**。

---

### P11-C 验收记录（2026-09-17）

**★ 交付物**

| # | 交付物 | 说明 |
|---|---|---|
| 1 | **14 张各端截图**（`docs/assets/`，2.2MB） | 登录页 1 + 平台端 6（工作台 / 租户 / 订单 / 设备 / 工厂订单 / OTA）+ 商户端 4（工作台 / 我的产品 / AI 配置 / 运营看板）+ 工厂端 2（工作台 / 生产订单）+ 小程序 1（扫码页）；**逐张人工核验为真实登录态** |
| 2 | **`README.md` 终版** | 新增「界面预览」（7 张内嵌 + 14 张清单）、规模速查表、「本项目的验收方式」、「已知局限」；徽章含 `coverage-70%`；**并修复快速开始的真实缺陷**（见下） |
| 3 | `CHANGELOG.md` | `[未发布]` 段覆盖 P1–P11 全部阶段；⚠️ 未切版本 tag（仓库无 release） |

**★ 两项 🔑 最终验证**

| 验证 | 命令 | 结果 |
|---|---|---|
| **`learning/` 未被跟踪** | `git check-ignore -v learning/README.md` | ✔ 命中 `.gitignore:15` |
| | `git ls-files \| grep "^learning/"` | ✔ **0 条** |
| **陌生环境 5 分钟内可跑起来** | 在 `/tmp` 全新克隆后按 README 操作并计时 | ✔ 见下（**但先发现了 README 的缺陷**） |

**★★ 验证 2 发现并修复了一个真实缺陷：README 的快速开始不完整**

启动期安全校验实际要求 **6 项**，而 README 原文只让改 3 个管理员口令。
**在干净目录里照 README 字面操作，`make setup` 直接失败（81 秒）**，报错原文：

```
InsecureConfigurationError: 配置未通过安全校验，服务拒绝启动：
  - JWT_SECRET_KEY 仍为占位符，请替换为强随机值
  - QR_SIGN_SECRET 仍为占位符，请替换为强随机值
  - PLATFORM_ADMIN_PASSWORD 未设置（账号 admin）
  - MERCHANT_ADMIN_PASSWORD 未设置（账号 15555555555）
  - FACTORY_ADMIN_PASSWORD 未设置（账号 13600000000）
  - PLATFORM_OPERATOR_PASSWORD 未设置（账号 13812345678）
```

**两段式实测（同一脚本，先证伪再证真）**：

| 阶段 | 条件 | `make setup` | 健康检查 | 四端入口 |
|---|---|---|---|---|
| **A（照 README 字面）** | 只设 3 个管理员口令 | ✘ **失败（81 秒）** | 000 | 不可达 |
| **B（补齐 6 项）** | + `JWT_SECRET_KEY` / `QR_SIGN_SECRET` / `PLATFORM_OPERATOR_PASSWORD` | ✔ **成功（72 秒）** | **200** | `/`、`/login`、`/platform/`、`/merchant/`、`/factory/`、`/miniapp/` **全部 200** |

**结论：从克隆到四端全部可访问 ≈ 91 秒（含 18 秒启动探测），远低于 5 分钟** ✅。
→ 已修正 README 的两种方式，列出全部 6 项，并附一段**可选的一行脚本**自动替换。
（`docs/01-安装部署指南.md` 的 `## 5.` 一直列全了这 6 类条件——**是根 README 漏了**。）

> **为什么这条值得单列**：它是本项目**唯一一个由「按文档操作」发现的缺陷**。
> 之前所有缺陷都来自真跑接口、真点浏览器、真跑 Docker；
> 这一次是「装作第一次来的用户，照着 README 做」——**同一个项目，换个身份就有新发现**。

**★ 截图采集方式（诚实记录）**

`bsk screenshot` 在本机**只能成功一次**，之后持续 `tool RPC timed out after 30s`
（同期 `bsk evaluate` 正常；`bsk daemon restart` 能恢复一次但不足以支撑十几张图）。
因此改用**独立 headless Chrome + 同源引导页**：
用 HTTP 登录拿令牌 → 引导页把令牌写进 localStorage（同源）→ 每个端一个 `--user-data-dir`
→ 直接截目标页 → 收尾删除引导页并确认 `git status` 干净。

> ⚠️ **这是上游工具问题**（`bsk` 0.2.1 CLI / 1.1 协议 vs 扩展 0.3.0，存在版本漂移告警），
> 已记录供后续排查；**截图本身是真实服务渲染的真实页面**，只是采集通道换了。

**P11-C 的已知局限**

1. **截图由 headless Chrome 采集**，不是用户日常浏览器窗口（原因见上）。
2. **截图是静态画面**：不含弹窗 / 下拉 / Toast 等交互过程，也不含响应式适配截图。
3. **验证 2 的 72 秒偏乐观**：本机 pip 有缓存；冷机器首次安装依赖会更久。
4. **未在真正的另一台机器上验证**（同机不同目录），不能排除环境相关差异。
5. **`CHANGELOG.md` 未切版本号**：全部变更仍在 `[未发布]` 段，仓库也没有 git tag。
6. **`docs/12` 的「M5 未达成」已过期**：本次按约定未改动 `docs/`（见上方待办 ③）。

**★ 公开前整理（2026-09-17，用户提出「公开前的待办也帮我处理」）**

六项待办逐一处理（完整表格见 `todolist.md` 的「公开前整理」小节），其中两项产出**实质改进**：

| # | 待办 | 结果 |
|---|---|---|
| 1 | CI 未真实运行 | ✅ 本地可验项全验（YAML 可解析 5 job / 引用路径全在 / 命令与 Makefile 一致）；★ **修掉 e2e 重复跑**（每次 CI 白等约 5 分钟） |
| 2 | `CHANGELOG` 未切版本 | ✅ 切 **`[1.0.0] - 2026-09-17`** + 保留空 `[未发布]` + 更新链接 + 本地 git tag |
| 3 | `docs/12`「M5 未达成」过期 | ✅ 三处同步 |
| 4 | AI 未联调 / 合规未实现 | ✅ 无法真正实现 → 改为 README 顶部**不可错过** |
| 5 | 覆盖率无门禁 / 无压测 | ✅ CI 新增 **`--cov-fail-under=65`**（实测 70 留余量）；⚠️ 压测仍无法做 |
| 6 | PG / TLS / 多实例 | ✅ **PG 已真跑**（冒烟 **57/57**）+ **抓到 1 个真缺陷**；⚠️ TLS 与多实例仍未做 |

**★★ 真跑 PostgreSQL 抓到的真缺陷（SQLite 上永远复现不出来）**

| | |
|---|---|
| 代码 | `metrics_service.py` 的 `_date_between`：`func.date(col).between(d1.isoformat(), d2.isoformat())` |
| SQLite | `func.date()` 返回 TEXT → 字符串比字符串 → **正常，804 用例全绿** |
| PostgreSQL | `func.date()` 返回 `date` → 与 `varchar` 比较 → `UndefinedFunctionError: operator does not exist: date >= character varying` |
| 后果 | **运营看板的概览与快照重建两个最核心端点 500** |
| 修复 | 绑定 `date` 对象（两种方言都正确） |
| 守卫 | 新增 `tests/unit/test_sql_dialect_portability.py`：**不测行为、测绑定参数类型**；已实测旧写法会被拦下 |

> ★ **这条修正了 P10 的一个判断**：当时把「PostgreSQL 只验证了配置可解析」列为**可接受**的局限，
> 理由是「SQLite 全链路已实测」。事实证明不够 —— **一个核心页面在 PG 上直接 500**。
> 教训：**「配置能解析」与「服务能跑」之间的差距，比想象的大得多。**

**剩余待办（诚实记录）**：① CI 首次推送后必须实跑；② Nginx TLS 与多实例未验证；③ 压测未做；
④ 真实厂商联调与儿童数据合规未实现；⑤ 截图仍由 headless Chrome 采集（`bsk screenshot` 上游问题）。

### P11-B 验收记录（2026-09-17）

**★ 交付物：20 个文件 / 8177 行**（`learning/`，**不上传 GitHub**）

| 类别 | 文件 | 行数 | 内容 |
|---|---|---|---|
| 说明 | `README.md` | 194 | 12 段结构说明、三条阅读路径、阅读约定、12 份产物清单 |
| 地图 | `00-学习地图.md` | 203 | 依赖图（mermaid）、13 模块时长表、三个里程碑、**四条不能跳的依赖**、卡住时的降级办法 |
| 模块 | `01` 项目业务全景与四端角色 | 445 | 五类角色、14 步闭环、工厂跨租户与「两个 `NULL` 语义不同」 |
| 模块 | `02` 多租户 SaaS 基础概念 | 549 | 隔离三种做法、`scope.py` 六个函数、越权返回 404 的理由 |
| 模块 | `03` 需求分析与 PRD 怎么写 | 550 | PRD 七步写法、术语表为何最被低估、**「事后补写」的诚实说明** |
| 模块 | `04` 数据模型与 ER 图入门 | 549 | ER 符号、多对多拆表、索引按查询建、`in_stock` 同名不同义 |
| 模块 | `05` 状态机与流程图 | 550 | 红绿灯类比、订单 9 态回退边、设备四维、**解冻三条出边与 `ADR-10` 教训** |
| 模块 | `06` API 与前后端协作 | 550 | 餐厅点菜类比、幂等与 `traceId`、PM 怎么写 API 契约 |
| 模块 | `07` AI 能力与 Provider 抽象 | 528 | 同声传译类比、**为什么 `chat()` 必须是流式迭代器**、四层解析、内容安全顺序 |
| 模块 | `08` 设备接入与激活 | 528 | 两种方案全方位差异、激活 ≠ 绑定、绑定四步刻意顺序 |
| 模块 | `09` 权限与安全 | 550 | 门禁卡类比、**工厂两层保护**、白名单脱敏、密钥只进不出 |
| 模块 | `10` 测试与验收标准 | 547 | 装修验收类比、**覆盖率 70% 的真相**、「会静默通过的检查更危险」 |
| 模块 | `11` 部署与上线常识 | 550 | 开分店类比、容器五专属变量、**「本地能跑 ≠ 容器能跑」** |
| 模块 | `12` 作品集呈现与 GitHub 规范 | 550 | 诚实记录局限为何是加分项、提交规范、**最常见的三类追问** |
| 练习 | `exercises/01`–`06` | 各 203–228 | 每个含情境、任务分解、**评分要点**、**「被开发这样问说明哪里没想清」**、自查清单、进阶挑战 |

**★ 客观校验（一次性脚本，**刻意不入库**：`learning/` 不上传，提交的校验器会在 CI 与
他人机器上因目录不存在而失败）**

| # | 检查项 | 实测结果 |
|---|---|---|
| 1 | 20 个文件齐全 | ✔ 2 + 12 + 6 |
| 2 | 每模块含元信息块 + `## 0. 结论先行` + **恰好 12 个二级章节，编号与标题顺序符合规范** | ✔ 12/12 |
| 3 | 自测题含**参考答案** | ✔ 12/12 |
| 4 | 装饰性 emoji（只允许 `🔑 📌 ✅ ⚠️ ★ —`） | ✔ 0 处 |
| 5 | 第一人称「我」（**排除反引号内代码与引号内话术**） | ✔ 0 处 |
| 6 | 篇幅区间（300–650 行） | ✔ 最短 445 / 最长 550 |
| 7 | 真实代码标识引用（去重 ≥ 8） | ✔ 12/12 |
| 8 | **相对链接可解析** | ✔ **198 处**全部可解析 |

**★ 本阶段发现并修正的 6 处 `docs/` 与代码不一致**（这是本阶段最有价值的产出：

由 4 个AI Agent 在写作时按**代码**核对 `docs/` 而发现，我逐条复核后修正）

| # | 不一致 | 性质 | 处理 |
|---|---|---|---|
| 1 | `docs/08` 能力矩阵把 `mock` 的 `device` 写成「**不支持**（设备侧走平台自身逻辑）」，而 `mock/engine.py` 是 `capabilities = ALL_CAPABILITIES`（含 `device`），且 `provision_devices`/`activate_device`/`push_ota` 都返回 `simulated=True`；`base.py` 的 docstring 原话是「`LOCAL`（mock）—— **四类能力全有，否则离线跑不通全流程**」 | **真错**（能力矩阵错了一格，且与同节的解释自相矛盾） | ✔ 修正矩阵为「已实现（结果带 `simulated=True`）」，并补上「为什么 mock 声明全四类能力」；同时把这次纠错过程写进文档（**留痕比悄悄改掉更有价值**） |
| 2 | `docs/07` §6.4 与 `docs/11` 各有一处把 `factory:batch:read` 写成 **`factory:batch_read`**（漏一段冒号） | **真错**（读者会以为该码不存在），**且暴露了校验器的盲区** | ✔ 修正两处；★ **并给 `check_docs.py` 增加「格式写错」子检查**——原正则只匹配「格式正确的权限码」，所以错码**静默通过**了。**教训：只校验「正确的形状」会漏掉「形状都不对」的输入** |
| 3 | `make docs-check` 的类数在各处写得不一致：`docs/README` 写「六件事」、`docs/04`/`docs/09` 写「六类」、`docs/11` 写「七类」，而脚本实有 **8** 个检查函数 | **真错**（同一事实四处不同） | ✔ 统一为**八类**，并把 8 类逐条列出（端点 / 表 / 错误码 / 权限码 / 枚举值 / 引用 / 测试计数 / 边界声明） |
| 4 | `docs/04` 与 `docs/12` 写「**18 个菜单项未实现**（平台 7 / 商户 5 / 工厂 1）」——7+5+1=**13**，自相矛盾 | **真错**（算术不自洽） | ✔ 核实 `frontend/shared/app/menus.js` 的菜单项数与各端已注册路由后修正为 **13** |
| 5 | `项目进度.md` 与 `CHANGELOG.md` 写「**27 条术语表**」，实际 **24 条** | **真错**（我自己的计数错，且被代理按源码数出来） | ✔ 两处均改为 24 |
| 6 | `docs/05` §3.3 列 **19** 条 `idx_*` 复合索引，而 ORM 共 **20** 处 `Index(...)` 声明 | **口径差异**（都不是错，但读者会对不上） | ✔ 在手册中写清两个口径与差额来源；★ **并在 `docs/05` 新增 `## 3.4 外键是否真的生效`**——`session.py` 对每个连接执行 `PRAGMA foreign_keys=ON`（SQLite 默认**不**强制外键），这是一个「默认行为与直觉相反」的坑 |

> **★ 这一节的由来值得记下**：这 6 处**没有一处**是靠「读一遍文档」发现的——
> 4 个AI Agent 在写学习手册时**按代码逐项核对**发现 4 处，我自己核对计数发现 2 处。
> 其中第 2 处（权限码拼写）**在修正后又被新增的校验抓到第二次**（`docs/11` 里还有同源的一处）。
> 这与本项目一贯的结论一致：**文档的正确性靠「换个视角重新核对」，而不是靠重新读一遍**。

**P11-B 的已知局限**

1. **`learning/` 不上传 GitHub**（`.gitignore:15`，`git ls-files` 中 0 条）——
   因此本阶段**没有可提交的产物**，只有追踪文件的更新会进提交。
2. **校验只覆盖结构，覆盖不了内容**：12 段齐全、无装饰 emoji、链接可解析都能机械验证；
   但「讲得对不对、类比是否恰当、难度是否适合初级 PM」**只能人工判断**。
3. **未做真人试读**：没有一个真实初级 PM 读过并反馈「哪一段看不懂」——
   所有难度判断都是作者的自我估计。
4. **篇幅硬上限导致压缩**：每模块压到 550 行以内，模块 03 的 RICE/MoSCoW/Kano、
   模块 04 的「主键/外键」与「实体/属性」、模块 12 的自检评分表都被合并或一句话带过。
5. **无截图与配图**：手册全部用 mermaid 文本图。
6. **模块 04 的索引条数有两个口径**（19 条表格 vs 20 处 ORM 声明），手册写清了但读者仍需注意。
7. **结构校验器不入库**：它是一次性工具，**没有单元测试、不在 CI 中**；
   它靠「首跑报出 11 处违规（含我校验器自身的 2 个 bug）→ 修正后转绿」证明有效。

### P11-A 验收记录（2026-09-17）

**★ 交付物**

| # | 交付物 | 说明 |
|---|---|---|
| 1 | `docs/README.md` | 文档导航：按「你想做什么」指路，含三条阅读路径 |
| 2 | `docs/01-安装部署指南.md` | 711 行。环境要求 / 本地三步 / Docker 三 profile / **80 个环境变量分 11 类** / 启动期拒绝条件 / 迁移与种子 / **故障排查（9 条真实踩坑）** / 生产加固清单 |
| 3 | `docs/02-系统流程图.md` | 全 mermaid。业务闭环 / 订单 9 态 / 资产 10 态 / 激活分支 / 批次导入 / 分配与绑定 / 对话时序 |
| 4 | `docs/03-系统架构图.md` | 六层架构 / 部署拓扑 / 模块依赖 / 隔离层 / 数据流 / **技术选型与替代方案对比** |
| 5 | `docs/04-产品需求文档PRD.md` | 定位 / 6 角色 + 终端用户 / **24 条术语表** / 页面清单（含 13 个未实现菜单项）/ 权限矩阵 / 7 条设计原则 |
| 6 | `docs/05-数据模型与ER图.md` | **1809 行**。8 张分域 ER 图 / **46 张表 × 655 个字段的完整字段字典**（由 ORM 元数据生成）/ 六条复合唯一键 / **`in_stock` 语义对齐** |
| 7 | `docs/06-API接口文档.md` | 通用约定 / **31 个错误码** / **145 个端点表**（与快照双向核对）/ WebSocket 帧协议 / 9 组示例 |
| 8 | `docs/07-多租户与权限设计.md` | 隔离层级 / **54 个权限码** / 6 角色矩阵 / 越权测试策略 / 脱敏规则 / 令牌隔离 |
| 9 | `docs/08-AI能力接入设计.md` | 能力矩阵 / Provider 抽象 / **四层解析顺序** / 离线引擎 / 4G 与 Wi-Fi 差异 / 火山与百度指引 / 密钥管理 |
| 10 | `docs/09-系统交付标准.md` | **逐条可勾选的功能验收清单**（约 90 条）/ 性能 SLO / 安全基线（已实现 vs 未实现）/ **缺陷等级定义** / 验收流程与模板 |
| 11 | `docs/10-AI验证可用性.md` | 验证目标 / 环境 / **24 用例数据集** / 指标定义（两栏判定）/ **实测基准数据表** / 复现命令 / 结论与 10 条局限 |
| 12 | `docs/11-测试与质量保障.md` | 测试金字塔 / **覆盖率 70% 如实记录** / 隔离矩阵 / 四类契约 / CI / **`P-01`~`P-08` 对照 + 20 条自发现缺陷** / 测试方法论 |
| 13 | `docs/12-项目复盘.md` | 整合决策 / 架构演进（`ADR-09`/`ADR-10` 的由来）/ 12 个关键问题 / 度量 / 不足与路线图 / **8 条经验沉淀** |
| 14 | `scripts/check_docs.py` + `make docs-check` | **文档一致性门禁（8 类校验）**，已接入 `make check` 与 CI |
| 15 | `docs/14` 补充 | 补上「已知局限与取证边界」附录（原缺，门禁报出后补齐） |

**★ 客观凭据：`make check` 全绿（含新接入的文档门禁）**

| 门禁 | 命令 | 实测结果 |
|---|---|---|
| 代码规范 | `make lint` | ✔ `All checks passed!`（含 `tests/` 与 `scripts/`） |
| 静态类型 | `make typecheck` | ✔ `no issues found in **95 source files**` |
| 测试 | `make test` | ✔ **804 passed in 310.55s (0:05:10)** |
| 接口契约 | `make openapi-check` | ✔ 与快照一致（119 paths / 145 operations） |
| **文档一致性** | `make docs-check` | ✔ **8 类校验全部通过** |
| 一键门禁 | `make check` | ✔ **全部检查通过** |

**★ 文档门禁的八类校验（实测输出）**

| # | 校验 | 结果 |
|---|---|---|
| 1 | `docs/06` 端点表 vs OpenAPI 快照（**双向**） | ✔ 一致（**145** 个端点，`paths` **119**） |
| 2 | `docs/05` 字段字典表名 vs 迁移/模型（并交叉校验两者一致） | ✔ 覆盖 **46** 张表 |
| 3 | `docs/06` 错误码 vs `ErrorCode` + **HTTP 状态 vs `_STATUS_MAP`** | ✔ **31** 个，状态全部一致 |
| 4 | `docs/07` 权限码 vs `permissions.py`（双向） | ✔ **54** 个全部已注册 |
| 5 | `docs/05` 状态枚举取值 vs `enums.py` 成员 | ✔ **104** 个取值均真实存在 |
| 6 | 文档引用的 `make` 目标 / `scripts` 文件 / 相对链接 | ✔ 全部可解析 |
| 7 | `docs/11` 逐文件用例数 vs 源码 | ✔ **22** 个文件一致（合计 **545** 个测试函数） |
| 8 | 每份文档含「已知局限 / 取证边界」；无不上传目录的内部路径 | ✔ **14** 份文档全部合规 |

**★ 门禁第一次运行就报出 5 处问题（这正是它的价值）**

| # | 问题 | 性质 | 处理 |
|---|---|---|---|
| 1 | `docs/11` 把 `test_allocation.py` 写成 **33** 个用例（实际 **23**） | **真错**（手写数字错，且会让分组合计对不上 353） | ✔ 修正为 23，并把「逐文件用例数 vs 源码」加为第 7 类校验 |
| 2 | `docs/14` **缺少「已知局限 / 取证边界」小节** | **真缺口**（既有文档未达本阶段自定标准） | ✔ 补 8 条边界声明 |
| 3 | `docs/07` 把**不存在的**权限码 `factory:ship:write` 写在反引号里 | **真错**（会误导读者以为该码存在） | ✔ 改写为「**没有**单独的出货权限码」 |
| 4 | `docs/README.md` 的检查规则描述里含字面 `make xxx` 占位 | **误报**（文档在描述规则本身） | ✔ 改写措辞，避免与「引用必须存在」冲突 |
| 5 | 4 份文档含 `learning/` 字样（均在说明「该目录不上传」） | **误报**（风险是**断链**，不是提到目录名） | ✔ **收窄校验规则**为「禁止指向其内部的路径/链接」——校验器本身也需要被审视 |

> **第 5 条的复盘价值**：校验器第一次跑就暴露了**它自己的规则过严**。
> 如果当时的反应是「把文档改到不提 `learning/`」，就会为了迁就工具而牺牲文档的准确表述。
> 正确做法是**修订规则以匹配真实风险**（断链），而不是让内容变形。

**★ 本阶段的三条做法（可复用）**

| # | 做法 | 具体体现 |
|---|---|---|
| 1 | **用脚本生成，而不是手抄** | `docs/05` 的 655 个字段**由 ORM 元数据程序化导出**；`docs/06` 的 145 个端点**由源码 AST + 快照联合生成**，且生成时**断言两侧集合相等**，不一致就拒绝生成 |
| 2 | **把「文档与代码脱节」变成红灯** | `make docs-check` 接入 `make check` 与 CI，八类校验 |
| 3 | **诚实记录未验证项** | `docs/10` 明确标注「全部数据来自离线引擎」；`docs/11` 如实写「覆盖率 70%，三个 P9 服务 < 31%」；`docs/12` 把「M5 未达成」写进度量表 |

**★ P11-A 的已知局限（7 条，诚实记录）**

1. **文档集不含截图**（`docs/assets/` 为空）。→ 读者看不到界面，需自己跑起来；属 P11-C。
2. **`docs/04` PRD 是事后补写**：描述的是**已交付**能力而非事前需求假设，无用户调研、竞品数据与需求优先级。
3. **`docs/10` 的基准数据来自离线模拟引擎**（24 用例、单机单进程、`MOCK_LATENCY_MS=120` 人为延迟），
   **不代表真实厂商链路**，也不能代表大模型的对话质量。
4. **门禁只保证「名字与数字对得上」**：端点/表/错误码/权限码/枚举/引用可机械校验，
   但**叙述性描述与设计解释无法校验**，仍需人工评审。
5. **`docs/05` 的字段名与类型由 ORM 元数据导出，未逐列核对 SQLite 实际 DDL**（SQLite 类型亲和性会改变实际存储类型）。
6. **`scripts/check_docs.py` 自身没有单元测试**（覆盖率统计只覆盖 `app`）：
   它靠「首跑报出 5 处问题、修正后转绿」证明有效，属**行为验证**而非测试验证。
7. **`docs/03` / `docs/12` 里「为什么这样分层 / 这样决策」的解释属事后归纳**，不是当时的决策记录。

### P10 验收记录（2026-09-17）

**门禁（全部实测通过）**

| 验收项 | 命令 | 结果 |
|---|---|---|
| 代码检查 | `make lint` | ✔ ruff 0 问题（含 `tests/` 与 `scripts/`） |
| 类型检查 | `make typecheck` | ✔ mypy strict，**95 个源文件**无问题 |
| 测试 | `make check` | ✔ **804 passed in 476.25s**（P10 无新增单测，全部既有测试未回归） |
| 契约一致性 | `make openapi-check` | ✔ OpenAPI 与快照一致 |
| 一键质量门禁 | `make check` | ✔ 全部通过 |
| 前端导入契约 | `make fe-check` | ✔ 68 个 JS 模块、**399 处**导入全部匹配 |

**★ 四项验收标准（全部实测达成）**

| 验收项 | 怎么验的 | 结果 |
|---|---|---|
| **一键起服务成功** | `docker compose up -d --build` | ✔ 镜像构建成功，容器进入 `healthy`（非 root 用户运行、健康检查通过） |
| **全端点冒烟通过** | `make smoke`（`scripts/smoke_test.sh`，57 项断言） | ✔ **57/57 直连通过**；**再经 Nginx（`:8080`）跑一遍同样 57/57**；脚本自带 `MIN_EXPECTED_CHECKS=25` 守卫，断言数不足时判「脚本异常」而非「通过」 |
| **数据持久化正常** | `docker compose down` → `up` → 重新统计 | ✔ 起停前后**完全一致**：租户 2 / 设备 11 / 订单 2 / 工厂工单 0（`down && up` 不丢数据） |
| **弱口令扫描通过** | `python3 scripts/scan_secrets.py` | ✔ 扫描 **236 个被跟踪文件，0 命中**；`.env` 未被 git 跟踪；扫描器另有「规则确能命中」的自测 |
| **WebSocket 经 Nginx 通** | `ws://localhost:8080/ws/miniapp/chat`（用真实小程序的 URL 形态） | ✔ 握手成功建立、随后以 `4401`（非法令牌）关闭——**这正是修好 `/ws/` location 的证据**：修复前握手会落到 SPA 回退、拿回一段 HTML |

**本阶段由「真跑一遍 Docker」发现并修复的 12 个真问题**（全部属于「文档写了但实际跑不通」）

| # | 问题 | 为什么集成测试发现不了 | 处理 |
|---|---|---|---|
| 1 | **`make up` 是坏的**：Makefile 写 `cd deploy && docker compose up`，而 compose 文件在仓库根 | 只跑 `make test` 不会执行 Makefile 的 docker 目标 | ✔ 改为在仓库根执行，并拆出 `up-nginx` / `up-full` |
| 2 | **nginx 的 WebSocket location 写成 `/api/v1/ws/`**，真实路径是 `/ws/miniapp/chat` → 握手落到 SPA 回退、返回 HTML | 本地不经 nginx，WS 直连 8000 端口一切正常 | ✔ 改为 `location /ws/`；用真实 WS 握手验证（见上表末行） |
| 3 | **nginx 对未指纹化的 JS/CSS 声明 `immutable` 强缓存 1 年** → 发版后用户卡在旧代码且无法失效 | 本地开发不走缓存 | ✔ 改为 `no-cache, must-revalidate`；图片/字体仍给 30 天 |
| 4 | **compose 的 `DATABASE_URL: ${DATABASE_URL:-…}` 读到了开发机 `.env` 的相对路径** → SQLite 落到镜像层而非卷，`down && up` 丢数据 | 容器不重建时看不出差别 | ✔ 引入容器专属变量 `APP_DATABASE_URL`，默认 `sqlite+aiosqlite:////data/toyverse.db`（绝对路径 + 挂在卷上） |
| 5 | **缺 `.dockerignore`** → 构建上下文含 229MB 的 `.venv`，且 `.env` 有被 `COPY . .` 带进镜像的潜在风险 | 构建慢/泄漏都只在构建时暴露 | ✔ 新增 `.dockerignore`（排除 `.env`、`.venv`、`data/`、`learning/`、`docs/`、`*.md`） |
| 6 | **`POSTGRES_PASSWORD: ${…:?}` 在未启用该 profile 时也参与插值** → 没配 PostgreSQL 口令的 `.env` 让整个 compose 不可用 | 同上（插值发生在 profile 判定之前） | ✔ 改为 `${POSTGRES_PASSWORD:-}` 并加注释说明失败仍是安全的（postgres 镜像本身会拒绝空口令） |
| 7 | **`--workers 4` 下每个 worker 各播种一次** → 4 进程并发写同一 SQLite（实测 1 成功 3 失败），且赢得竞争的 worker 可能只写一半 | 本地 `make dev` 是单进程 | ✔ 设 `SEED_AT_STARTUP=false`，改为在 CMD 里 fork **之前**播种一次 |
| 8 | **`FRONTEND_ROOT` 按「`__file__` 上溯三级」推算** → 容器布局没有 `backend/` 层，数到了 `/` → **整个 UI 404 而 API 正常**，日志只有一条 WARNING | 本地布局恰好是三级 | ✔ 新增 `_guess_repo_root()` / `_resolve_frontend_root()`（支持 `FRONTEND_DIR` 显式覆盖），compose 设 `FRONTEND_DIR=/app/frontend` |
| 9 | **`DATA_ROOT` 不可配置 + 进程非 root** → 容器启动即 `PermissionError: /app/data` 崩溃循环 | 本地开发目录可写 | ✔ 新增 `DATA_DIR` 变量（compose 设 `/data`），`ensure_runtime_dirs()` 把 `PermissionError` 转成**可操作的** `InsecureConfigurationError` |
| 10 | **nginx 缺 `/login`**（它是**应用路由**不是磁盘文件）→ 经 nginx 访问登录页 404 | 直连时由 FastAPI 提供 | ✔ 加 `location = /login` 显式反代；因配置是 bind-mount，改后需 `docker compose restart nginx` |
| 11 | **`smoke_test.sh` 的 `json_get` 用 heredoc 读程序、又想从 stdin 读响应体** → 取值恒为空，**登录永远失败**（而同一口令用 curl 直连成功） | 脚本自身没跑过 | ✔ 改用 `python3 -c` 传程序 |
| 12 | **同一脚本在误删计数函数后仍打印「0 通过 / 全部通过」**——**会静默通过的检查比没有检查更危险** | 只有真跑并核对断言数才会发现 | ✔ 恢复 `ok/bad/skip` 并加 `MIN_EXPECTED_CHECKS=25` 守卫 |

**本阶段交付的部署与质量资产**

| 资产 | 说明 |
|---|---|
| `docker-compose.yml`（仓库根，P0 草案 → 可用） | 三个 profile：默认（app，SQLite）、`nginx`（前置反代）、`postgres`（可选数据库）；`env_file` 用 `required: false` 让缺 `.env` 也能起；`APP_ENV` 默认 `development` |
| `deploy/Dockerfile.backend` | 多阶段构建、非 root 运行；CMD 改为 `alembic upgrade head && python -m app.db.seed && uvicorn … --workers ${WORKERS:-4}`（迁移与播种在 fork 之前完成） |
| `deploy/nginx.conf` | 修正 WS location、缓存策略、`/login` 反代、逐端 SPA 回退（`/platform/` `/merchant/` `/factory/` `/miniapp/` 各自回退到自己的 `index.html`） |
| `deploy/.env.example` | **生产部署清单**而非开发模板副本：必须修改项、「不安全就拒绝启动」的三类硬校验、容器专属变量、部署后自检与回滚步骤 |
| `.dockerignore`（新增） | 构建上下文从 229MB 降下来，并杜绝 `.env` 进镜像 |
| `.github/workflows/ci.yml`（新增） | 五个并行 job：lint / test / contract / frontend / secrets |
| `scripts/smoke_test.sh`（新增，~330 行） | 57 项断言，按真实角色登录后逐端点请求、以只读为主，带最低断言数守卫 |
| `scripts/scan_secrets.py`（新增） | 四类规则：弱口令 / 硬编码密钥 / **出参模型里的明文密钥字段** / 敏感文件入库；**误报经两轮收敛**（见下） |
| `scripts/gen_qrcodes.py` / `reset_db.sh` / `dev.sh`（新增） | 演示二维码清单、带备份与二次确认的库重置、开发机启动脚本 |
| `backend/app/db/seed.py` | 补 `if __name__ == "__main__":`，使 `python -m app.db.seed` 可在容器 CMD 中直接调用 |

**敏感信息扫描器的误报收敛**（一个本身值得记录的教训）

首版扫描器报出 **133 条误报**，全部来自「规则太宽」而非真有泄露：手机号 `13812345678` 命中「8 位连续数字」的弱口令规则、HTML 的 `placeholder=` 被当成密钥赋值、黑名单表里的枚举字面量、`*SCREAMING*` 常量、URL 路径常量、以及请求 DTO 里**本来就要收明文**的 `access_key`。处理：弱口令表收窄并只匹配**赋值形态**；给测试目录加 `ALLOWED_PATH_PREFIXES`；新增 `_looks_like_a_real_secret()`；把「明文密钥字段」规则**限定到响应类**（请求 DTO 收明文是正常的）。收敛后 **0 命中**，并补了一个自测证明规则**确实会命中**——**「规则不响」与「规则坏了」必须能区分开**。

**门禁与验收的完整证据链**

| 证据 | 结果 |
|---|---|
| 容器健康 | `docker compose ps` → `healthy` |
| 冒烟（直连） | `make smoke` → 57/57 |
| 冒烟（经 nginx） | 57/57（`:8080`） |
| WS 经 nginx | 握手成功 → `4401` 关闭（证明路由正确，非 HTML 回退） |
| 持久化 | `down && up` 前后计数一致（租户 2 / 设备 11 / 订单 2 / 工厂工单 0） |
| 敏感信息 | 236 文件 / 0 命中；`.env` 未被跟踪 |
| 全量回归 | `make check` → 804 passed、mypy 95 文件、契约一致 |
| 前端导入 | `make fe-check` → 399 处全部匹配 |

**已知局限（7 条，诚实记录）**

1. **CI 未在真实 GitHub 上跑过**（本机无 remote，无法推送到 Actions）。`ci.yml` 的命令与本地 `make check` 逐字一致，但「YAML 语法 + 表达式」只做了本地静态审查，**首次推送后需要实跑确认**。
2. **未在 CI 里构建镜像**：镜像构建含 pip 全量安装（分钟级），本阶段把它留在部署流程而非每次 CI（理由写在 `ci.yml` 的注释里）。
3. **`docker compose --profile postgres` 只验证了配置可解析**，未真跑 PostgreSQL 实例；到 asyncpg 的连接串与实际迁移未在 PG 上执行过（SQLite 全链路已实测）。
4. **Nginx 只验证了 HTTP 与 WebSocket 转发**，未验证 TLS（配置里预留了 80→443 的注释段）、未验证多实例负载均衡（`upstream` 只有一个 server）。
5. **`scripts/reset_db.sh` 的备份策略是「同盘备份」**：宿主机磁盘故障时备份与数据一起丢，生产应改用异机/对象存储备份。
6. 容器内以 4 个 uvicorn worker 运行，而 **SQLite 的写并发能力有限**：演示与轻负载没问题，真实多租户并发写应切 PostgreSQL（compose 已提供 profile）。
7. **冒烟脚本只覆盖读路径**（+2 个幂等写）：写路径的验证依赖 `make test`（隔离测试库）。

### P9 验收记录（2026-09-17）

**门禁（全部实测通过）**

| 验收项 | 命令 | 结果 |
|---|---|---|
| 代码检查 | `make lint` | ✔ ruff 0 问题（含 `tests/`） |
| 类型检查 | `make typecheck` | ✔ mypy strict，**95 个源文件**无问题 |
| 测试 | `make test` | ✔ **804 passed**（P9 新增 46 条） |
| 契约一致性 | `make openapi-check` | ✔ OpenAPI 与快照一致（P9 新增 34 个端点） |
| 一键质量门禁 | `make check` | ✔ 全部通过 |
| 前端导入契约 | `make fe-check` | ✔ 68 个 JS 模块、**399 处**导入全部匹配 |
| 迁移可逆与零漂移 | `alembic upgrade head` → `downgrade 0014` → `upgrade head`；`alembic check` | ✔ 0015 升降可逆；**零漂移** |

**★ 遗留缺陷 P-07 / P-08 的守卫（本阶段核心）**

| 断言 | 实测 |
|---|---|
| **P-07 维度数据真实汇总** | ✔ `trend`（快照之和）**等于** `overview`（实时聚合）的交互数与助手消息数；24 小时分布**不是平的**（种子小时刻意错开，实测 4 个不同值） |
| **P-08 按产品隔离** | ✔ 两个产品的交互数（6 vs 30）、会话数、活跃设备数**均不同**；地域设备数之和 = 该产品地域已知设备数；内容榜按产品分别聚合且 hits 降序、rank 从 1 连续 |
| 重建的边界 | ✔ 只写被请求的产品（另一产品的快照表零行）；**幂等**（连跑两次行数一致、无重复行） |
| 留存口径 | ✔ 分母为 0 时比率为 `null` 而非 0（不把「未知」伪装成「零」） |
| **OTA 仅平台可见** | ✔ 商户 token 访问 `/platform/ota/*` → 403 `PERMISSION_DENIED`（含触发推送） |
| **OTA 拒绝 Wi-Fi** | ✔ 409 `OTA_NOT_SUPPORTED`，`details` 带 `otaSupport/cloudVendor/cloudProviderName`；**且不留任何推送记录**（「拒绝」是真的没发生） |
| OTA 4G 正常路径 | ✔ 推送成功逐台 `SUCCESS` 且**设备固件版本被更新**；重推计 `skipped`；混入他人设备只该行失败；扩展名白名单与版本唯一性校验生效 |

**浏览器实测（真实服务 + 本机 Chromium，6 项）**

| 验收项 | 操作方式 | 结果 |
|---|---|---|
| 我的产品列表（P9 富化） | 商户登录 → `#/products` | ✔ 列含 设备数 / 已激活 / 累计交互 / AI 配置；**两产品的交互数不同（6 vs 30）**——P-08 隔离在界面上可见 |
| **AI 配置：角色分区按联网方式启用** | `#/ai-config?productId=prod-t001-cube`（Wi-Fi）→ 切到 4G 产品 | ✔ Wi-Fi 下 `rolePresetCode` **disabled** 并显示「Wi-Fi 方案的对话角色由厂商侧智能体配置决定……本分区整体禁用——不是页面故障」；切到 4G 后 `disabled=false`、「平台支持：支持」 |
| **AI 配置：分区独立保存** | 切换「大模型审核」开关 → 保存内容安全分区 | ✔ 保存成功，底部「当前生效说明」随之更新为「每次回复在下发到设备前额外经过一次大模型审核，更稳妥，但首字延迟会略增」+「配置最后更新：刚刚」 |
| **运营看板：实时聚合与快照分离** | `#/metrics?productId=prod-t001-4g` | ✔ 明确标注「数据来源：实时聚合 / 快照未生成」；概览卡 13 项（设备 1 / 活跃 1 / 总交互 6 / 会话 3 / 平均延迟 209.5ms / 安全拦截 1 / 内容命中 3 …）；「活跃设备占比 —」（无数据显示破折号而非 0%） |
| **运营看板：刷新快照** | 点「刷新快照」→ 确认框 → 开始重建 | ✔ toast「快照已重建：日汇总 7 行 / 小时 168 行 / 地域 7 行 / 内容 3 行（覆盖 7 天）」，页面状态转「快照已生成」，日趋势与 24 小时热力随即出现数据（33 个 SVG 图表元素） |
| **OTA：Wi-Fi 拒绝 + 4G 诚实失败** | `#/ota` → 推送 → 选 Wi-Fi 产品 | ✔ **Wi-Fi**：409，弹窗内明示「火山引擎智能云（硬件对话智能体）的 OTA 推送能力为 UNSUPPORTED……**这不是设备选择错误，换成其它设备也不会成功**」（厂商名是数据驱动的，正确显示火山而非写死的京东）；**4G**：因演示包未上传文件，逐台 `失败 1.2.3 → 1.3.0 固件包没有上传文件，无法推送`——ADR-07 在 UI 上可见 |

> 一次**假警报**已排除：`bsk click`（坐标点击）对页头按钮无效，我一度以为「刷新快照」按钮没接线；改用合成 `.click()` 后确认处理逻辑正常（确认框会弹出、请求会发出），属 bsk 的命中测试问题而非产品缺陷。**这正是「先用另一种方式复核再下结论」的价值**。

**本阶段发现并修复的 3 个真缺陷**

| # | 缺陷 | 级别 | 处理 |
|---|---|---|---|
| 1 | **小时分布聚合错误**：`rebuild_metrics` 复用了「聚合基座」`_message_base`（`select(func.count())` 起手）再叠 `created_at`，生成 `SELECT count(*), created_at FROM ...` **没有 GROUP BY** → SQLite 只返回**一行**（count=6、`created_at` 取自任意一条），于是 24 小时分布被算成「某个小时 1 次」而不是「9 点 3 次、14 点 3 次」。总量对不上但不报错，属典型「看板数字悄悄错」 | 真缺陷（数据错误） | ✔ 改为显式取行（`select(DialogueMessage.created_at).select_from(...).join(...)`），并给 `_message_base` 补了「⚠️ 只用于聚合」的警告 docstring |
| 2 | **同一路径两个处理函数**：P4 的 `merchant.py` 与 P9 的 `merchant_ops.py` 都定义了 `/merchant/products` 与 `/merchant/products/{id}`，生效取决于 `router.py` 的 include 顺序——「改了别处 import 顺序就静默换实现」的隐性缺陷 | 真缺陷（架构） | ✔ 收敛为一个**富化版本**（列表也带设备计数与 AI 摘要，避免前端逐行回查详情造成 N+1），删除 P4 版并在原处留下说明 |
| 3 | **演示数据时间戳落在未来**：种子把「今天」的会话放在 09:00/14:00，凌晨（UTC）运行时 `created_at` 属于未来，按 `created_at <= now` 的聚合把它们排除 → 「今天」在图表上恒为空 | 真缺陷（演示数据） | ✔ 种子对 `day_offset == 0` 的小时做钳制，保证落在已过去的时刻 |

**另修 3 处「写死厂商名」的文案**（角色不支持原因、OTA 弹窗两处）：判定本就数据驱动（`ota_support` / 联网方式），文案却点名「京东 JoyInside」，而演示的 Wi-Fi 产品实际用火山——改为只描述规则、不点名厂商，避免换供应商后文案变误导。

### P8 端到端接口验收（2026-09-17，真实服务 + 真实 HTTP，50/50 通过）

脚本 `/tmp/p8_acceptance.py`（一次性验收脚本，未入库）。

| 验收项 | 实测结果 |
|---|---|
| 验证码链路 | ✔ 非法手机号 400；mock 通道**回显验证码并标注 `mock: true`**；错误码 400 且带 `attemptsLeft` |
| **令牌类型双向隔离** | ✔ 管理端令牌调 `/miniapp/profile` → 401；小程序令牌调 `/platform/devices` → 401 |
| 扫码解析双分支 | ✔ JX → `4G`、JD → `WIFI`；非法载荷 404 `QR_INVALID`；SN 不存在 404 `DEVICE_NOT_FOUND` |
| **4G 激活** | ✔ `NOT_ACTIVATED → ACTIVATED` + `BOUND`，响应 `mock: true`（离线模拟引擎）；重复激活幂等 |
| **Wi-Fi SN 不一致** | ✔ 409 `BIND_FAILED`，`details` 带 `qrSn` / `reportedSn`，且设备 `activationStatus` 落库为 `BIND_FAILED`（走平台端接口核对） |
| Wi-Fi 正常激活 | ✔ 修正 SN 后 200 `ACTIVATED`（失败可恢复） |
| 设备列表 | ✔ 2 台，含派生 `online` 布尔 |
| 设备设置 | ✔ 默认值（volume 60 / childMode false / wakeWord）；更新生效；未知键被白名单挡掉；音量越界 400 |
| **仅 4G 支持充值** | ✔ 4G → 3 个套餐；**Wi-Fi → `supported: false` + 原因 + 空数组（不是报错）** |
| 充值下单 / 支付 | ✔ 201 且金额为套餐快照；mock 支付 → `PAID` + `mock: true`；重复支付 409 `INVALID_STATE_TRANSITION` |
| **对话流（SSE）** | ✔ `session.ready` → `assistant.delta` × 8（真分块）→ `assistant.done` 带 `messageId` |
| **内容安全** | ✔ 命中 → `safetyFlag=KEYWORD` + `blocked=true`，且被拦截原文不出现在回复里 |
| **跨用户隔离** | ✔ 他人设备详情 / 改设置 / 解绑一律 404；新用户设备列表 0 条 |
| 解绑 | ✔ `bindStatus → UNBOUND`、资产状态回 `ALLOCATED`、**激活状态保持 `ACTIVATED`** |

**门禁（全部实测通过）**

| 验收项 | 命令 | 结果 |
|---|---|---|
| 代码检查 | `make lint` | ✔ ruff 0 问题（含 `tests/`） |
| 类型检查 | `make typecheck` | ✔ mypy strict，**87 个源文件**无问题 |
| 测试 | `make test` | ✔ **758 passed**（P6 基线 737 → P8 新增 20 + 全闭环 e2e 1） |
| 契约一致性 | `make openapi-check` | ✔ OpenAPI 与快照一致（P8 新增 18 个端点） |
| 一键质量门禁 | `make check` | ✔ 全部通过 |
| 前端导入契约 | `make fe-check` | ✔ 62 个 JS 模块、**344 处**导入的路径与具名导出全部匹配 |
| 迁移可逆与零漂移 | `alembic check` | ✔ 0014 落地且**零漂移** |

**浏览器实测（真实服务 + 本机 Chromium，11 项）**

| 验收项 | 操作方式 | 结果 |
|---|---|---|
| 小程序加载（9 屏拆分后） | 打开 `/miniapp/` | ✔ 手机壳正常渲染，**控制台无应用错误**（仅 Chrome 扩展告警），扫码页两个演示设备按钮就位 |
| 未登录扫码 | 点「4G 演示设备」 | ✔ 被引导到登录页并提示「请先登录」（401 后的正确降级） |
| 验证码登录 | 填手机号 → 获取验证码 | ✔ **mock 验证码自动填入输入框**（`755194`），登录成功回到扫码页（该用户尚无设备） |
| 扫码 → 分派路径 | 点「4G 演示设备」 | ✔ 进入「开机激活」页，显示真实 SN / IMEI / 型号 / 固件 / 归属商户 / 云服务商 |
| 4G 激活 | 点「我已完成开机，开始激活」 | ✔ 激活成功页显示设备信息、厂商返回、并标注「演示环境：厂商通道为模拟实现」 |
| 首页 | 进入首页 | ✔ 设备卡（产品名 / SN / 离线）、功能宫格含「流量充值」（4G 才显示）、底部 Tab 就位 |
| **发现真缺陷** | 点「对话」Tab | ✘ → 修复后 ✔ 修复前被 WS `4403` 拒绝：「这台设备不属于当前账号，无法对话」 |
| 修复后复核 | 走真实 WS 连接 | ✔ `session.ready` → 完整对话到 `assistant.done{messageId, latencyMs}`（详见下方缺陷记录） |
| 对话降级提示 | — | ✔ 连接失败时提示降级路径 |
| 充值入口（4G） | — | ✔（接口层已验收；界面入口已确认渲染） |
| 设置 / 解绑 | — | ✔（接口层已验收） |

**本阶段由浏览器实测发现并修复的真缺陷**

| # | 缺陷 | 级别 | 处理 |
|---|---|---|---|
| 1 | **「已激活但未绑定」时重复激活只回放结果、不补绑定** → 用户看到「激活成功」，但库里没有绑定关系，一进对话页就被 WS `4403` 拒绝（「这台设备不属于当前账号」），且界面上看不出原因。触发场景很常见：设备在别处激活过，或上一位用户解绑过（**解绑刻意不改变激活状态**，所以「已激活 + 未绑定」是合法状态） | 真缺陷（功能不可用） | ✔ 幂等分支改为**同时保证绑定关系**（已绑当前用户则空操作、未绑定则补绑、绑给别人则 409）；`_bind_to_end_user` 增加「同用户已绑定 → 空操作」的早返回（否则 `bind_count` 会被重复激活灌水）；补回归测试 `test_reactivate_after_unbind_rebinds` |

**另由代理报出并处理的问题**

- **既有 flaky 测试**（约 1/16 概率失败）：`test_binding.py` 的「篡改 JD 签名末位」用例写作 `payload[:-1] + "0"`，而末位本身就是 `"0"` 时等于没篡改 → 改为显式翻转末位，并加断言保证「改动确实发生」
- **4G 激活演示路径默认 503**：演示模板的 `vendor` 是真实厂商（集贤 / 火山）且无密钥，按 ADR-07 会正确返回 503，但那样两条激活链路都演示不了。已补种子 `DEMO_AI_CONFIGS`：把两个**演示客户产品**显式配成离线模拟引擎；未配 AI 配置的产品（如新建的）**依然 503**，ADR-07 行为仍可观测
- `frontend/shared/core/ws.js` 与真实协议四处不符（见上方遗留观察①）——P8 用「继承并覆盖」绕开，未改共享模块
- `app/services/miniapp_service.py` 同一处相邻两行的 `detail=` 与 `details=` 是不同用途的参数，命名极易看错（代理报备，已确认无功能影响）

**遗留观察（不影响 P8 判定）**

1. `end_users` / `recharge_plans` / `recharge_orders` **由 P8 而非 P9 落地**（P8 的登录与充值直接依赖它们），迁移编号据此由 P9 的 `0012` 改为 `0014`，**P9 顺延为 `0015`**。
2. 短信与支付都是**可配置的模拟通道**（`MINIAPP_SMS_PROVIDER` / `PAYMENT_PROVIDER`）：mock 时验证码回显、支付直接置成功并显式标注 `mock: true`；**生产环境（`APP_ENV=production`）禁止 mock，启动期即拒绝**——与既有 DEBUG / SEED_DEMO_DATA / localhost CORS 同一处置方式。
3. 内容安全目前是**离线关键词级**拦截（`SAFETY_KEYWORDS` 六个词），完整词库与 LLM 复核归 P9 的商户端配置。
4. 语音（`user.audio` / `assistant.audio`）后端帧与 `provider.asr/tts` 已就绪，前端未实现录音与播放。
5. 设备昵称字段占位（`devices` 表无该列）。
6. 验收在开发库留下痕迹（终端用户 `13900001234` / `13700009999` / `13700007777` 及其设备绑定与充值订单）；`make reset-db && make seed` 可回到纯净演示态。

---

### P6 端到端接口验收（2026-09-17，真实服务 + 真实 HTTP，71/71 通过）

脚本 `/tmp/p6_acceptance.py`（一次性验收脚本，未入库）。覆盖：派单 → 烧录（含超量拒绝）→ 抽检（含三类非法 SN）→ 出货 → 入库 → 分配 → 冻结，以及脱敏红线、跨厂隔离、权限边界。

| 验收项 | 实测结果 |
|---|---|
| 派单 201，工单 `quantity` = 实际派工台数 | ✔ 合同 5 台 / 派工 3 台（1 台冻结、1 台已分配被正确排除），`orderQuantity=5` 一并下发 |
| 订单与设备状态联动 | ✔ 订单 → `PRODUCING`；3 台设备 → `PRODUCING`；冻结/已分配设备原样不动 |
| 设备时间线 | ✔ 留下 `PRODUCING` 事件 |
| 重复派单 | ✔ 409 `FACTORY_ORDER_EXISTS` + `existingFactoryOrderNo` |
| 未入库订单派单 | ✔ 409 `INVALID_STATE_TRANSITION` |
| 工厂 JWT | ✔ 含 `factoryId` claim |
| 工厂端列表范围 | ✔ 只含本厂工单 |
| **脱敏红线** | ✔ 真实客户名 / 联系人 / 电话 / 邮箱在响应中出现 0 次；`unitPrice`/`totalAmount`/`applicantName`/`applicantPhone`/`contactPhone`/`email`/`tenantName`/`clientProductName` 字段名全部不存在 |
| 客户名脱敏形状 | ✔ `星********户`（括号不占可见位，长度与原名一致） |
| 烧录上报 | ✔ 分批累加正确；超量 → 400 `BURN_COUNT_EXCEEDED` + `details.remaining`；上报 0 台 → 400 |
| 抽检 | ✔ 合格/不合格均 201 落库并汇总；不存在 SN → 404；空白 SN → 400；同订单未派工 SN → 400；订单外 SN → 400 |
| 烧满联动 | ✔ 工单 `COMPLETED`、订单 `SHIPPED_TO_CLIENT`、3 台设备 `PRODUCED` |
| 出货登记 | ✔ 工单 `SHIPPED`、3 台设备 `SHIPPED`；已出货再上报 → 409 |
| 工单二维码清单 | ✔ 3 条（= 工单委托台数，不再多打标签），JD 格式，不含客户名 |
| **跨厂隔离** | ✔ 他厂列表 0 条、详情/二维码/烧录一律 404（不泄漏存在性） |
| 权限边界 | ✔ 商户 token → 工厂端 403；工厂 token → 平台端 403 |
| **入库端点（P5 遗留②）** | ✔ 批次导入 → `GENERATED` → 入库 `moved=2` → 重复入库 `skipped=2`（幂等） |
| **分配已出货设备** | ✔ 3 台 `SHIPPED` 设备分配成功（P4 的 `SHIPPED → ALLOCATED` 边接通） |
| **冻结已分配设备（P5 遗留①）** | ✔ `ALLOCATED` → 冻结 200 且 `previousAssetStatus=ALLOCATED` → 解冻**原路恢复为 `ALLOCATED`**（不回落库存） |

**门禁（全部实测通过）**

| 验收项 | 命令 | 结果 |
|---|---|---|
| 代码检查 | `make lint` | ✔ ruff 0 问题（含 `tests/` 与 `scripts/`） |
| 类型检查 | `make typecheck` | ✔ mypy strict，**81 个源文件**无问题 |
| 测试 | `make test` | ✔ **737 passed**（P5 基线 456 → P6 新增 281） |
| 契约一致性 | `make openapi-check` | ✔ OpenAPI 与快照一致（88 个路径，P6 新增 15 个端点） |
| 一键质量门禁 | `make check` | ✔ 全部通过 |
| 前端导入契约 | `make fe-check` | ✔ 52 个 JS 模块、**287 处**导入的路径与具名导出全部匹配 |
| 迁移可逆与零漂移 | `alembic upgrade head` → `downgrade 0011` → `upgrade head`；`alembic check` | ✔ 0012/0013 升降可逆；模型与迁移**零漂移** |

**浏览器实测（真实服务 + 本机 Chromium，12 项）**

| 验收项 | 操作方式 | 结果 |
|---|---|---|
| 平台端订单详情「派单给工厂」 | `#/orders/o-demo-stocked` → 页头按钮 | ✔ 弹窗含工厂下拉（带「已认证」标记）+ 交期 + 生产要求；提交后订单转「生产中」，**页头「派单给工厂」按钮随即消失**（状态相关动作随状态更新，P4 的 `setActions()` 生效） |
| 平台端工厂订单页（菜单 P6 新增） | `#/factory-orders` | ✔ 1 条工单，含工单号/平台订单号/工厂/客户（脱敏）/型号固件/数量/进度/状态/交期 |
| 工厂端工作台 | 工厂账号登录 → `#/dashboard` | ✔ 待生产 1 单、累计烧录 0/3、固件版本 1 个；生产概览与字段脱敏说明卡齐全 |
| 工厂端生产订单列表 | `#/orders` | ✔ 列含「客户（脱敏）」；`星********户`；**DOM 全文检索真实客户名/联系人/电话均为 -1（不存在）** |
| 工厂端工单详情 | `#/orders/fo-xxx` | ✔ 三 tab（概览/烧录记录/抽检记录）；同时显示「需求数量 3」与「平台订单数量 5」——合同与委托的差异在界面上可对账 |
| 烧录上报页 | 页头「烧录上报」→ `#/burn?orderId=…` | ✔ 默认填剩余台数；填 99 提交被拦下并提示「本单还剩 3 台（需求量 3，已烧录 0）」；填 3 提交后进度 100%、状态「烧录完成」、上报记录含 SN 区间与操作人 |
| 页头动作随状态更新 | 烧录完成后回详情页 | ✔ 由「刷新/烧录上报/导出二维码」变为「刷新/抽检确认/出货登记/导出二维码」；出货后仅剩「刷新/导出二维码」 |
| 抽检确认页 | `#/inspect` | ✔ **对同订单但未派工的 SN 提交 → 界面报「SN SN-20260101-DEMO05 不属于本工单」+ traceId**（本轮修复项在 UI 侧复核）；合法 SN 合格/不合格各落一条，列表与汇总一致 |
| 出货登记 | 页头按钮 → 确认框 | ✔ 弹窗明示「工单进入终态、出货后不可撤销」；确认后工单「已出货」、出货时间回填 |
| 固件版本页 | `#/firmware` | ✔ `1.0.0 / 1 单 / 3 台 / 3/3 / 100%` |
| 工厂端批次查询 | `#/batches` | ✔ 按设计显示「建设中 · 该页面将在后续阶段交付」（P6 未交付，见遗留观察①） |
| 平台端设备页「入库」 | 行内「入库」+ 勾选后「批量入库 N」 | ✔ 单台入库：`GENERATED` → `已入库待生产`，按钮从「入库」变「冻结」；批量入库同样生效 |
| **平台端「冻结已分配设备」** | 对 `ALLOCATED` 设备（DEMO05）点「冻结」 | ✔ 弹窗文案含「已分配给商户的设备冻结后即停服」；确认后设备「已冻结」；再解冻**回到「已分配」**（不回落平台库存） |

> 截图证据：`~/toyverse-p6-screenshots/p6-factory-orders.png`（工厂端生产订单列表，客户名脱敏）、`p6-platform-devices.png`（平台端设备库存）。

**本阶段由验收发现并修复的 3 个真缺陷**

| # | 缺陷 | 级别 | 处理 |
|---|---|---|---|
| 1 | **工单二维码清单返回订单下全部设备**（含被冻结 / 已先分配给客户的）——工单委托 3 台却导出 5 张标签，标签贴上后很难挽回 | 真缺陷（数据/物料） | ✔ 新增迁移 `0013`：`devices.factory_order_id` 把「设备 ↔ 工单」的归属落库（派单是唯一诞生点）；二维码清单按它筛选 |
| 2 | **抽检范围过宽**：只比对 `order_id`，一台**从未进入生产**的同订单设备也能被这张工单抽检并计入合格率 | 真缺陷（质量口径） | ✔ 抽检改判 `devices.factory_order_id`；界面文案同步说明「SN 必须属于所选工单」 |
| 3 | **平台端设备页 `FREEZABLE` 集合仍是 P5 的 `['IN_STOCK']`**：后端已放宽到可冻结已分配设备，界面上却点不到「冻结」——能力在 API 层可用、在 UI 层不可达 | 真缺陷（功能不可用） | ✔ 前端集合同步为 `['IN_STOCK','ALLOCATED','BOUND']` 并在注释里留下两次漂移的教训；冻结弹窗文案补「停服」语义 |

**独立验证代理报出的问题（已全部处理）**

| 发现 | 级别 | 处理 |
|---|---|---|
| 工单 `quantity` 取订单合同数量：订单 2 台、其中 1 台被冻结 → 工单写 2 台却只有 1 台流转，报满后 `burned_count(2) > 实际出货设备数(1)`，两边永久对不上账 | 真缺陷（对账口径） | ✔ 改取**实际可派工台数**，合同数量另以 `orderQuantity` 下发；超量上报随之被正确拦下 |
| `FACTORY_ORDER_EXISTS` 在正常流程下**不可达**（状态校验排在前面，重复派单只会得到笼统的 `INVALID_STATE_TRANSITION`） | 缺陷（可观测性） | ✔ 重复派单检查提到状态校验之前，前端能拿到 `existingFactoryOrderNo` 直接跳转 |
| 三处注释/文档与实现不符：`DeviceHeartbeatResponse.online` 写「恒为 true」、冻结端点文案写「仅 `IN_STOCK` 可冻结」、`_resolve_thaw_target` 写「必然是 `IN_STOCK`」 | 观察 | ✔ 三处均已改（其中两处会进 OpenAPI 描述，已重生成契约快照） |
| 脱敏只保留「第一个/最后一个字符」时，`星辰玩具（演示租户）` → `星********）`（括号占可见位，看起来像坏数据） | 观察（观感） | ✔ 改为取「首个/末个**文字字符**」，长度不变、幂等性与全部不变量保持 |

**另由我自己补齐的 P5 欠账测试**（todolist 明确要求但 P5 未交付）：

- `tests/integration/test_allocation.py`（33 条）、`tests/integration/test_tenant_isolation.py`（36 条，含**路由元测试**：双向比对 `app.routes` 与覆盖清单，新增端点漏登记会红灯）、`tests/integration/test_device_heartbeat.py`（31 条）

**遗留观察（不影响 P6 判定）**

1. **工厂端「批次查询」页未交付**：菜单项存在（P2 预留），页面显示「建设中」。`factory:batch:read` 权限与平台端 `/platform/batches` 已可用，缺一个 `/factory/batches` 端点与只读页。
2. **工单与订单一对一**：`dispatch_order` 拒绝二次派单（含历史脏数据形态）。因此「同一订单分批派给两家工厂」不可用；被冻结设备造成的差额无法通过二次派单补齐，只能解冻后走分配单。
3. **一单一张工单是服务层约束**：`factory_orders.order_id` 无唯一索引，并发下理论上可派两次。收紧需补一次迁移（`UNIQUE(order_id)`）。
4. **迁移 `0013` 不回填**历史设备的 `factory_order_id`（回填只能靠「订单 + 状态」反推，而那正是要消除的不可靠来源）。旧数据抽检会被拒；`make reset-db && make seed` 回到一致态。
5. 工厂端工作台「最近操作」卡仍写「随着后续阶段接口交付…」——工厂端没有审计查询端点与权限，该文案在共享模块里，属跨阶段问题（商户端同样存在）。
6. 验收在开发库留下痕迹（工单 `FO-20260916-ECN9`、批次 `BATCH-20260916-4RBY` / `BATCH-20260916-JSYM`、DEMO05 的冻结解冻记录、DEMO01–03 已 `SHIPPED`）；`make reset-db && make seed` 可回到纯净演示态。
7. `productionNote`（平台填写的生产要求）**原样下发**给工厂：若运营在备注里写客户名，工厂可见。这是已知的可接受面（平台自填内容不在脱敏范围内），已在 `FactoryOrderSerializer` 的 docstring 里写明。
8. 工厂端 JD 二维码载荷含 `tenantId` / `productId`（技术 ID，非名称）：工厂据此可做跨工单的客户关联。因需与平台端导出**逐字节一致**而保留，已在 `qrcode_service` 与本文档登记。

---

### P1 验收记录（2026-09-16）

| 验收项 | 命令 | 结果 |
|---|---|---|
| 代码检查 | `make lint` | ✔ ruff 0 问题 |
| 类型检查 | `make typecheck` | ✔ mypy strict，36 个源文件无问题 |
| 测试 | `make test` | ✔ **118 passed**，6.5 秒 |
| 契约一致性 | `make openapi-check` | ✔ OpenAPI 契约与快照一致 |
| 一键质量门禁 | `make check` | ✔ 全部通过 |
| 服务启动 | `make dev` | ✔ 日志正常，traceId 贯穿，安全校验通过，种子数据写入 |
| 登录返回 JWT | `POST /api/v1/auth/login` | ✔ 平台超管 `permissions=["*"]`、`tenantId=null`（ADR-01） |
| 账号锁定 | 连续 5 次密码错误 | ✔ 第 6 次（含正确密码）返回 `ACCOUNT_LOCKED` |
| **P-04 修复** | 禁用租户登录 | ✔ 返回 `TENANT_DISABLED`，账号本身仍为 ACTIVE |
| 迁移可回滚 | `alembic downgrade base` → `upgrade head` | ✔ 全链可逆，可跳步，模型与迁移零漂移 |
| 密钥不泄漏 | 响应体检查 | ✔ 全量响应无 SecretKey；`.env` 与 `learning/` 均未被 git 跟踪 |

### P2 验收记录（2026-09-16）

| 验收项 | 命令 / 方式 | 结果 |
|---|---|---|
| 后端门禁未被前端改动破坏 | `make check` | ✔ ruff 0 问题 / mypy strict 36 文件 / **118 passed** / OpenAPI 契约一致 |
| ES Module 导入契约 | `make fe-check` | ✔ 30 个 JS 模块 + 9 处 HTML 引用，77 处导入的路径与具名导出全部匹配 |
| 手写二维码编码器正确性 | `make qr-verify` | ✔ 13 个载荷 × 8 个掩码 = **104 组矩阵与 `qrcode` 库逐模块一致** |
| 四端页面可打开 | `GET /`、`/login`、`/platform/`、`/merchant/`、`/factory/`、`/miniapp/` | ✔ 全部 200；JS 以 `text/javascript` 提供（ES Module 可加载），CSS 为 `text/css` |
| 登录后进入对应端工作台 | 浏览器：平台超管登录 | ✔ 落地 `/platform/#/dashboard`，渲染「欢迎回来，平台管理员」，无白屏与控制台报错 |
| 未登录直访子页被守卫拦截（P-05） | 浏览器：跨 Origin 访问 `/platform/` | ✔ 重定向 `/login?redirect=%2Fplatform%2F&reason=unauthenticated` |
| 刷新子页面状态不丢失（P-01） | 浏览器：子路由 `#/tenants` 后 `reload` | ✔ URL 与页面内容均保持，未回落首页 |
| ⌘K 命令面板 | 浏览器：工作台按 `Control+k` | ✔ 出现 `dialog "命令面板"` 浮层 + 搜索框 + 候选列表 |
| 全新视觉（不得沿用早期实现） | `tokens.css` 检查 | ✔ 主色靛紫 `#4F46E5`（非 Ant 蓝 `#1677ff`）、浅色侧导轨（非深色 `#001529`）、Inter + Noto Sans SC、数值 `tabular-nums` |
| 密钥不泄漏（前端侧） | 前端源码扫描 | ✔ 无任何硬编码口令/JWT；登录页仅有「演示账号快捷填充（只填账号，不含密码）+ 提示密码见 `.env`」，且 `.env.example` 中密码字段为空占位 |

> 本轮遗留观察（不影响 P2 判定）：① 尚无页面使用路径参数（`#/tenants/t-001` 回落 `#/dashboard`），参数化能力已在 `router.js` 落地，将于 P3/P5 详情页启用；② 工作台首屏 5 个统计接口返回 404 属预期（对应域未开发，页面已降级为「建设中」）。

### P3 验收记录（2026-09-16）

**门禁（全部实测通过）**

| 验收项 | 命令 | 结果 |
|---|---|---|
| 代码检查 | `make lint` | ✔ ruff 0 问题（含 `tests/` 与 `scripts/`） |
| 类型检查 | `make typecheck` | ✔ mypy strict，**56 个源文件**无问题 |
| 测试 | `make test` | ✔ **251 passed**（P3 新增 82 条） |
| 契约一致性 | `make openapi-check` | ✔ OpenAPI 与快照一致（31 个路径） |
| 一键质量门禁 | `make check` | ✔ 全部通过 |
| 前端导入契约 | `make fe-check` | ✔ 37 个 JS 模块、**145 处**导入的路径与具名导出全部匹配 |
| 迁移可逆与零漂移 | `alembic upgrade head` → `downgrade 0004` → `upgrade head`；`alembic check` | ✔ 0005/0006 升降可逆；模型与迁移零漂移 |

**浏览器实测（真实服务 + 本机 Chromium，逐项操作）**

| 验收项 | 操作方式 | 结果 |
|---|---|---|
| 工作台统计接入真实数据 | 登录 `admin` → `#/dashboard` | ✔ `租户总数=2 个`、`产品模板=2 个`（订单/设备当时仍为「—」，已在 P4 补齐，见 P4 验收记录） |
| 租户列表字段与筛选 | `#/tenants` | ✔ 2 条租户，编码/联系人/电话/行业/状态/创建日期全部正确回显；行业下拉取自真实数据；分页「共 2 条 · 第 1/1 页」 |
| **参数化路由真正启用（P-01）** | 列表点「详情」→ 地址变 `#/tenants/t-001` → **按 F5 刷新** | ✔ 刷新后 URL 与页面内容均为该租户详情，**未回落 `#/dashboard`**；P2 遗留观察①就此解除 |
| 租户详情四标签页 | 概览 / 产品授权 / 客户产品 / 登录账号 | ✔ 均正确渲染；概览含 4 张统计卡与 11 项资料 |
| **P-03 验收视角「建后客户详情可见」** | 详情 → 客户产品标签 | ✔ 显示该租户的 `星辰小方块（Wi-Fi 智能故事机）CP-T001-CUBE`；切换到 t-002 只显示其自有产品（租户间不串台） |
| 产品授权清单 | 详情 → 产品授权标签 | ✔ 2 条授权，含「已建产品」计数（4G 故事机 0、ESP32-S3 1） |
| **一次性密码与强制改密** | 详情 → 登录账号 → 开通账号（口令留空） | ✔ `POST /tenants/t-002/accounts → 201`；弹窗显示「初始密码（仅显示一次）」+ `复制密码` 按钮；回读该账号 `mustChangePassword=true` |
| **P-06 删除级联校验** | 模板页点「删除」→ 确认 | ✔ 确认框先提示「该模板当前有 1 条授权…按设计会被拒绝」；确认后 `DELETE → 409`，响应 `code=CASCADE_CONFLICT`、`message` 说明「仍存在 1 条租户授权」、`details={authorizations:1, clientProducts:0}`；模板未被删除 |
| **ADR-07 连通性检测不伪造成功** | 云服务商页对「火山引擎智能云」点「连通性检测」 | ✔ `POST /clouds/{id}/test → 200`，`result=NOT_CONFIGURED`、`ok=false`、`lastTestMessage=尚未配置 AccessKey / SecretKey，已按安全策略跳过真实调用`、`latencyMs` 为空；行内状态同步为「未接入 / 未检测」 |
| **密钥只进不出（小程序）** | 客户产品详情 → 小程序配置 → 填哨兵 `SENTINEL_SECRET_…` → 保存 | ✔ `PUT → 200`；GET 响应仅 `appSecretHint="SENT****"`，**明文哨兵在响应中出现 0 次**；库内 `app_secret_enc` 为 Fernet 密文（`gAAAAA…`）且不含明文；`decrypt_secret()` 可正确还原 |
| 账号改名后可用 | 登录页 4 个演示账号按钮 → 分别登录 | ✔ `admin`（平台超管）登录成功，`role=PLATFORM_ADMIN`、`tenantId=null`、权限为通配；`13812345678`（平台运营）登录成功并**被强制进入改密界面**（`mustChangePassword=true`） |

**遗留观察（不影响 P3 判定）**

1. 工作台 5 个统计接口中，`/platform/orders`、`/platform/devices`、`/platform/audits` 仍返回 404（属 P4/P5 与未交付域），页面已按「—」降级，P2 已声明的降级策略继续生效。
2. 客户产品删除尚未接入「有设备则拒绝」的校验——`devices` 表属 P4，代码注释已声明延后。
3. 工厂端字段脱敏属 P6，本阶段无法验证。
4. 交互式验收在开发库留下了两条痕迹（t-002 下的账号 `13900000009`、prod-t001-cube 的小程序配置），属正常使用痕迹；`make reset-db && make seed` 可回到纯净演示态。

### P4 验收记录（2026-09-16）

**门禁（全部实测通过）**

| 验收项 | 命令 | 结果 |
|---|---|---|
| 代码检查 | `make lint` | ✔ ruff 0 问题（含 `tests/` 与 `scripts/`） |
| 类型检查 | `make typecheck` | ✔ mypy strict，**69 个源文件**无问题 |
| 测试 | `make test` | ✔ **397 passed**（P4 新增 146 条：单元 14 + 集成 132） |
| 契约一致性 | `make openapi-check` | ✔ OpenAPI 与快照一致（**51 个路径**，P4 新增 20 个端点） |
| 一键质量门禁 | `make check` | ✔ 全部通过 |
| 前端导入契约 | `make fe-check` | ✔ 42 个 JS 模块、**195 处**导入的路径与具名导出全部匹配 |
| 迁移可逆与零漂移 | `alembic upgrade head` → `downgrade 0007` → `upgrade head`；`alembic check` | ✔ 0008/0009/0010 升降可逆；模型与迁移**零漂移** |

**浏览器实测（真实服务 + 本机 Chromium，逐项操作）**

| 验收项 | 操作方式 | 结果 |
|---|---|---|
| 工作台 6 张统计卡接入真实数据 | 登录 `admin` → `#/dashboard` | ✔ `租户2 / 模板2 / 客户产品2 / 待审核订单1 / 订单2 / 设备5`；**P2 遗留观察②就此完全解除** |
| 订单列表 | `#/orders` | ✔ 2 条订单，单号/租户/产品/数量/状态标签/联网方式/设备数/时间齐全；**仅「待审核」单才渲染「审核」按钮**（状态相关动作渲染正确） |
| **订单闭环（本阶段核心）** | 待审核单 → 审核通过 → 生成设备 | ✔ `POST /orders/{id}/audit → 200`；`POST /orders/{id}/generate → 200`；订单最终 `IN_STOCK`、`generatedCount=3`、新增 3 台设备（SN 规则 `SN-20260916-XXXXXX`） |
| **页头动作随状态更新** | 审核后观察页头按钮 | ✔ 由「审核通过 / 驳回」自动变为「生成设备」，生成后再变为「导出二维码」——**修掉一个真实缺陷**（见下） |
| 设备库存与四维状态 | `#/devices` | ✔ 8 台设备；「四维（资产/激活/在线/绑定）」**分列展示**为 `已入库待生产 / 未激活 / 从未在线 / 未绑定`（ADR-03 要求不合并成单列）；统计卡 `总数8 / 已入库6 / 已激活0 / 在线0` |
| 冻结 / 解冻 | 行内操作（原因必填） | ✔ 冻结 → `FROZEN` 且 `previousAssetStatus=IN_STOCK`；解冻 → 恢复 `IN_STOCK`（**原路恢复生效**，印证冻结范围收紧后的对称性）；**空原因会被拦下且不关闭弹窗**（设计如此，非缺陷） |
| **批次导入全链路** | `#/batches` 上传 CSV → 预检 → 导入 → 错误报告 | ✔ 预检 `6 行 / 有效 3 / 无效 3 / 重复 2`（与构造的异常行完全对应）；导入后 `已导入 3` 且设备总数 8 → 11；错误报告为 `text/csv`、**BOM=`efbbbf`**、含行号与原因（第 4 行缺 SN、第 5 行文件内重复、第 6 行 SN 已存在于设备库） |
| `api.raw` 修复验证 | 浏览器内通过页面模块发起真实请求 | ✔ `HTTP 200 \| text/csv \| blob=137B \| 首行=行号,SN,错误原因`（返回未消费的 `Response`，`blob()` 可用） |
| **D2 修复验证** | 同文件 + 不同订单 / 同订单 | ✔ 不同订单 → `409 BATCH_FILE_CONFLICT` + `details{existingBatchNo,existingOrderId,requestedOrderId}`；同订单 → 幂等复用（返回同一批次号） |

**独立验证（AI Agent 对抗性测试，产出 56 条集成用例）**

| 发现 | 级别 | 处理 |
|---|---|---|
| **D1** 4G 生成因厂商未配置失败时，审计写的是 `GENERATE_DEVICES`，`VENDOR_CALL_FAILED` 为 0；且失败审计 `resource_id=None` | 真缺陷 | ✔ 已修：按失败根因分类（厂商原因 → `VENDOR_CALL_FAILED`），并给 `record_failure()` 补 `resource_type`/`resource_id` 形参 |
| **D2** 同一份 CSV 配**另一个订单**上传时静默复用旧批次，导入的设备会挂到旧订单/旧租户 | 真缺陷（数据归属） | ✔ 已修：新增 `BATCH_FILE_CONFLICT`(409)，命中摘要后校验订单一致性，不一致显式拒绝 |
| **O1** 冻结端点 OpenAPI 文案仍写「GENERATED / IN_STOCK 可冻结」 | 观察 | ✔ 已修（改为「仅 IN_STOCK」，与收紧后的规则一致） |
| **O2** 批次的 `error-report` / `import` 读路径未走 `assert_visible`，破坏 ADR-08 收口不变式 | 观察 | ✔ 已修（与 `list_lines` 对齐） |
| **O3** `MAX_FIELD_LENGTH=128` 大于 `sn String(64)` / `imei,iccid,mac String(32)`，换 PostgreSQL 会写库报错 | 观察 | ✔ 已修（改为按字段上限校验，超长日志写明是哪个字段） |
| **O4** `duplicatedRows` 把「库内已存在」与「文件内重复」合并计数，与字段名语义不符 | 观察 | ✔ 已修（只统计文件内重复） |

**本阶段顺带修复的缺陷**

1. **状态相关的页头动作不随状态更新**（`order_detail.js`）：页头按钮只在首屏按当时状态构建一次，审核通过后仍显示「审核通过/驳回」，用户必须手动 F5 才能看到「生成设备」。已给 `createDetailPage` 增加 `setActions()`（`shared/app/page.js`），并在 `reload()` 中按新状态重算。
2. **`api.raw()` 形同虚设**（P2 遗留）：`request()` 的形参里根本没有 `raw`（只在 JSDoc 里写了），且最初版本即便补上 `raw` 也会先 `parseBody()` 消费 body，调用方拿到的 `Response` 再 `blob()` 会抛 `Body is unusable`。已修复为「`raw` 时跳过 body 消费」，并让 `api.raw()` 自身隐含 `raw: true`。
3. **「审核通过」确认框沿用了驳回流程的误导文案**（写「驳回是不可逆的」），已改为符合通过语义的说明。
4. **`FREEZABLE_ASSET_STATUSES` 与 `ASSET_TRANSITIONS[FROZEN]` 不对称**（P1 遗留）：原允许冻结 `GENERATED`，但解冻只能回到 `IN_STOCK`，「恢复原状态」无法成立。已收紧为**只允许冻结 `IN_STOCK`**，并新增 `tests/unit/test_device_state_machine.py`（14 条）把该决策钉死。

**遗留观察（不影响 P4 判定）**

1. **工厂端端点未实现**（属 P6）：`factory_orders` / `burn_reports` / `inspections` 三表已建好，端点与脱敏留给 P6。
2. **设备详情页未做**：设备列表的「详情」目前给出指引提示而非跳转页（详情页不在本阶段前端归属清单内，P5 做设备流转与时间线时一并补）。
3. **`duplicatedRows` 是预检时落库的快照**：口径修正后不会回溯重算历史批次，因此演示库里那条旧批次仍显示 `2`。
4. **`COMPLETED` 订单再次调用生成接口为幂等回放 200**（不新增设备）：这是有意的重试安全设计，不是终态泄漏。
5. 商户端「我的订单」的客户产品下拉依赖本阶段新增的 `GET /merchant/products`；P9 会把它扩成完整的「我的产品」页。

---

> **P6 的并行开发方式**（沿用 P4/P5 已验证的组合）：主会话先冻结数据层（迁移 0012/0013 + 模型 + 枚举 + 状态机放宽 + JWT `factoryId` + 种子工厂）与**端点契约**，再开 2 个AI Agent 并行推进「服务层 + API」与「前端 6 页」，最后派 2 个独立验证代理分别补 P6 测试与 **P5 欠账测试**。三条防冲突约定不变：① 严格文件归属；② Alembic 迁移编号预先分配（P6=0012/0013、**P9 顺延为 0014**）；③ 三份追踪文件只允许主会话修改，代理一律不提交。
> **P6 的实测结论**：代理交付的 275 条测试全绿，**但这不足以证明可用**——本轮 3 个真缺陷全部由主会话的端到端验收与浏览器实测发现（二维码清单多打标签、抽检范围过宽、前端冻结集合漏改），另有 1 处对账口径缺口与 1 处不可达错误码由独立验证代理发现。**「代理自测全绿」与「功能真的可用」之间仍然隔着一次自己动手的验收。**

> **P5 的并行开发方式**（用户明确要求评估并执行）：主会话负责数据层（迁移 + 模型 + schema）与端到端验收，另开 2 个AI Agent 并行推进「服务层 + API」与「前端页面」，最后派 1 个独立验证代理补集成测试并做对抗性验证。三条防冲突约定：① 严格文件归属；② Alembic 迁移编号预先分配（P3=0005/0006、P7=0007、P4=0008–0010、**P5=0011**、P9 顺延）；③ 三份追踪文件只允许主会话修改，代理一律不提交。

### P5 验收记录（2026-09-16）

**门禁（全部实测通过）**

| 验收项 | 命令 | 结果 |
|---|---|---|
| 代码检查 | `make lint` | ✔ ruff 0 问题（含 `tests/` 与 `scripts/`） |
| 类型检查 | `make typecheck` | ✔ mypy strict，**77 个源文件**无问题 |
| 测试 | `make test` | ✔ **450 passed**（P4 基线 397 → P5 新增 53 条） |
| 契约一致性 | `make openapi-check` | ✔ OpenAPI 与快照一致（**65 个路径**，P5 新增 14 个端点） |
| 一键质量门禁 | `make check` | ✔ 全部通过 |
| 前端导入契约 | `make fe-check` | ✔ 46 个 JS 模块、**234 处**导入的路径与具名导出全部匹配 |
| 迁移可逆与零漂移 | `alembic upgrade head` → `downgrade 0010` → `upgrade head`；`alembic check` | ✔ 0011 升降可逆；模型与迁移**零漂移** |

**主会话独立端到端验证（真实服务 + 真实 HTTP 请求，29 项断言）**

| 验收项 | 结果 |
|---|---|
| 平台库存设备可查（`unallocated=true`） | ✔ HTTP 200，4 台可用 |
| 创建分配单 201 + 单号规则 + `DRAFT` | ✔ `AL-20260916-QP5F`，`total=2` |
| 创建分配单幂等重放返回同一条 | ✔ 同 `Idempotency-Key` → 同一 `id` |
| 执行分配（逐行校验后落库） | ✔ `allocated=2 failed=0 skipped=0` |
| 重复执行幂等（不重复分配，计入 skipped） | ✔ `allocated=2 skipped=2` |
| 设备四维与归属同步 | ✔ `asset=ALLOCATED tenant=t-001 product=prod-t001-cube` |
| precheck 签发 5 分钟确认令牌 | ✔ `ttl=300s format=JD` |
| 篡改载荷被拒（SHA-256 未命中） | ✔ 404 `QR_INVALID` |
| bind 成功（幂等键 + 写设备四维 + 写事件） | ✔ `BOUND`、`bindCount=1`、`boundBy=13812345679` |
| bind 幂等重放返回同一条（令牌已销毁仍成功） | ✔ HTTP 200，同一 `id`，`bindCount` 不变 |
| 不带幂等键重放被拒 | ✔ 409 `DEVICE_ALREADY_BOUND`（单绑约束优先于令牌校验，见下） |
| 已绑定设备再 precheck 被拒 | ✔ 409 `DEVICE_ALREADY_BOUND` |
| 设备详情带 `binding` 与事件时间线 | ✔ 事件 4 条：`BOUND → ALLOCATED → IN_STOCK → GENERATED` |
| 解绑成功（原因与操作者落库） | ✔ `status=UNBOUND`，原因写入 |
| 解绑后设备维度回落 | ✔ `bind=UNBOUND asset=ALLOCATED` |
| 解绑后可重新扫码（复用同一绑定行、令牌轮换） | ✔ `bindingId` 复用 = true，新令牌 ≠ 旧令牌 |
| **令牌销毁的直接证据** | ✔ 解绑后用**旧令牌** bind → 409 `QR_EXPIRED`（解绑不会复活旧令牌） |
| 冻结设备被绑定流程拦截 | ✔ 409 `DEVICE_FROZEN`（用种子里 `IN_STOCK` 时冻结的 `d-demo-04` 验证） |
| 模拟心跳 `simulated=true` + 窗口 180s | ✔ `online=true simulated=true window=180s` |
| 签发设备密钥（明文仅一次 + 掩码） | ✔ 201，`hint=CVyB****`，明文长度 43，库内仅 64 位摘要 |
| 真实心跳（密钥校验，无 JWT） | ✔ 200 `onlineStatus=ONLINE simulated=false` |
| 错误密钥 / 未知 SN 被拒 | ✔ 401 `UNAUTHENTICATED`；404 `DEVICE_NOT_FOUND` |
| 在线派生与固件回填 | ✔ `lastHeartbeatAt` 更新、`online=true`、`firmwareVersion=v9.9.9` |
| 模拟掉线（演示窗口超时） | ✔ `online=false onlineStatus=OFFLINE` |
| 商户访问平台端分配接口被拒 | ✔ 403 `PERMISSION_DENIED` |
| 商户绑定列表只含本租户记录 | ✔ 租户集合 = `['t-001']` |

> **两处「假失败」经复查证伪**（是我的测试预期写错，不是实现缺陷）：①「对已分配设备调冻结」返回 409 `INVALID_STATE_TRANSITION`——这正是 P4 收紧后的必然结果，因此「已分配 + 已冻结」在现有规则下**不可达**；②「设备已 BOUND 时无幂等键重放」返回 `DEVICE_ALREADY_BOUND` 而非 `QR_EXPIRED`——与冻结判定顺序一致（单绑约束先于令牌校验），语义正确且文案更有用。

**浏览器实测（真实服务 + 本机 Chromium，逐项操作）**

| 验收项 | 操作方式 | 结果 |
|---|---|---|
| 分配页渲染与筛选 | `#/allocations` | ✔ 列（单号/租户/产品/状态/总台数/已分配/失败/创建人/执行时间/操作）与状态/租户/关键字筛选齐全，空态「暂无数据」 |
| **分配单创建 → 执行闭环** | 新建弹窗 → 选租户（产品按租户联动过滤）→ 勾 2 台平台库存设备（「已选 2 台」）→ 提交 → 执行 | ✔ `AL-20260916-KUNL`；执行结果弹窗「已分配 2 / 已跳过 0 / 失败 0」；列表转「已完成」，**「执行」按钮按设计消失** |
| 分配生效 | `#/devices` | ✔ STOCK01/02 归属变为「星辰玩具 + 星辰小方块」、状态「已分配待激活」；STOCK03/04 仍为「平台库存」 |
| 分配明细 | 「查看明细」 | ✔ 2 行（SN / 状态 / 失败原因 / 分配时间），「已显示全部 2 条明细」 |
| 商户端租户隔离 | 商户登录 → `#/devices` | ✔ 7 台（仅 t-001），统计卡「已分配待激活 3」 |
| **扫码绑定两步流程** | 行内「绑定」→ 粘贴 `JD\|…` 载荷 → 预检 | ✔ 回显 SN / 设备标签 / 客户产品 / 资产状态 / 联网方式 / 归属租户 / 二维码格式，并显示「确认码剩余有效时间：286 秒」倒计时 |
| 确认绑定 | 点「确认绑定」 | ✔ 弹窗关闭；服务端 `status=BOUND`、`boundBy=13812345679`、`bindCount=1`、**`confirm_token_hash=NULL`（已销毁）**；设备 `asset=BOUND bind=BOUND` |
| 状态相关行内动作随状态更新 | 绑定后回到列表 | ✔ STOCK01 的按钮由「绑定」变为「解绑」 |
| 绑定管理 | `#/bindings` | ✔ 记录含绑定时间/操作者、绑定次数 1；仅 `BOUND` 显示「解绑」 |
| 解绑（原因必填） | 「解绑」→ 直接确认 | ✔ 弹窗不关闭、`aria-invalid=true`（拦下非法提交） |
| 解绑成功 | 填原因 → 确认 | ✔ `status=UNBOUND`、原因与操作者落库、`bindCount` 保留、设备回落 `ALLOCATED/UNBOUND` |
| 设备详情页（P4 欠账） | `#/devices/d-stock-01` | ✔ 3 个 tab（概览 / 绑定 / 事件时间线）；四维**各带中文说明**；页头动作按状态给出（刷新 / 模拟心跳 / 签发密钥 / 报废，**无「冻结」**，与 P4 收紧一致） |
| 时间线与事件明细 | 事件时间线 tab | ✔ 6 条事件（在线/解绑/绑定/分配/入库/生成），含状态迁移、操作者、`traceId` |
| **180 秒窗口口径** | 心跳后放置 4 分钟再读 | ✔ 派生「是否在线」= 离线、「最近心跳」= 4 分钟前，而四维标签仍显示落库的「在线」——即「枚举只用于定位与筛选、结论以窗口为准」的设计被实证 |
| 在线统计与筛选同口径 | `GET /devices/stats`、`?onlineStatus=ONLINE` | ✔ `byOnlineStatus` 把超窗设备从 ONLINE 归入 OFFLINE（`ONLINE=0, OFFLINE=1`）；`onlineStatus=ONLINE` 筛选返回 0 条 |
| 模拟心跳 | 详情页「模拟心跳」 | ✔ 「是否在线」变「在线（180 秒心跳窗口内）」、「最近心跳」变「刚刚」 |
| 签发设备密钥 | 「签发设备密钥」 | ✔ 二次确认 → **一次性明文弹窗**（含「复制密钥」与「只显示一次」警示）；关闭后明文在 DOM 中出现 0 次、不落 `localStorage`；凭证表只显示 `CVyB****` |
| 续签覆盖 | 「重新签发设备密钥」 | ✔ 仍只有 1 行凭证（覆盖而非新增），新掩码与新明文一致，旧密钥立即失效 |

**本阶段顺带修复的缺陷（6 个，全部有实测证据）**

| # | 缺陷 | 级别 | 处理 |
|---|---|---|---|
| 1 | **所有页头按钮显示成字面文本 `<span>刷新</span>`** —— `shared/ui/components.js:76` 嵌套 `html` 模板漏了 `raw()`，被二次转义。**P2 就存在**（P4 设备页同样中招，此前验收未发现），影响所有页面 | 真缺陷（观感） | ✔ 已修（补 `raw()` + `esc(label)`），截图对比确认 |
| 2 | **分配单弹窗选完租户后「可选设备」永远为空** —— 租户 `change` 处理把设备列表清空却没重新加载。因为「先选租户」是必然动作，**分配功能实际完全不可用** | 真缺陷（功能不可用） | ✔ 已修（设备池与租户无关，只清勾选、不清列表） |
| 3 | **时间线事件明细显示字面 `<div>…</div>`** —— 调用方按「body 是 HTML」传参，而 `timeline()` 的契约是纯文本，被二次转义 | 真缺陷（观感） | ✔ 已修（调用方改传纯文本 + `.timeline-body` 补 `white-space: pre-line`）。**刻意不把组件改成 `raw`**：body 含用户填写的解绑原因，组件转义是必要的 XSS 防线 |
| 4 | **签发密钥后详情页不刷新**，凭证区块仍显示「尚未签发设备密钥」，用户会误以为签发失败；且弹窗打开期间背景表格显示的是**上一轮的掩码**，看起来像「弹窗里的密钥与页面上不是同一个」 | 真缺陷（误导） | ✔ 已修（把 `reload()` 挪到弹窗之前，「先刷新、再弹窗」） |
| 5 | **空白串可当「原因」** —— `BindingUnbindRequest` / `DeviceFreezeRequest` / `DeviceRetireRequest` 只有 `min_length=1`、不 strip，`"   "` 能过关并原样落库，时间线里出现「解绑设备 X：   」，责任追溯失效（P4 的 `rejectReason` 已是「空白不算填了」口径） | 真缺陷（审计） | ✔ 已修（三处都加 strip 校验器 → 400 `VALIDATION_ERROR`，与订单驳回同口径）；由独立验证代理发现 |
| 6 | **前缀大小写导致自相矛盾** —— `parse_payload` 用 `.upper()` 容忍 `jd\|`（P4 单测已钉死该宽容行为），但 precheck 的 SHA-256 命中按字节严格比对，于是 `jd\|…` 先被判「格式合法」再被判「伪造」，报错文案误导排查 | 真缺陷（一致性） | ✔ 已修（`payload_hash` 只把**前缀段**归一为大写，其余字节仍严格比对；前缀不承载安全语义）；由独立验证代理发现 |

**遗留观察（不影响 P5 判定）**

1. **「冻结已分配/已绑定设备」的能力缺失（需在 P6 决策）**：`FREEZABLE_ASSET_STATUSES={IN_STOCK}`（P4 收紧）与「绑定只接受 `ALLOCATED`」互斥，两者交集为空，因此绑定流程里的冻结校验只对「入库时被冻结、尚未分配」的设备生效——语义正确但正常流程下不可达。若要恢复「冻结一台已出货给商户的设备」，需放宽 `ASSET_TRANSITIONS[FROZEN]` 允许 `FROZEN → ALLOCATED/BOUND`，并同步修改 P4 已钉死的 14 条单元断言。
2. **`GENERATED → IN_STOCK`（入库动作）仍无端点**：CSV 批次导入的设备停在 `GENERATED` 且 `tenant_id` 为空，而分配只接受 `IN_STOCK`，所以导入的设备当前进不了分配链路。本阶段为此在种子数据里补了 4 台**平台自有库存**设备（`tenant_id` 空 + `IN_STOCK`）作为分配链路的输入——否则分配页的选设备列表永远是空的。该动作归 P6 的入库环节。
3. **验证AI Agent 只交付了 `test_binding.py`（53 条，全绿）**：`tests/integration/test_allocation.py`、`test_tenant_isolation.py`（todolist 明确要求的租户隔离参数化矩阵）、`test_device_heartbeat.py` 尚未产出，需在下一会话补齐。因此**租户隔离目前由主会话的定向断言覆盖**（商户访问平台端 403、绑定列表租户集合、越权 404），而非全端点参数化矩阵。
4. **`end_user_id` 只存不建外键**（`end_users` 属 P9），与 P7 的 `dialogue_sessions.device_id` 同一处理方式；绑定弹窗里的「终端用户标识」输入框已标注该说明。
5. 商户端设备「详情」实现为**弹窗**而非独立页面（本阶段前端路由清单未含商户端设备详情页），其接口已在 API 自测中验证。
6. `EXPECTED` 分配/绑定演示数据留在开发库（分配单 `AL-20260916-KUNL`、`d-stock-01` 的绑定与凭证记录、`d-stock-01/02` 已归属 t-001）；`make reset-db && make seed` 可回到纯净演示态。
7. **元素截图通道在验收后半段持续 RPC 超时**（同期 `bsk evaluate` 正常），因此「按钮修复」有修复前后 DOM 证据与一张修复前截图，其余界面修复只有 DOM 证据。
8. 平台/商户工作台的阶段说明文案已更新到 P5（原 P2/P4 的「接口将在后续阶段交付」已过期）；商户工作台的审计卡仍返回 404（`/merchant/audits` 属未交付域），页面已降级为「—」。

---

### P7 验收记录（2026-09-16）

| 验收项 | 命令 / 方式 | 结果 |
|---|---|---|
| 代码检查 / 类型检查 | `make lint`、`make typecheck` | ✔ ruff 0 问题；mypy strict 56 文件无问题 |
| 单元 + 集成测试 | `pytest tests/unit/test_provider_registry.py tests/integration/test_ai_mock.py tests/integration/test_vendor_unavailable.py` | ✔ **42 passed**（注册表 31 / 模拟引擎 11 / 无密钥安全失败 7） |
| 迁移与漂移 | `alembic upgrade head` → `downgrade -1` → `upgrade head`；`alembic check` | ✔ `0007` 落地且**零漂移**；升降往返可逆 |
| 离线可跑 | `AI_DEFAULT_PROVIDER=mock` 下 `/ai/chat`、`/ai/tts`、`/ai/asr` | ✔ 全通；流式分块数 > 1；会话与消息落库 |
| **ADR-07 安全失败** | `tests/integration/test_vendor_unavailable.py` | ✔ 未配置密钥的厂商 `health_check → DOWN`、端点返回 `VENDOR_UNAVAILABLE` 且写入 `VENDOR_CALL_FAILED` 审计，**绝不伪造成功** |

> **P7 已知局限（诚实记录，待真实联调消除）**：① 火山引擎的**签名算法与签名位置**仍是占位（官方《服务端 API › 调用方法》页正文不可读），已收口在 `BaseHttpProvider.sign` 一处并在代码中显式标注；② `AibotCreate` 之外的 Action Body 字段名按官方 PascalCase 惯例统一，未逐字核对（风险示例：`AibotId` vs `AgentId`）；③ `TTSConfig` / `LLMConfig` 的层级为同构推断，仅 `ASRConfig` 逐字来自官方示例；④ 实时对话的真实链路是 RTC/WebSocket 长连接，当前 `chat()` 为「HTTP 取整段 + 本地分块」，联调时替换为帧解析（对外接口 `AsyncIterator[ChatChunk]` 不变）；⑤ `dialogue_sessions.device_id` / `end_user_id` 刻意未建外键（`devices` 属 P4、`end_users` 属 P9），P4 落地后需补一次轻量迁移。

---

## 项目整体复盘与质量评估（2026-09-24）

> **性质**：应「查看分析项目进度及各阶段质量」要求做的整体复盘。**只做分析与建议，未改动任何代码**。
> **方法**：所有论断均经只读实核（git 历史与远端 CI API、迁移链、路由与页面存在性、测试计数），不凭文档自述下结论。

### 一、分析思路

1. **四源交叉核对**：主进度表 ↔ 各阶段验收记录 ↔ 各阶段「遗留观察」↔ 变更记录，找内部矛盾与口径不一。
2. **实物验证**：对文档关键论断逐项做只读实核（见下表），避免「文档说完成了就当完成」。
3. **口径区分**：全程区分「**阶段验收标准达成**」与「**功能全量交付**」——主进度表的 100% 指前者，不是后者。
4. **问题分级**：P1（影响下一步，应先处理）/ P2（一致性，近期处理）/ P3（预测性提醒，顺手处理）。

### 二、实物核对结果（本次复盘的证据底座，2026-09-24 实测）

| # | 核对项 | 文件/早先记录的说法 | 实核结果 | 判定 |
|---|---|---|---|---|
| 1 | 迁移链 | `0001`–`0015` | `alembic/versions` 实存 15 个迁移，无 `0016` | ✔ 一致 |
| 2 | 商户隔离矩阵 | 曾记「P9 的 26 条待补」 | `test_tenant_isolation.py` 的 `MERCHANT_ROUTE_COVERAGE` 已含全部 P9 路由（AI 配置/知识库/内容库/指标） | ✔ 已闭环（早先记录过时） |
| 3 | 商户审计端点 | P5 遗留⑧「`/merchant/audits` 属未交付域」 | `backend/app` 中确无该端点 | 与记载一致，**遗留仍未闭环** |
| 4 | 工厂端批次查询页 | P6 遗留①「建设中」 | `frontend/pages/factory/` 下确无批次页面 | 与记载一致，**遗留仍未闭环** |
| 5 | 外键轻量迁移 | P5 遗留④ / P7 局限⑤「P4 落地后需补一次轻量迁移」 | `models/ai.py` 中 `dialogue_sessions.device_id` / `end_user_id` 仍为松散引用（无 ForeignKey）；迁移止于 `0015`，**三处白纸黑字承诺的迁移从未发生**；模型注释仍写「end_users 表 P9 落地」（实际 0014/P8 落地） | ✘ **有承诺、无闭环、无人认领** |
| 6 | 进度文件时效 | 「最后更新 2026-09-20；下一步 P-A 未完成；采样率待确认」 | git 已有 09-22 提交：后端代注册接口、P-A 注册+建连**通过**、P-B 文本验证**通过**、采样率**已确认**（16kHz 上行 / 24kHz 下行）；测试现为 27 文件 / 567 函数（本文件记载 26 / 553） | ✘ **进度文件落后实际进度 2 天** |
| 7 | 远端 CI 状态 | 「run #1 五 job 全绿」之后无记录 | GitHub Actions 共 4 次 run：**run #4（`4bc67b7` 代注册接口）conclusion=failure**；其后的 4 个本地提交（`606622a`…`a71fc31`）**未推送**、从未跑过 CI；`backend/app/ai/volcano.py` 另有未提交修改 | ✘ **最新已推送提交的 CI 是红的，且无人记录、无人处理** |
| 8 | 硬件资料泄露风险 | 曾记仓库目录下的密钥文件有被 `git add -A` 带入公开仓库的风险 | `ESP32-S3/` 目录整体已被 `.gitignore` 忽略，目录内现无密钥文件 | ✔ 已缓解 |
| 9 | License 容量 | 09-22 P-A/P-B 记录「火山 License 2/2 已满，新设备注册失败 `12000130`」 | 无后续扩容动作 | ✘ **真实前置阻塞（ESP32 移植需真机注册），但下方阻塞事项表写「暂无」** |

> **结论先行**：本文件的「诚实记录」在抽查中**全部属实**——每一个「已完成」与每一个「未闭环」都与实物一致，没有虚报。
> 主要问题不在「做得对不对」（已交付质量见下节，属上乘），而在**四件事：CI 红灯无人看、遗留无台账、路线演进未回写、进度文件阶段性失同步**。

### 三、各阶段质量评估

| 阶段 | 质量判定 | 核心依据（均引自既有验收记录） | 未闭环遗留 |
|---|---|---|---|
| P0 脚手架 | 优 | 三项验收全过；`.gitignore`/`.env.example` 边界清晰 | 无 |
| P1 后端地基 | 优 | 迁移全链可逆；启动期安全校验；密钥不泄漏验证 | 无 |
| P2 前端地基 | 优 | 导入契约 + 二维码 104 组矩阵比对；修 P-01/02/05 | 无（遗留观察均已在 P3+ 闭环） |
| P3 目录域 | 优 | 12 项浏览器实测；密钥「只进不出」哨兵验证 | 无重大遗留 |
| P4 订单/设备 | 优 | 独立对抗验证产出 D1/D2 真缺陷；146 条新测试 | 无重大遗留（批次快照口径等已显式接受） |
| P5 分配/绑定 | 优 | 29 断言 E2E + 19 项浏览器实测；6 缺陷修复 | 外键迁移欠账（核对⑤）、`/merchant/audits` |
| P6 工厂端 | 良 | 71/71 E2E；3 真缺陷；脱敏红线 DOM 级验证 | 批次查询页、`order_id` 唯一索引、`0013` 不回填 |
| P7 AI 抽象 | 良 | 42 测试；ADR-07「不伪造成功」有守卫 | 火山 HTTP 适配器多项占位（后被路线 B 部分取代，未回写文档） |
| P8 小程序端 | 优 | 50/50 E2E + 11 项浏览器；激活幂等缺陷由实测发现 | ws.js/api.js 契约不符（绕开未修）、语音前端、设备昵称 |
| P9 配置/看板 | 良 | 「快照之和==实时聚合」守卫；产品隔离守卫 | 三个服务覆盖率 <31%、UTC 时区口径、内容库只读、知识库关键词级检索 |
| P10 部署 | 优 | 12 个「真跑才暴露」的问题全修；冒烟双通道 57/57 | 7 条局限（其中 3 条已被 P11-D 关闭） |
| P11 文档/收官 | 优 | 六处 docs/代码不一致被独立视角找出；冷启动 91 秒实测 | 见 `docs/11` §8.5 清单 |

**值得保留的三条质量主线**：
1. **真跑验收文化**：P10 的 12 个问题、播种不幂等、CI 触发分支、PG 方言缺陷——全部只有真跑才暴露，无一是读代码或跑集成测试能发现的。
2. **独立视角核对**：P4 对抗验证、P11-B 四代理按代码复核、P11-C 换新手身份照 README 操作——「自己写的东西自己读」发现不了错。
3. **把一致性变成门禁**：docs-check 八类、OpenAPI 快照、隔离矩阵元测试、覆盖率下限——靠机制而非记忆同步。

### 四、发现的问题与修改方案（按优先级）

#### P1 级（影响下一步，建议先处理）

**问题 1：远端 CI 红灯无人发现，本地 4 个提交未推送（最紧急）**
- 现状：run #4（`4bc67b7`）**失败**，之后没有任何记录或处理；本地 `main` 领先 `origin/main` 4 个提交（`606622a`…`a71fc31`，其中 `606622a` 的 fix 前缀提示它可能正是修复），另有 `volcano.py` 未提交修改。
- 为什么严重：这恰恰违背了本项目自己总结的最重要教训——「对声明式配置，唯一有效的验收是推一次，然后去查它有没有真的跑」。推了、跑了、红了，但**没人查**。
- 方案（均为流程动作，不含代码）：① 本地在干净环境重跑 `make check` 定位 run #4 的失败原因（很可能与新增端点/测试后 `openapi-check` 或 `docs-check` 的文档同步有关，见问题 7）；② 确认 `606622a` 是否即修复，若是则推送 4 个提交并盯到 CI 转绿；③ `volcano.py` 的未提交修改要么提交、要么显式搁置并说明；④ 把「推送后查 CI 结论」加入每会话收尾清单。

**问题 2：阻塞事项表「暂无」与实际不符（License 容量）**
- 现状：License 已 2/2 用满，新设备注册会失败（`12000130`）；而既定下一步「ESP32 移植」需要真机注册。
- 方案：在「阻塞事项」表登记（影响阶段：ESP32 移植与真机联调；处理路径二选一：License 扩容，或复用已注册测试设备的 `device_secret` 完成移植调试）。只登记与决策，无需改代码。

**问题 3：进度文件落后实际进度 2 天**
- 现状：见核对⑥——09-22 的代注册接口、P-A 通过、P-B 文本验证通过、采样率确认，均未写入本文件；「当前节点」仍把 P-A 列为「下一步（未完成）」。
- 方案：① 在「当前节点」补记 09-22 成果（P-A 判据达成拿到 `session.created`、过程中发现并修复 4 个问题、P-B 文本双向验证通过、音频上行需真机）；② 同步测试计数口径；③ 恢复本文件「更新约定」第 1 条的纪律——每会话收尾同步三份追踪文件。

#### P2 级（一致性，建议近期处理）

**问题 4：遗留事项散落各阶段，无统一台账与闭环机制（本次复盘最核心的结构性发现）**
- 现状：「剩余待办（诚实记录，1 条）」只列真实厂商联调与合规；但各阶段「遗留观察」中至少还有 8 项**功能性开口**从未闭环也无人认领：外键迁移（P5/P7 三处承诺）、工厂批次查询页（P6）、`/merchant/audits`（P5）、ws.js/api.js 契约不符（P8）、语音前端（P8）、设备昵称列（P8）、13 个未实现菜单项（P11-B 核实）、UTC 时区聚合（P9）。它们都「被记录过」，但**没有任何机制保证被处理或被显式放弃**——外键迁移就是实证：三处承诺，最终无人执行。
- 方案：建立**遗留事项登记表**（可加在本文件或 `todolist.md` 末尾），每条含：编号 / 来源阶段 / 描述 / 影响 / 优先级 / 闭环判据 / 状态（待办 · 已闭环 · **显式放弃+理由**）。今后每阶段验收前先过一遍台账。「显式放弃」是合法状态——**悬置才是唯一不允许的状态**。

**问题 5：路线演进未回写旧文档（火山接入）**
- 现状：09-20 已定案走「路线 B（标准协议 WebSocket + 后端代注册）」并落 `docs/16`；但 `docs/13`（记录的 `volc_*` API 名与官方真实 `byte_rtc_*` 存在出入）与 `docs/08`（HTTP Provider 适配路径）未标注「已被路线 B 部分取代」。后续读者（含未来的自己）照旧文档走会浪费取证成本。
- 方案：在 `docs/13`、`docs/08` 各补一段「路线决策影响」指向 `docs/16`（纯文档改动）；`models/ai.py` 里「end_users 表 P9 落地」这类过期注释，下次动到相关文件时一并修正。

**问题 6：「100% 完成」的口径易被误读**
- 现状：主进度表 12 阶段全 100%，但同时存在 13 个未实现菜单项、工厂端「建设中」页面、`/merchant/audits` 缺失。细节是诚实的，但**标题数字会先入为主**——本仓库定位是作品集，读者第一眼看到的就是 100%。
- 方案：在「总体进度」下加一行口径说明：「阶段完成 = 该阶段验收标准达成，不等于功能全量交付；已知功能缺口见遗留事项登记表」。README 的「已知局限」小节已有同类表述，口径可对齐。

#### P3 级（预测性提醒）

**问题 7：文档门禁的计数类校验预计已红灯**
- 09-22 新增端点（代注册接口）与测试（27 文件 / 567 函数 vs 文档记载 26 / 553）后，`make openapi-check` 与 `make docs-check` 的第 7 类（逐文件用例数）预计会失败，直到快照与 `docs/11` 同步——这很可能就是 CI run #4 失败的原因之一（待问题 1 的本地复跑确认）。此处提前告知，避免把「文档未同步」误判为「代码坏了」。

### 五、整体阶段性总结

**项目轨迹**：12 个阶段（P0–P11）+ P11-D 收官验证 + 真机刷机与音频链路验证（09-20）+ P-A/P-B 云端协议验证（09-22）。从脚手架到「平台 - 商户 - 工厂 - 终端用户 - 真实设备」的全链路多租户 SaaS 已交付，并被 814 用例、六类门禁、真实浏览器/容器/双数据库验证钉住。**当前实际节点已推进到「ESP32 移植前夜」**：协议规格取证完毕、后端代注册接口落地、PC 端注册+建连+文本对话全部真跑通过；剩余三步——License 扩容（或复用）、音频上行验证（需真机麦克风）、固件协议层移植。

**质量总评**：交付质量与过程记录质量**均属上乘**——本次抽查中文件记载与实物**零出入**，无虚报；各阶段真缺陷均在交付当时被发现并修复，无一遗留到「已宣称完成」的交付物里。**真正的短板不在「做得对不对」，而在「记住了什么」**：CI 红灯无人看、遗留无台账、承诺不闭环、路线演进不回写、进度文件阶段性失同步——这五件事都不影响已交付代码的正确性，但会持续放大未来工作的成本，且恰好都发生在 P11-D 之后「项目转入外部依赖型任务」的阶段（印证了 09-18 复盘的判断：瓶颈已从代码交付转向流程纪律与外部依赖）。

**三条可复用的复盘结论**：
1. **「记录过」不等于「会处理」**——凡跨阶段的承诺（如「P4 落地后补迁移」）必须进入带闭环判据的台账，否则必然丢失。
2. **诚实记录需要「汇总视图」**——分阶段的诚实若没有统一台账，在整体层面就会变成「剩余待办 1 条」的低估。
3. **「推一次并查它跑没跑」要成为固定动作**——run #4 的红灯静默了两天证明：教训写过不等于机制成立，查 CI 结论必须进收尾清单。

**下一步建议排序**（均为决策/文档/流程动作，不含代码）：
1. 本地复跑 `make check` 定位 run #4 失败原因 → 推送 4 个提交并盯 CI 转绿（问题 1、7）；
2. 登记 License 阻塞并二选一决策（问题 2）；
3. 补记 09-22 进度到「当前节点」、处理未提交文件（问题 3）；
4. 建立遗留事项登记表并完成首轮 8 项汇总（问题 4）；
5. `docs/13`/`docs/08` 补路线 B 标注（问题 5）；
6. 之后回到既定主线：P-B 音频上行（需真机麦克风）→ ESP32 移植。

### 六、本次复盘的边界（诚实声明）

1. 全程**只读核对**（git / GitHub 公开 API / 文件系统 / 静态检索），未运行测试、门禁或服务——「814 全绿」等历史结论引自既有记录，本次未复跑；run #4 的**具体失败原因**因 job 日志需鉴权而未能取证，需按问题 1 的方案本地复跑确认。
2. 早先记忆中有两处过时信息，本次已按实物修正：隔离矩阵「26 条待补」实已补齐；`docs/15`/`docs/16` 实已提交（`c1f75fa`）。
3. 各阶段「良/优」判定依据为本文件记载的验收证据强度（门禁 + E2E + 浏览器/容器/数据库实测 + 独立验证的覆盖度），属定性评估。
4. 所有「修改方案」均为文档/流程/决策动作，**不涉及任何代码改动**；是否采纳及采纳顺序由用户决定。

### 七、执行记录（2026-09-24，用户批准「执行建议方案」当日完成）

| 问题 | 执行结果 |
|---|---|
| 问题 1 / 7（CI 红灯） | 根因确认：09-22 提交**未跑门禁**（ruff 16 处、mypy 4 处）+ 快照/文档计数过期。已修复：ruff --fix + 3 处 mypy 机械修复；重生成快照；`docs/06` 补 `POST /api/v1/device/register` 行、`docs/11` 计数同步（27 文件 / 567 函数）。**另发现并修复一个新问题：测试不密封**——开发机 `.env` 的火山 AK/SK 使 8 条 ADR-07「未配置安全失败」测试在本机反向失败（CI 无 `.env` 所以没炸过），已在 `conftest.py` 导入前把四家厂商凭证钉空 |
| 问题 2（License 阻塞） | 已登记为阻塞事项 **B-01**，并**经用户批准用 bsk 实查火山控制台后修正前提并决策**：账号共 10 个免费基础版 License，「2/2 已满」实为「智能对话硬件001」产品的分配额度用满，账号级未分配还有 6 个 → **决策：先复用 `pa-test-6333c399`，新设备注册前自助分配 License，无需扩容**（详见 B-01） |
| 问题 3（进度滞后） | 「当前节点」已补记 09-22 P-A/P-B 成果与采样率确认；头部指标更新为 **828 passed / 68%** |
| 问题 4（遗留台账） | 「遗留事项登记表」已建立，首轮登记 **L-01～L-15**（含 2 项显式接受） |
| 问题 5（路线回写） | `docs/13` §4 与 `docs/08` 已加路线 B 标注与 `volc_*`/`byte_rtc_*` 出入说明 |
| 问题 6（口径） | 「总体进度」下已加口径说明；`更新约定` 新增第 6/7 条（推送后必查 CI、提交前必跑 openapi/docs 门禁） |
| 最终门禁 | **`make check` 全绿：828 passed in 260s + ruff 0 问题 + mypy 96 文件 + 契约一致（146/120）+ docs-check 八类通过**；推送后 **CI run #5（`a89e3da`）五 job 全绿**（ruff+mypy / pytest / 契约 / 前端导入 / 敏感扫描）——run #4 起的 CI 红灯就此闭环 |

---

## 阻塞事项

| 编号 | 阻塞内容 | 影响阶段 | 提出日期 | 处理进展 |
|---|---|---|---|---|
| B-01 | ~~火山引擎 License 已用满（2/2）~~ → **经控制台实查修正（2026-09-24）**：账号共 **10 个免费基础版 License**（每个含 9 小时语音 + 1350k tokens），仅「智能对话硬件001」产品的**分配额度**用满（分配 2 / 激活 2 / 剩余 0——09-22 报 `12000130` 的真实原因是它，不是账号配额）；box1、小方块玩具各分配 1（均未激活）；**账号级未分配还有 6 个** | ESP32 移植、真机联调（下一步主线） | 2026-09-22（P-A 验证时报错）；2026-09-24 复盘登记、同日经控制台实查修正并决策 | **已决策并解除阻塞（2026-09-24，用户批准「先复用 + 扩容挂待办」）**：① 移植调试**复用**已注册设备 `pa-test-6333c399` 的 `device_secret`（P-A/P-B 已验证可用）；② 新设备注册前，在控制台「AI 服务购买与管理 → 管理」把未分配的基础版 License **分配**给目标产品——自助操作，**无需向火山申请扩容**；③ 仅当免费额度（每 License 9 小时语音 + 1350k tokens）不足时才考虑购买扩展资源（真机产品化时另行评估） |

> 遇到阻塞时按此格式登记，并在 `todolist.md` 对应任务标记为 `[!]`。**不擅自改变已定案的架构决策**（见 `todolist.md` 附录 C）。
> ★ 2026-09-24 复盘教训：本表写「暂无」的期间，License 容量已实际阻塞下一步主线两天——**阻塞登记必须与「当前节点」同步刷新**。

---

## 遗留事项登记表（2026-09-24 建立，复盘问题 4 的落地）

> **规则**：凡跨阶段承诺或未闭环的功能开口，一律登记在此，**每阶段验收前过一遍**。
> 状态只有三种：`待办` / `已闭环`（附证据）/ `显式接受`（附理由）。**「显式接受」是合法状态，悬置不是。**
> 来源标注：阶段编号或复盘章节；优先级影响排期，不改变登记义务。

| 编号 | 来源 | 事项 | 影响 | 优先级 | 闭环判据 | 状态 |
|---|---|---|---|---|---|---|
| L-01 | P5 遗留④ / P7 局限⑤ / 复盘核对⑤ | `dialogue_sessions.device_id` / `end_user_id` 与 `device_bindings.end_user_id` 无外键（曾三处承诺「落地后补轻量迁移」） | 数据完整性无库级保障（SQLite 本就不强制，PG 下才显现） | 中 | 一次轻量迁移补 FK + `alembic check` 零漂移 + 升降可逆 | 待办 |
| L-02 | P6 遗留① | 工厂端「批次查询」页未交付（菜单显示「建设中」） | 功能缺口，作品集观感 | 低 | `/factory/batches` 端点 + 只读页，或菜单暂撤 | 待办 |
| L-03 | P5 遗留⑧ | `/merchant/audits` 端点未实现，商户工作台审计卡降级为「—」 | 功能缺口 | 低 | 端点交付 + 卡片恢复真实数据 | 待办 |
| L-04 | P8 遗留（`reference` 记录） | `shared/core/ws.js` 与小程序契约四处不符、`api.js` 无条件注入后台令牌——P8 用继承覆盖绕开 | 新页面若直接复用共享模块会重踩 | 中 | 修正共享模块，或在模块头注释声明「仅限管理后台端使用」 | 待办 |
| L-05 | P8 遗留④ | 语音前端未实现（后端 `user.audio` / `assistant.audio` 帧与 asr/tts 已就绪） | 对话只有文字；真机联调后变高优 | 中 | miniapp 接录音/播放并与 WS 音频帧打通 | 待办 |
| L-06 | P8 遗留⑤ | `devices` 表无昵称列（前端占位） | 观感 | 低 | 迁移加列 + 端到端 | 待办 |
| L-07 | P11-B 核实 | 13 个未实现菜单项（平台 7 / 商户 5 / 工厂 1，见 `docs/04`） | 作品集观感（菜单可点进「建设中」） | 低 | 逐项实现，或从菜单移除并留痕 | 待办（逐项决策） |
| L-08 | P9 遗留① | 运营快照按 UTC 日期归档，`tenants.timezone` 未参与聚合，商户本地日期可能差一天 | 凌晨时段看板口径歧义 | 中 | 按租户时区聚合（涉及 4 张快照表 + 前端日期选择） | 待办 |
| L-09 | P6 遗留③ | `factory_orders.order_id` 无唯一索引（「一单一张工单」仅是服务层约束） | 并发下理论上可重复派单 | 低 | 迁移加 `UNIQUE(order_id)` | 待办 |
| L-10 | P6 遗留④ | 迁移 `0013` 不回填历史设备的 `factory_order_id` | 旧数据抽检会被拒 | — | — | **显式接受**（回填来源不可靠正是 ADR-09 要消除的；`make reset-db && make seed` 可回一致态） |
| L-11 | P10 遗留⑤ | `reset_db.sh` 备份与数据同盘 | 灾备能力弱 | 低 | 异机/对象存储备份（生产化时） | **显式接受**（当前为技术作品集定位，`docs/09` 已登记） |
| L-12 | P10 遗留② / 复盘 | CI 不构建镜像，CI 环境与部署环境有差异 | 「CI 绿」不证明「镜像可部署」 | 中 | CI 增加镜像构建 job（或明确记录理由） | 待办 |
| L-13 | P11-A 局限⑥ / P9 | 覆盖率 70%，三个 P9 服务 < 31% | 看板与 AI 配置的回归保障弱 | 中 | 关键路径补测，覆盖率门禁逐步上调（当前 65） | 待办 |
| L-14 | P9 遗留②③ | 内容库只读；知识库为关键词级检索、无向量化 | AI 能力深度 | 低 | 写接口与审核流程；向量化另立决策 | 待办 |
| L-15 | P8 遗留 / 复盘核对③④ | 开发库留有验收痕迹（终端用户 139…、工单 `FO-…ECN9` 等） | 演示纯净度 | 低 | `make reset-db && make seed` 回纯净态（演示前执行） | 待办（低频动作） |

> ★ 本表建立缘由见「项目整体复盘与质量评估（2026-09-24）」问题 4：**L-01 三处承诺从未执行**即是「记录过 ≠ 会处理」的实证。

---

## 里程碑

| 里程碑 | 交付标志 | 状态 | 达成日期 |
|---|---|---|---|
| M0 项目启动 | 脚手架、进度文件、仓库初始化就绪 | 已完成 | 2026-09-16 |
| M1 可登录骨架 | 后端可登录鉴权 + 前端可登录进工作台 | 已完成 | 2026-09-16 |
| M2 业务闭环（无 AI） | 客户开通 → 下单 → 生成设备 → 烧录 → 分配 → 扫码激活 | 已完成 | 2026-09-17 |
| M3 AI 闭环 | 终端设备可对话（离线模拟引擎），商户可配置 AI | 已完成 | 2026-09-17 |
| M4 可部署交付 | `docker compose up` 一键起服务 + 全端点冒烟通过 | 已完成 | 2026-09-17 |
| M5 公开就绪 | 专业文档集齐备 + 学习手册完成 + README 终版 | **已完成** | 2026-09-17 |

> **M2 达成说明**：整条链路已在真实服务上端到端跑通，并有**一条自动化测试**（`tests/e2e/test_full_loop.py`）把它钉进回归范围——客户开通与授权（P3）→ 商户下单与平台审核（P4）
> → 生成设备并入库（P4）→ 派单工厂、烧录上报、抽检、出货（P6）→ 批次导入设备入库（P6）→ 分配设备（P5）
> → 终端用户扫码解析、激活、绑定（P8）→ 解绑与再激活（P8）。P6 与 P8 各自的端到端验收脚本
> （71/71 与 50/50）覆盖了这条链路的每一段。
>
> **M3 达成说明**：「商户可配置 AI」由 P9 交付——商户端 AI 配置页（供应商 / 提示词 / 角色 / 音色 / 内容安全三开关，分区独立保存）、知识库（上传→解析→**真正注入对话**）、
> 以及「终端设备可对话」（P8 的离线模拟引擎，SSE 与 WebSocket 双通道 + 内容安全标注）共同构成闭环。浏览器实测确认：内容安全开关保存后「当前生效说明」即时更新。
>
> **M4 达成说明**：`docker compose up -d --build` 起服务后容器进入 `healthy`；`make smoke` 57 项断言**直连与经 Nginx 各通过一次**；`down && up` 后数据完全一致（持久化验证）；
> 敏感信息扫描 236 个文件 **0 命中**。WebSocket 经 Nginx 握手成功并以 `4401` 关闭，证明 `/ws/` 路由修复真实生效。
> **这一阶段的 12 个缺陷全部是「写好了但真跑起来不通」的类型**，没有一个是靠读代码或跑集成测试能发现的——**可部署性只能靠真的部署一次来证明**。
>
> **M5 达成说明**：**专业文档集齐备**（`docs/README` + `01`–`14` 共 14 篇，另有 `make docs-check` 八类一致性门禁）；
> **学习手册完成**（`learning/` 20 个文件 / 8177 行，**不上传 GitHub**）；
> **14 张各端截图**（`docs/assets/`）与 **`README.md` 终版**（界面预览 + 验收方式 + 已知局限）也已完成。
> 一项客观验证：在 `/tmp` 全新克隆后按 README 操作，**从克隆到四端全部可访问 ≈ 91 秒**（远低于 5 分钟）。
>
> 本阶段的一条做法值得记下：`docs/05` 的 655 个字段与 `docs/06` 的 145 个端点**都是脚本生成的**，
> 后者在生成时还会断言「源码提取的端点集合 == 契约快照集合」，不一致就拒绝生成——
> **文档与代码的一致性由门禁保证，而不是靠人记得同步**。
>
> ★ P11-B 补上了另一半：**让 4 个独立视角按代码复核文档**，一次就找出 **4 处真错**
> （能力矩阵错了一格、权限码拼写错、门禁类数四处不一致、菜单项算术不自洽），
> 我自己核对计数又找出 2 处。**这 6 处没有一处是靠「重新读一遍」发现的。**
>
> ★★ P11-C 又补上第三种视角：**装作第一次来的用户，照着 README 做一遍**——
> 在干净目录里照字面操作，`make setup` **直接失败**（启动期校验要求 6 项，README 只让改 3 项）。
> 这是本项目**唯一一个由「按文档操作」发现的缺陷**，已修复并附上可选的一行脚本。
> **同一个项目，换个身份就有新发现。**

---

## 关键架构决策（已定案，不再变更）

| # | 决策 |
|---|---|
| ADR-01 | 平台管理员 `tenant_id = NULL`（全局长），修正早期实现把它挂在 `t-001` 的矛盾 |
| ADR-02 | 烧录工厂独立 `factories` 表 + `role_type=FACTORY`，账号 `tenant_id = NULL` |
| ADR-03 | 设备状态以**四维**（`asset_status`/`activation_status`/`online_status`/`bind_status`）为权威，原型的 9 态降为前端派生展示 |
| ADR-04 | AI 业务标准以早期原型与 PRD-03 为准（早期实现无 AI 实现） |
| ADR-05 | 默认 SQLite，PostgreSQL 可选，Alembic 统一管控迁移 |
| ADR-06 | 前端零构建 ES Module，克隆即可打开，无需 `npm install` |
| ADR-07 | 未配置密钥的厂商一律返回 `VENDOR_UNAVAILABLE`，**绝不伪造成功** |
| ADR-08 | 多租户过滤只在 repository 层收口，禁止在 router 手写过滤 |
| ADR-09 | **设备与工单的归属落库**（`devices.factory_order_id`）：派单是归属的唯一诞生点，此后所有工单维度的查询（二维码清单 / 烧录联动 / 抽检校验）一律以该列为准，不再用「订单 + 状态」推断 |
| ADR-10 | **可冻结 / 可分配集合按业务能力定义并与迁移表严格对称**：冻结范围 P6 放宽为 `{IN_STOCK, ALLOCATED, BOUND}`（补齐入边），可分配范围补 `SHIPPED`（接通 P4 就存在的 `SHIPPED → ALLOCATED` 边）；单测钉成「集合 == 迁移表对应边」 |

---

## 变更记录

| 日期 | 变更内容 |
|---|---|
| 2026-09-16 | 项目启动；完成计划制定 |
| 2026-09-16 | **P0 完成**：目录骨架、`.gitignore`（含 `learning/` 排除）、`.env.example`、`Makefile`（30 命令）、`LICENSE`、`README`、`CONTRIBUTING`/`SECURITY`/`CHANGELOG`、`requirements*.txt`、`pyproject.toml`、`docker-compose.yml`、`deploy/Dockerfile.backend`、`deploy/nginx.conf`；产出 `todolist.md` 与 `项目进度.md`；`git init` 完成并通过三项验收 |
| 2026-09-16 | **P1 完成**：后端地基全部落地——配置与启动期安全校验、统一错误码（25 个）、traceId 中间件、权限目录（6 角色）、四层 Alembic 迁移、SQLAlchemy 模型（11 张表）、bcrypt+JWT 鉴权（含登录锁定与刷新令牌重放检测）、租户作用域收口、幂等键、分页、审计服务、8 个 API 端点、应用工厂、幂等种子数据；118 个测试通过，`make check` 全绿；**修复遗留缺陷 P-04** |
| 2026-09-16 | **P2 完成**：前端地基全部落地（40 文件 / 13567 行，零构建 ES Module）——全新视觉设计系统（靛紫主色 + 浅色侧导轨）、五个 CSS 文件、core 层（config/api/auth/router/guard/store/ws/end-user）、ui 组件层（含纯 SVG 图表、Canvas 手写二维码、⌘K 命令面板）、四端入口与统一登录页；**修复 P-01（刷新状态丢失）、P-02（back 目标）、P-05（无路由守卫）**；新增 `make fe-check`（77 处导入契约）与 `make qr-verify`（104 组二维码矩阵比对）；`make check` 保持全绿；浏览器实测四端可开、可登录、守卫生效、⌘K 可用 |
| 2026-09-16 | **P3 完成**：目录域全部落地——迁移 `0005_catalog` / `0006_client_product_miniapp`（`cloud_providers` / `product_templates` / `product_authorizations` / `client_products` / `miniapp_configs`）、`app/core/crypto.py` 密钥加密（明文只进不出，响应仅回掩码）、`tenant_service` / `catalog_service`、平台端 **17 个端点**、前端 **6 个页面**（首次启用参数化路由 `#/tenants/:id`、`#/client-products/:id`）、种子数据补充 2 云服务商 / 2 模板 / 3 授权 / 2 客户产品；**修复遗留缺陷 P-03、P-06**；顺带修复 6 项缺陷（前端监听器跨导航累积、`updated_at` 过期导致 5 个 PUT 端点 500、模板详情字段缺失、编码重复错误码不一致、`utcnow` 前向引用等）；测试 118 → 251；`make check` 与 `make fe-check` 全绿；浏览器实测 12 项 |
| 2026-09-16 | **P7 完成（并行开发）**：AI 抽象层与离线模拟引擎——`ai/base.py` 抽象与 DTO、`ai/registry.py` 四层解析、`ai/mock/*` 规则对话与可复现模拟、四个厂商适配器骨架（火山引擎按官方「硬件对话智能体」文档校对 Action 与请求体结构）、迁移 `0007_ai_dialogue`（8 张表）、`/ai/*` 六个端点；42 条测试通过，迁移零漂移 |
| 2026-09-16 | **并行开发机制落地**：主会话负责 P3，另开 2 个AI Agent 并行推进 P7 与 P3 集成测试；确立三条防冲突约定（严格文件归属 / 迁移编号预分配 / 追踪文件单写者，代理不提交） |
| 2026-09-16 | 新增 `docs/13-火山引擎硬件对话智能体配置说明.md`（控制台实测 + 官方文档取证）；**迁移编号顺延**：P3=`0005`/`0006`、P7=`0007`、P4=`0008`–`0010`、P5=`0011`、P9=`0012` |
| 2026-09-16 | **P4 完成**：订单与设备生成全部落地——迁移 `0008_orders` / `0009_devices_batches` / `0010_factory_production`（9 张表）、`qrcode_service`（JX/JD 双格式 + HMAC 签名，修正原型参数顺序缺陷）、`order_service`（状态机 + 审核 + 生成设备，4G 走厂商 / Wi-Fi 本地生成）、`device_service`（四维状态流转 + 冻结解冻报废 + 事件时间线）、`batch_service`（CSV 去重预检导入 + 错误报告 + 幂等重跑）、平台端 20 个端点、前端 5 个页面（订单 / 订单详情 / 设备 / 批次 / 商户订单）；**测试 251 → 397**；`make check` 与 `make fe-check` 全绿；浏览器实测 10 项 |
| 2026-09-16 | **P4 独立验证发现并修复 2 个真缺陷 + 4 条观察项**：D1 厂商失败审计口径不一致且缺资源标识；D2 同一 CSV 用于另一订单时静默复用旧批次（设备会挂到旧订单）；O1 冻结端点文案过期；O2 批次读路径未走 `assert_visible`（破 ADR-08 不变式）；O3 批次字段上限大于数据库列长（换 PostgreSQL 会写库报错）；O4 `duplicatedRows` 口径与字段名不符 |
| 2026-09-16 | **顺带修复 4 个缺陷**：① 状态相关页头动作不随状态更新（审核后仍显示「审核通过」，须手动 F5 才出现「生成设备」）→ `createDetailPage` 增加 `setActions()`；② **`api.raw()` 形同虚设**（P2 遗留：`raw` 形参不存在，且即便补上也会先消费 body 导致 `blob()` 抛 `Body is unusable`）；③ 「审核通过」确认框沿用驳回文案；④ `FREEZABLE_ASSET_STATUSES` 与 `ASSET_TRANSITIONS[FROZEN]` 不对称（P1 遗留）→ 收紧为只允许冻结 `IN_STOCK` 并新增 14 条单元断言 |
| 2026-09-16 | 新增 `docs/14-ESP32-S3刷机与联调步骤清单.md`：面向真机联调当天的可执行清单（云端自检 → 工具链 → 四条路径决策树 → 配网 → 首次对话三条判据 → F-01~F-10 失败速查 → 真机结果回填项） |
| 2026-09-16 | **账号中立化**：平台超级管理员账号原沿用自早期第三方实现，已改为 `admin`；新增平台运营账号 `13812345678`（`PLATFORM_OPERATOR`，可处理产品/订单/设备，不能改客户档案、云服务商、产品模板）；同步更新 `.env` / `.env.example` / `config.py` / 种子数据 / 测试夹具与断言 / 登录页演示账号按钮，并迁移开发库中的既有账号（**保留原密码哈希**） |
| 2026-09-16 | **P5 完成**：设备/分配/绑定全部落地——迁移 `0011_allocations_bindings`（`allocation_orders` / `allocation_items` / `device_bindings`，**三表内承载 5 分钟 confirm-token**，`UNIQUE(device_id)` 把单绑约束下沉到库层）、`allocation_service`（逐行校验、部分失败不回滚、`FAILED→EXECUTING` 可重跑、`COMPLETED` 幂等回放）、`binding_service`（precheck 的 SHA-256 命中 + 租户匹配 + 冻结拦截 + 授权校验 → 确认令牌用后销毁）、`heartbeat_service`（设备密钥鉴权 + 180 秒窗口口径）、**13 个新端点**（平台端 5 / 商户端 6 / 设备侧 1，另加设备密钥签发与模拟心跳）、前端 4 个新页 + 1 页改造；**测试 397 → 450**；`make check` 与 `make fe-check` 全绿；浏览器实测 19 项 |
| 2026-09-16 | **P5 补的演示数据**：种子新增 4 台**平台自有库存**设备（`tenant_id` 空 + `IN_STOCK`）——否则分配单的「选择设备」永远为空，分配链路不可达 |
| 2026-09-16 | **P5 实测发现并修复 6 个缺陷**：① 所有页头按钮显示成字面 `<span>刷新</span>`（`components.js` 漏 `raw()`，**P2 遗留**）；② 分配弹窗选完租户后可选设备永远为空（**分配功能完全不可用**）；③ 时间线事件明细显示字面 `<div>` 标签；④ 签发密钥后详情页不刷新且背景显示上一轮掩码；⑤ 空白串可当原因落库（与订单驳回口径不一致，验证代理发现）；⑥ 二维码前缀大小写导致「格式合法但判为伪造」的自相矛盾（验证代理发现） |
| 2026-09-17 | **P6 完成**：烧录工厂端全部落地——迁移 `0012_factory_account_link`（`user_accounts.factory_id` + JWT `factoryId`，工厂账号终于有归属）与 `0013_device_factory_order`（`devices.factory_order_id`，设备与工单的归属落库）、`FactoryOrderSerializer` 字段白名单脱敏（客户名取首个/末个文字字符，规避 `星********）` 这类观感缺陷）、`factory_service`（派单 / 烧录上报 / 抽检 / 出货 / 固件 / 统计 / 入库）、工厂端 9 + 平台端 6 = **15 个新端点**、前端 6 新页（工厂端）+ 平台端工厂订单页 + 订单派单与设备入库动作；**测试 456 → 737**；`make check` 与 `make fe-check` 全绿；端到端接口验收 71/71、浏览器实测 12 项 |
| 2026-09-17 | **P6 落实两处 P5 遗留**：① 放宽冻结范围为 `{IN_STOCK, ALLOCATED, BOUND}` 并补齐 `ALLOCATED`/`BOUND → FROZEN` 入边——「已出货给商户的设备」现在可以被停服（欠费 / 内容违规），解冻原路恢复；② 新增 `POST /platform/devices/stock-in`，批次导入的设备（`GENERATED`）终于能入库并进入分配链路；③ 可分配范围补 `SHIPPED`，接通 P4 就存在却无处使用的 `SHIPPED → ALLOCATED` 边 |
| 2026-09-17 | **P6 验收发现并修复 3 个真缺陷**：① 工单二维码清单返回订单下**全部**设备（工单委托 3 台却导出 5 张标签）→ 归属落库后按 `factory_order_id` 筛选；② 抽检只比对 `order_id`，同订单但未派工的设备也能计入合格率 → 改判 `factory_order_id`；③ 平台端设备页 `FREEZABLE` 集合仍是 `['IN_STOCK']`，后端已允许的「冻结已分配设备」在界面上点不到 → 集合同步 + 弹窗文案补「停服」语义 |
| 2026-09-17 | **P6 独立验证代理报出并修复**：① 工单 `quantity` 取订单合同数量导致 `burned_count` 与实际出货设备数永久对不上账 → 改取实际派工台数并另下发 `orderQuantity`；② `FACTORY_ORDER_EXISTS` 被状态校验抢先拦下、正常流程不可达 → 校验顺序调整；③ 三处注释/文档与实现不符（含会进 OpenAPI 描述的两处）；④ 脱敏首尾取字面字符导致的观感与被包裹标点泄漏 |
| 2026-09-17 | **补齐 P5 欠账测试**：`test_allocation.py`（33）、`test_tenant_isolation.py`（36，含路由元测试双向比对 `app.routes`）、`test_device_heartbeat.py`（31）；另新增 P6 的 `test_mask.py`（132）、`test_factory_desensitize.py`（7）、`test_factory_flow.py`（42） |
| 2026-09-17 | **迁移编号再次顺延**：P6 占用 `0012` / `0013`，P9 由 `0012` 顺延为 `0014` |
| 2026-09-17 | **接口验收脚本发现的历史测试笔误**：`order_service._transition_order` 重命名为公开 `transition_order`（P6 的派单/烧录/出货需在**同一处**校验订单状态机，禁止复制迁移规则），同步更新 `test_order_state_machine.py` 的 5 处调用 |
| 2026-09-17 | **P8 数据层**：迁移 `0014_miniapp_end_users_recharge`（`end_users` / `recharge_plans` / `recharge_orders` / `devices.settings`，**这三张表原属 P9**，因 P8 的登录与充值直接依赖它们而先落，**P9 顺延为 `0015`**）、终端用户令牌（与后台令牌同密钥、以 `type` 双向隔离）、`EndUserContext`（与 `AuthContext` 分成两个类型，让不成立的输入在编译期被挡住）、短信与支付的可配置模拟通道 + **生产环境启动期拒绝 mock**、种子补 4G 客户产品与 2 台「已分配待激活」设备 |
| 2026-09-17 | **P8 完成**：终端用户小程序端全部落地——`miniapp_service`（验证码登录 / 扫码解析 / 双路径激活 / 绑定与解绑 / 设备设置 / 充值）、`dialogue_service`（会话与消息落库 + 内容安全）、`app/realtime/ws_chat.py`（WebSocket 帧协议 + SSE 降级）、小程序 **16 + WS/SSE = 18 个端点**、前端 9 屏（从 720 行的 `main.js` 拆成 9 个独立模块 + `shell.js` 外壳）；**测试 737 → 757**；端到端接口验收 50/50、浏览器实测 11 项 |
| 2026-09-17 | **P9 数据层**：迁移 `0015_ops_metrics_ota`（`metrics_daily` / `metrics_hourly` / `metrics_region` / `content_hot_ranking` / `content_items` / `ota_packages` / `ota_records` + `devices.region` + `dialogue_messages.content_item_id`）、`app/models/ops.py`、**`app/core/storage.py`**（对象存储抽象：local 实现 + s3 显式安全失败、key 校验作为唯一路径拼接入口）；种子补内容库 6 条 / 终端用户 3 个 / **对话历史 13 会话 72 消息**（确定性、两产品规模刻意不同）/ 固件包 1 个 / 设备地域 11 台 |
| 2026-09-17 | **P9 完成**：AI 配置与运营看板全部落地——`ai_config_service`（AI 配置 + 知识库 + 内容库）、`metrics_service`（实时聚合 + 快照重建 + 留存）、`ota_service`（固件包 + 逐台推送）、商户端 26 + 平台端 8 = **34 个新端点**、前端商户端 5 页 + 平台端 OTA 页 + 客户产品详情「运营」Tab；**测试 758 → 804**；`make check` 与 `make fe-check` 全绿；浏览器实测 6 项（含 AI 配置分区保存、角色 4G/Wi-Fi 差异、运营看板快照重建 168 行、OTA 的 Wi-Fi 拒绝与 4G 诚实失败） |
| 2026-09-17 | **P9 修复 3 个真缺陷**：① 小时分布聚合用 `COUNT` 基座叠加行级列（`SELECT count(*), created_at` 无 `GROUP BY`，SQLite 只返回 1 行）→ 改为显式取行并给基座补警告 docstring；② P4 与 P9 都定义 `/merchant/products`（生效取决于 include 顺序）→ 收敛为一个富化版本，删除 P4 版；③ 种子把「今天」的会话放在未来时刻（凌晨运行时聚合会排除，导致「今天」恒为空）→ 钳制到已过去的时刻 |
| 2026-09-17 | **P9 遗留缺陷守卫落地**：`test_metrics_isolation.py`（9 条，核心断言「快照之和 == 实时聚合」——用系数摊派的实现过不了）与 `test_ota.py`（14 条，Wi-Fi 推送 409 且**不留任何记录**）；租户隔离矩阵**扩到 26 条 P9 商户路由**，首次覆盖「查询参数形态的越权」（`productId` 传他人产品 → 404） |
| 2026-09-17 | **P9 顺带修正**：3 处写死厂商名的文案（角色不支持原因、OTA 弹窗两处）改为厂商中立——判定本就数据驱动（`ota_support` / 联网方式），文案点名「京东 JoyInside」而演示的 Wi-Fi 产品实际用火山 |
| 2026-09-17 | **P8 浏览器实测发现并修复 1 个真缺陷**：「已激活但未绑定」时重复激活只回放结果、不补绑定 → 界面显示「激活成功」但库里没有绑定关系，一进对话页被 WS `4403` 拒绝且看不出原因（触发场景常见：别处激活过、或上一位用户解绑过——解绑刻意不改变激活状态）。幂等分支改为同时保证绑定关系，并给 `_bind_to_end_user` 加「同用户已绑定 → 空操作」早返回；补回归测试 |
| 2026-09-17 | **P8 代理报出并处理**：① 既有 flaky 测试（篡改 JD 签名末位时若末位本就是 `0` 等于没篡改，约 1/16 概率假通过）→ 改为显式翻转并加断言；② 4G 激活演示路径默认 503（演示模板指向真实厂商且无密钥）→ 补种子把**演示产品**显式配成离线模拟引擎，未配置的产品依然 503（ADR-07 仍可观测）；③ `frontend/shared/core/ws.js` 与真实协议四处不符（URL / 令牌来源 / 帧名 / SSE 用 GET）→ P8 用「继承并覆盖」绕开，**共享模块未改**，记入遗留 |
| 2026-09-17 | **P10 部署资产落地**：`docker-compose.yml`（仓库根，三 profile：默认 SQLite / `nginx` 前置 / `postgres` 可选）、`deploy/Dockerfile.backend`（CMD 改为 `alembic upgrade head && python -m app.db.seed && uvicorn --workers`）、`deploy/nginx.conf`（修 WS location / 缓存策略 / `/login` 反代 / 逐端 SPA 回退）、`deploy/.env.example`（**生产部署清单**）、新增 `.dockerignore`、`.github/workflows/ci.yml`（lint / test / contract / frontend / secrets 五 job）、`scripts/smoke_test.sh`（57 断言 + 最低断言数守卫）、`scripts/scan_secrets.py`、`scripts/gen_qrcodes.py`、`scripts/reset_db.sh`、`scripts/dev.sh`；`config.py` 引入 `FRONTEND_DIR` / `DATA_DIR` 与 `_resolve_frontend_root()` / `_guess_repo_root()` |
| 2026-09-17 | **P10 完成（四项验收标准全部实测达成）**：`docker compose up -d --build` → 容器 `healthy`；`make smoke` **57/57**（直连与经 Nginx 各一次）；`down && up` 后数据**完全一致**（租户 2 / 设备 11 / 订单 2 / 工厂工单 0）；`scan_secrets.py` 扫描 236 个文件 **0 命中**；WebSocket 经 Nginx 握手成功并以 `4401` 关闭（证明 `/ws/` 路由修复生效）；`make check` **804 passed**、mypy 95 文件、契约一致、`fe-check` 399 处导入全匹配。**M4 可部署交付达成** |
| 2026-09-17 | **P10 由「真跑一遍 Docker」发现并修复 12 个真问题**（全部属「文档写了但跑不通」，无一是靠读代码或跑集成测试能发现的）：① `make up` 路径错（compose 在仓库根）；② nginx WS location 写成 `/api/v1/ws/` 而真实路径是 `/ws/miniapp/chat`；③ 未指纹化 JS/CSS 被声明 `immutable` 强缓存一年；④ `DATABASE_URL` 读到开发机相对路径致 SQLite 落在镜像层、`down && up` 丢数据；⑤ 缺 `.dockerignore`（上下文 229MB 且 `.env` 有进镜像风险）；⑥ `POSTGRES_PASSWORD: ${…:?}` 在未启用 profile 时也参与插值、整个 compose 不可用；⑦ `--workers 4` 下每个 worker 各播种一次（4 进程并发写 SQLite，1 成功 3 失败）；⑧ `FRONTEND_ROOT` 上溯三级在容器里数到 `/`，**整个 UI 404 而 API 正常**且只有一条 WARNING；⑨ `DATA_ROOT` 不可配置 + 非 root 运行 → `PermissionError` 崩溃循环；⑩ nginx 缺 `/login`（应用路由非磁盘文件）→ 经 nginx 登录页 404；⑪ `smoke_test.sh` 的 `json_get` 用 heredoc 读程序又占 stdin → 取值恒为空、登录永远失败；⑫ 同一脚本在误删计数函数后仍打印「0 通过 / 全部通过」——**会静默通过的检查比没有检查更危险** |
| 2026-09-17 | **P10 敏感信息扫描器误报收敛**：首版报出 **133 条误报**（手机号命中 8 位弱口令规则、HTML `placeholder=` 当成密钥赋值、黑名单表枚举字面量、`*SCREAMING*` 常量、URL 路径常量、请求 DTO 里**本就要收明文**的 `access_key`）。收窄弱口令表并只匹配赋值形态、给测试目录加 `ALLOWED_PATH_PREFIXES`、新增 `_looks_like_a_real_secret()`、把「明文密钥字段」规则限定到**响应类**；收敛后 0 命中，并补一个自测证明规则**确实会命中**——**「规则不响」与「规则坏了」必须能区分开** |
| 2026-09-17 | **P11-A 专业文档集完成（13 份新文档）**：`docs/README.md`（按「你想做什么」导航 + 三条阅读路径）与 `docs/01`–`docs/12`。其中 `docs/05` **1809 行**（8 张分域 ER 图 + **46 张表 × 655 个字段**的完整字段字典 + 六条复合唯一键 + `in_stock` 语义对齐）、`docs/06` 含 **145 个端点表与 31 个错误码**、`docs/07` 含 **54 个权限码与 6 角色矩阵**、`docs/09` 是**逐条可勾选**的验收清单、`docs/10` 是含**实测基准数据**的 AI 验证报告、`docs/11` 如实记录**覆盖率 70%**、`docs/12` 沉淀 **8 条经验** |
| 2026-09-17 | **P11-A 新增文档一致性门禁** `scripts/check_docs.py` + `make docs-check`（**8 类校验**：端点↔快照双向、表名↔迁移/模型、错误码↔`ErrorCode` **且校验 HTTP 状态↔`_STATUS_MAP`**、权限码↔`permissions.py` 双向、枚举值↔`enums.py`、`make` 目标/脚本/相对链接可解析、`docs/11` 逐文件用例数↔源码、每份文档含边界声明且无不上传目录的内部路径）；已接入 `make check` 与 CI 的 `contract` job |
| 2026-09-17 | **两处关键内容改为脚本生成而非手抄**：`docs/05` 的字段名/类型/约束**由 ORM 元数据（`Base.metadata`）程序化导出**；`docs/06` 的端点表**由源码 AST 与契约快照联合生成**，且生成时**断言「源码提取集合 == 快照集合」**（实测 145 == 145），不一致即拒绝生成——**文档不可能与实现脱节** |
| 2026-09-17 | **门禁首次运行即报出 5 处问题**（证明它有效）：① `docs/11` 把 `test_allocation.py` 的用例数写成 **33**（实际 **23**，会让分组合计对不上 353）；② `docs/14` 缺少「已知局限 / 取证边界」小节；③ `docs/07` 把**不存在的**权限码写在反引号里；④⑤ 两处为**检查器自身规则过严**导致的误报（规则描述里的字面占位、以及正文提到「不上传的目录名」）。处理：前三处修正内容，第四处改写措辞，第五处**收窄规则为「禁止指向其内部的路径/链接」**——★ **正确做法是修订规则以匹配真实风险（断链），而不是让文档内容为迁就工具而变形** |
| 2026-09-17 | **P11-A 验收（`make check` 全绿）**：`make lint` → `All checks passed!`；`make typecheck` → `no issues found in **95 source files**`；`make test` → **804 passed in 310.55s**；`make openapi-check` → 契约一致（119 paths / 145 operations）；**`make docs-check` → 8 类校验全部通过**；`make check` → **全部检查通过**。里程碑 M5「专业文档集齐备」达成，学习手册与截图待 P11-B/C |
| 2026-09-17 | **P11-B 学习手册完成**：`learning/` 下 **20 个文件 / 8177 行**——`README.md`（12 段结构说明 + 三条阅读路径 + 12 份产物清单）、`00-学习地图.md`（依赖图 + 13 模块时长 + **三个里程碑** + 四条不能跳的依赖 + 卡住时的降级办法）、模块 `01`–`12`（**每个 445–550 行，严格 12 段固定结构**，含生活类比、「本项目中的体现」引用真实文件名/表名/端点、自测题附参考答案、⑨⑩两段给可直接照说的话术）、`exercises/01`–`06`（每个含情境、评分要点、**「被开发这样问说明哪里没想清」**、自查清单、进阶挑战）。**不上传 GitHub**（`.gitignore:15`，`git ls-files` 中 0 条） |
| 2026-09-17 | **P11-B 由 4 个独立AI Agent 并行撰写**（吸取 P11-A 时AI Agent 因全网 DNS 故障失败的教训，**先做小任务探测确认可用**才派活——探测回复「40 个 ID 前缀、首个是 `t`」，我核对 `ids.py` 后确认正确才启动）。每个代理的 prompt 里冻结了 12 段结构规范、风格约定、禁止事项与**可直接引用的已核对数字**，并要求「如发现 `docs/` 与代码不符，以代码为准并在回报中明确指出」 |
| 2026-09-17 | **★ P11-B 发现并修正 6 处 `docs/` 与代码不一致**（4 处由代理按代码核对发现，2 处由我核对计数发现）：① `docs/08` 能力矩阵把 `mock` 的 `device` 写成「不支持」，实际 `mock/engine.py` 是 `capabilities = ALL_CAPABILITIES`（含 `device`），`base.py` 原文为「`LOCAL`（mock）—— 四类能力全有，否则离线跑不通全流程」→ 修正矩阵并补上原因，**且把这个纠错过程写进文档留痕**；② `docs/07` 与 `docs/11` 各有一处 `factory:batch_read` 拼写错误（漏一段冒号）→ 修正，**并给 `check_docs.py` 增加「格式写错」子检查**（原正则只匹配格式正确的码，错码会静默通过——**教训：只校验「正确的形状」会漏掉「形状都不对」的输入**）；③ `make docs-check` 的类数四处不一致（六件事 / 六类 / 七类）→ 统一为**八类**并逐条列出；④ `docs/04` 与 `docs/12` 的「18 个菜单项（平台 7 / 商户 5 / 工厂 1）」算术不自洽（7+5+1=13）→ 核实 `menus.js` 与各端已注册路由后修正为 **13**；⑤ `项目进度.md` 与 `CHANGELOG.md` 的「27 条术语表」实际为 **24 条** → 修正；⑥ `docs/05` §3.3 列 19 条 `idx_*` 而 ORM 有 20 处 `Index(...)` 声明 → 在手册中写清两个口径，**并在 `docs/05` 新增 `## 3.4 外键是否真的生效`**（`session.py` 逐连接执行 `PRAGMA foreign_keys=ON`，而 SQLite **默认不强制**外键——「默认行为与直觉相反」的坑） |
| 2026-09-17 | **P11-B 客观校验（一次性脚本，不入库）**：**8 类检查全部通过**——20 个文件齐全；12/12 模块含元信息块 + `## 0. 结论先行` + **恰好 12 个二级章节且编号标题顺序符合规范**；12/12 含参考答案；**装饰性 emoji 0 处**（只用 `🔑 📌 ✅ ⚠️ ★ —`）；**第一人称「我」0 处**（排除反引号内代码与引号内话术）；篇幅 445–550 行均在区间；12/12 模块真实代码标识引用达标；**198 处相对链接全部可解析**。★ 校验器**不入库**（`learning/` 不上传，提交的校验器会在 CI 与他人机器上因目录不存在而失败），它靠「首跑报出 11 处违规（含它自身 2 个 bug）→ 修正后转绿」证明有效 |
| 2026-09-17 | **P11-C 截图与 README 终版**：新增 **14 张各端截图**（`docs/assets/`，2.2MB：登录页 + 平台端 6 + 商户端 4 + 工厂端 2 + 小程序 1），逐张人工核验为**真实登录态**；`README.md` 终版新增「界面预览」（7 张内嵌 + 14 张清单）、规模速查表、「**本项目的验收方式**」（四层手段及各自不可替代的理由）、「**已知局限**」表（合规 / AI 联调 / CI / 数据库 / 部署 / 性能 / 测试 / 功能缺口八类）；徽章含 `coverage-70%`（如实标注）；18 处文档链接与 7 处本地截图路径全部可解析 |
| 2026-09-17 | **★ P11-C 由「装作第一次来的用户」发现并修复 README 的真实缺陷**：在 `/tmp` 全新克隆后**照 README 字面操作，`make setup` 直接失败（81 秒）**——启动期安全校验要求 **6 项**（`JWT_SECRET_KEY`、`QR_SIGN_SECRET`、`PLATFORM_ADMIN_PASSWORD`、`MERCHANT_ADMIN_PASSWORD`、`FACTORY_ADMIN_PASSWORD`、**`PLATFORM_OPERATOR_PASSWORD`**），而 README 原文只让改 3 个管理员口令。补齐 6 项后 `make setup` 成功（**72 秒**）、健康检查 200、四端入口（`/`、`/login`、`/platform/`、`/merchant/`、`/factory/`、`/miniapp/`）**全部 200**，**从克隆到四端可用 ≈ 91 秒**。已修正 README 两种方式并附可选的一行脚本。★ `docs/01` 的 `## 5.` 一直列全了这 6 类条件——**是根 README 漏了**。这是本项目**唯一一个由「按文档操作」发现的缺陷** |
| 2026-09-17 | **P11-C 截图采集方式（诚实记录）**：`bsk screenshot` 在本机**只能成功一次**，之后持续 `tool RPC timed out after 30s`（同期 `bsk evaluate` 正常；`bsk daemon restart` 能恢复一次但不足以支撑十几张图，且存在 CLI 0.2.1 / 协议 1.1 与扩展 0.3.0 的版本漂移告警）。改用**独立 headless Chrome + 同源引导页**：HTTP 登录拿令牌 → 引导页写入 localStorage → 每端一个 `--user-data-dir` → 直接截目标页 → 收尾删除引导页并确认 `git status` 干净。**截图本身是真实服务渲染的真实页面，只是采集通道换了** |
| 2026-09-17 | **P11 完成（12/12 阶段，里程碑 M0–M5 全部达成）**：两项 🔑 最终验证通过——① `git check-ignore -v learning/README.md` 命中 `.gitignore:15`，`git ls-files \| grep "^learning/"` **0 条**；② 陌生环境按 README 操作 **≈91 秒**四端可用（远低于 5 分钟）。**全部 12 个阶段交付完毕**，公开前的待办已登记（CI 未真实运行、CHANGELOG 未切 tag、`docs/12` 的「M5 未达成」已过期、AI 未联调、未实现儿童数据合规等） |
| 2026-09-17 | **公开前整理（六项待办逐一处理）**：① CI 本地可验项全验（YAML 可解析 5 job、引用路径全在、命令与 Makefile 一致），**并修掉一处真实冗余**——`pytest -q` 本就会收集 e2e 之后又单独跑一次，**每次 CI 白等约 5 分钟**；② `CHANGELOG` 切 **`[1.0.0] - 2026-09-17`**（首个功能完整版本）+ 保留空 `[未发布]` + 更新 compare/release 链接 + 创建本地 git tag；③ `docs/12` 三处过期表述同步；④ AI 未联调 / 儿童数据合规未实现——无法真正实现，改为让它在 README 顶部**不可错过**；⑤ **CI 新增覆盖率下限 `--cov-fail-under=65`**（实测 70 留 5 点余量）——此前「804 用例全绿但覆盖率 70%」**没有任何机制会提醒**；⑥ **PostgreSQL 真跑验证** |
| 2026-09-17 | ★★ **公开前整理发现并修复一个 SQLite 上永远复现不出来的真缺陷**：`metrics_service.py` 的 `_date_between` 把日期用 `.isoformat()` **当字符串绑定**——SQLite 动态类型下 `func.date()` 返回 TEXT、字符串比字符串**完全正常（804 用例全绿）**，而 PostgreSQL 的 `func.date()` 返回真正的 `date`，与 `varchar` 比较直接抛 `UndefinedFunctionError: operator does not exist: date >= character varying`，**运营看板的概览与快照重建两个最核心端点直接 500**。修复为绑定 `date` 对象；**新增回归守卫** `tests/unit/test_sql_dialect_portability.py`（3 函数/5 用例）——**不测行为、测绑定参数类型**（断言是 `date` 而非 `str`，已实测旧写法会被拦下）。发现方式：**公开前真跑一次 PostgreSQL，冒烟报出这 2 项失败**。★ 这条修正了 P10 早先的判断：「PostgreSQL 只验证配置可解析」**不是**可接受的局限——一个核心页面在 PG 上直接 500 |
| 2026-09-17 | **PostgreSQL 完整验证通过（原「只验证配置可解析」已解除）**：`docker compose --profile postgres` 起真实 PG 16 → `alembic upgrade` 跑到 **`0015`** → 种子写入（**47 张表** = 46 业务表 + `alembic_version`；租户 2 / 设备 11 / 订单 2）→ 应用容器 **healthy** → **冒烟 57/57 全通过**。过程踩到并记录了三个「脚本坑」（都是我的测试脚本问题，非项目问题）：`down -v` 不删显式命名卷、postgres 仅在**首次初始化卷**时采用 `POSTGRES_PASSWORD`、冒烟脚本的变量名是 `SMOKE_BASE_URL` 而非 `API_BASE` |
| 2026-09-17 | **重新采集 14 张截图**：原截图采于「账号中立化」之前，**登录页的演示账号按钮仍显示旧商户账号**（该按钮是 `frontend/index.html` 里硬编码的——代码改了而图没改）。已对齐本地 `.env` → `make reset-db && make seed` 回到纯净演示态 → 重采全部 14 张；并新增**机器验证**：用 `--dump-dom` 抓 14 个页面的渲染 DOM，逐页 grep「不应出现的字符串」（旧账号 / 验收痕迹 / 已移除的内部文件名）——**不靠肉眼看图** |
| 2026-09-17 | **P11-D 收官验证（把三条 ⚠️ 局限真的跑掉）**：新增 `scripts/load_test.py`（标准库 + `httpx`，**不引入 ab/wrk/hey**；非 GET 需显式 `--allow-write`，只读方法传 `--body` 直接拒绝执行）。三项实测——① **负载测试**（20 并发 × 15 秒，同一脚本打两种后端）：读 **73.6 / 72.0** QPS、写 **30.7 / 93.0** QPS，SQLite 写失败 **4/487**（`database is locked`）而 PG **0/1414**；★ **结论修正了本项目自己的假设——读路径上 SQLite 并不输**（吞吐持平、尾延迟 p95 436 vs 994 ms 更好），PG 的优势**完全集中在写路径**；② **Nginx TLS** 四项全过（HTTPS API/SPA 全 200、HTTP→HTTPS **301**、★ **`wss` 握手成功并以 close code 4401 关闭**、`TLSv1.3/AES-256-GCM`）；③ **多实例**分发 **31/29**、WS 经 Nginx 正常，★ **但共享 SQLite 卷并发写 31.4 QPS + 2×500**（单实例 30.7 → **无提升**，冲突翻倍）→ **加实例救不了 SQLite 的写并发；多实例必须配 PostgreSQL** |
| 2026-09-17 | ★★ **P11-D 顺带抓到「改一个账号名就让部署起不来」的真缺陷**：`_seed_admin_users` **按 `account` 查重、却按固定 `id` 插入**——两个判据不一致；`.env` 里改了管理员登录账号名后按新名字查不到 → 拿同一个 `id` 再插 → `IntegrityError: UNIQUE constraint failed: user_accounts.id`；因容器入口是「**先播种、再起服务**」，表现为**启动即崩、无限重启**。**新库 / 测试库 / CI 全部复现不出来**，是多实例实验复用旧数据卷才炸出来的。修复为**一律按 `id` 查重**（同名空操作保密码 / 改名同步登录名并告警 / 名字被别的 `id` 占用则跳过并告警）；在**真实故障卷**上实测 `Restarting (1)` → `healthy`、改名告警出现、`password_hash` 不变、重启幂等；并新增 5 条回归测试（**改名那条在修复前的实现上会失败**，已实测） |
| 2026-09-17 | **全仓库表述中性化**（用户选择）：把指向早期实现的称谓统一为「**早期实现 / 早期第三方实现**」，并把几个历史机制名改为中性词（占位视图、其数据库 Schema、早期实现的编译产物等）；覆盖 `docs/**`、三份追踪文件、`CHANGELOG`、`.env.example` / `deploy/.env.example` / `.gitignore`、`backend/app/**` 与 `backend/tests/**` 的注释与断言消息、`frontend/shared/design/*.css`。**保留第三方 SDK / 库的 9 处合法用法**（火山 OSAL、segno、`qrcode` 库比对——它们指代的是那些库自身，不是本项目的来源）；`learning/` 因不上传**不在范围内**。★ **校验踩坑**：`git ls-files` 默认会把 CJK 文件名转义成 `\345…`，`grep` 因此**静默跳过整个 `docs/`**——我第一次就是这样把「没检查」误判成「已干净」的；必须用 `git -c core.quotePath=false ls-files` |
| 2026-09-17 | ★★ **推送到 GitHub 并在真实环境跑通 CI**：仓库 `https://github.com/Peterwong666/ToyVerse-Cloud`（public），推送 `main` + 附注 tag `v1.0.0`。远端原有**较旧快照**（20 提交 / 280 文件，止于「账号中立化」），与本地**无共同祖先**（本机做过作者重写）→ 强推（先 `--dry-run` 演练；核对「远端独有文件」只有 `SUMMARY.md`；旧历史在本地留为分支 `backup/origin-main-b6c1f06`）。★ **首次真跑抓到 CI 的一个真缺陷**：`ci.yml` 的 `on.push.branches` 写的是 **`master`**、而默认分支是 **`main`** → **推送根本不触发任何 CI**（查 API 得 `total_count = 0`）；修复后 **run #1 五 job 全绿**（代码规范 43s / 测试 2m42s / 契约 25s / 前端导入 5s / 敏感扫描 6s，**无步骤被跳过**）。另把仓库 `git clone` 到**空目录**（无 `.env`、无 `venv`）逐条重跑 CI 的 5 条命令做独立复核 → 全部通过（`813 passed`、覆盖率 **69.92%** ≥ 65 门槛） |
| 2026-09-18 | **进度复盘与待决项梳理**：用户反馈「昨天进度较慢」，核对后发现代码交付本身已完整（P0–P11 全部完成、CI 全绿），剩余瓶颈均为外部依赖型任务。已产出两份本地工作文档：[`分析_进度瓶颈与待决项.md`](./分析_进度瓶颈与待决项.md)（根因：ESP32/真实厂商联调、儿童合规、多实例+PG 验证；需用户确认的 5 项决策；P7 todolist 不一致）与 [`解决方案_后续待决项执行计划.md`](./解决方案_后续待决项执行计划.md)（ESP32 路径 D/C 决策树、儿童合规自查清单、多实例+PG exact 验证命令、优先级建议）。两文档均明确不上传 GitHub。已修正 `todolist.md` P7 三项 `[ ]` 为 `[x]` |
| 2026-09-18 | **ESP32 云端链路自检完成**：用户在平台端「云服务商配置」录入火山引擎 AccessKey / SecretKey 后，调用 `POST /api/v1/platform/clouds/cloud-volcano-hw/test`，返回 `SUCCESS` / `ok=true` / `latencyMs=758` / message「网络可达（HTTP 400）——该结论仅代表网络连通，密钥有效性将在实际调用厂商接口时验证」。★ 意义：火山引擎服务端接口网络可达，平台侧凭证已加密落库，**云端链路这一层的不确定性已清零**。下一步：待用户有时间刷机时，验证 ESP32 设备端配网/激活 |
| 2026-09-18 | **多实例 + PostgreSQL 组合验证完成**：① 在 `.env` 补全 `APP_DATABASE_URL` / `POSTGRES_*`；② 单实例 PG 冒烟 **57/57 通过**；③ 修改 `docker-compose.yml`（移除 `app` 的 `container_name`，Nginx `depends_on` 由 `condition: service_healthy` 改为简单依赖）后，`docker compose --profile postgres --profile nginx up -d --scale app=2` 启动 2 实例 + 1 Nginx + 1 PG；④ 经 Nginx 冒烟 **57/57 通过**；⑤ 读压测 20 并发 × 15 秒：142.5 QPS / 0 失败；⑥ 写压测（`POST /api/v1/platform/clouds/cloud-volcano-hw/test`）20 并发 × 15 秒：66.2 QPS / **1000/1000 成功**；⑦ 日志验证请求分发均衡（健康检查 1097/1095，写请求 503/502）；⑧ WebSocket 经 Nginx 握手成功并以 **4401** 关闭。★ 结论：**多实例 + PG 组合可用，写并发无 500，加实例能提升 PG 读吞吐**；同步更新 `docs/11-测试与质量保障.md`（`## 8.3.2` + `## 8.5`） |
| 2026-09-18 | **儿童数据合规自查完成**：在 `SECURITY.md` 新增「儿童数据合规边界（自查清单）」小节，列明 6 类涉及儿童个人信息的数据、7 项代码层面已实现的保护措施、8 项生产前必须补齐的缺口，并明确当前版本作为技术作品集/学习项目风险可控、面向真实儿童用户前必须经法务评估。 |
| 2026-09-24 | **项目整体复盘与质量评估**（纯复盘，未改动任何代码）：对 P0–P11 及 09-20/09-22 硬件与协议验证做整体质量评估（逐阶段判定 + 三条质量主线）。实核九项发现的要点——① **远端 CI run #4（`4bc67b7`）失败且无人记录**，其后 4 个本地提交未推送、`volcano.py` 有未提交修改；② 阻塞事项表「暂无」与 License 2/2 已满的实际阻塞不符；③ 进度文件落后 git 实际进度 2 天（P-A/P-B 已通过、采样率已确认，均未记入）；④ 外键轻量迁移在 P5/P7 三处承诺但从未执行（迁移止于 `0015`）；⑤ 遗留事项散落无台账（至少 8 项功能性开口未闭环）；⑥ `docs/13`/`docs/08` 未标注已被路线 B 部分取代。已给出 P1/P2/P3 三级修改方案与下一步排序，详见「项目整体复盘与质量评估（2026-09-24）」章节 |
| 2026-09-24 | **执行复盘方案（用户批准当日完成）**：① 修复 CI run #4 根因（09-22 提交未跑门禁：ruff 16 处 + mypy 4 处 + 快照/文档计数过期），并**新发现「测试不密封」问题**——开发机 `.env` 的火山 AK/SK 使 8 条 ADR-07 测试反向失败，conftest 已把厂商凭证钉空；② 顺带落地 `volcano.py` 的未提交修改（AK/SK HMAC-SHA256 签名实现，闭环 P7 局限①）；③ B-01 License 阻塞登记、09-22 补记、**遗留事项登记表 L-01～L-15**、docs/13+08 路线 B 标注、完成度口径说明、更新约定第 6/7 条；④ `make check` **828 passed**、覆盖率 **68%**（新代码拉低 2 点，高于 65 门槛）；⑤ 推送 8 个提交，**CI run #5（`a89e3da`）五 job 全绿**。四个提交：`2de7ddb` feat(ai) 签名 / `94c4992` fix(gate) 门禁与测试隔离 / `994152a` chore(contract) 快照与计数 / `a89e3da` docs(review) 本行以上全部文档 |
| 2026-09-24 | **B-01 实查与决策（用户批准）**：用 bsk 打开火山「硬件对话智能体」控制台实查——★ **「License 2/2 已满」的记载被修正**：账号已领 10 个免费基础版（各含 9h 语音 + 1350k tokens），仅「智能对话硬件001」产品分配额度用满（2/2/0），box1 与小方块玩具各 1 个未激活，**账号未分配尚余 6 个**——09-22 的 `12000130` 是产品级分配用尽，非账号配额。**决策：先复用 + 无需扩容**——移植调试复用 `pa-test-6333c399` 的 `device_secret`；新设备注册前在控制台自助给目标产品分配 License；购买扩展资源推迟到免费额度不足时评估。阻塞解除，「当前节点」同步更新 |
---

## 更新约定

1. **每完成一个阶段**，必须同步更新：本文件的总体进度条、主进度表、里程碑，以及 `todolist.md` 对应复选框。
2. **主进度表状态取值**：`未开始` / `进行中` / `已完成` / `阻塞`。
3. **日期格式统一** `YYYY-MM-DD`；开始与完成日期在同一天时也需完整填写。
4. **阻塞事项必须登记**，并注明影响的阶段与提出日期。
5. 新增或调整任务时，**两份文件必须同时更新**，保持一一对应。
6. **推送后必须实查 CI 结论**（公开 API 或 Actions 页），红灯不过夜——CI run #4 静默失败两天的教训（2026-09-24 复盘问题 1）。
7. **凡提交涉及端点 / 测试增删，提交前必须本地跑 `make openapi-check` 与 `make docs-check`**——09-22 的提交跳过了这一步，导致 CI 红灯（2026-09-24 复盘问题 1/7）。
