# ToyVerse Cloud 项目进度

> **项目**：多租户 AI 智能玩具 SaaS 平台
> **目录**：`/home/peter/py/saas_ai_toy_platform`
> **配套文件**：[`todolist.md`](./todolist.md)（详细任务清单）
> **最后更新**：2026-09-16

---

## 总体进度

```
P0  ████████████████████  100%   脚手架与进度文件          已完成
P1  ████████████████████  100%   后端地基                  已完成
P2  ████████████████████  100%   前端地基                  已完成
P3  ████████████████████  100%   目录域                    已完成
P4  ████████████████████  100%   订单与设备生成            已完成
P5  ░░░░░░░░░░░░░░░░░░░░    0%   设备/分配/绑定            未开始
P6  ░░░░░░░░░░░░░░░░░░░░    0%   烧录工厂端                未开始
P7  ████████████████████  100%   AI 抽象与模拟引擎         已完成
P8  ░░░░░░░░░░░░░░░░░░░░    0%   终端用户小程序端          未开始
P9  ░░░░░░░░░░░░░░░░░░░░    0%   AI 配置与运营看板         未开始
P10 ░░░░░░░░░░░░░░░░░░░░    0%   部署与质量保障            未开始
P11 ░░░░░░░░░░░░░░░░░░░░    0%   文档/学习手册/复盘        未开始
```

| 指标 | 数值 |
|---|---|
| 阶段总数 | 12（P0 – P11） |
| 已完成阶段 | 6（P0、P1、P2、P3、P4、P7） |
| 进行中阶段 | 0 |
| 阻塞阶段 | 0 |
| **整体完成度** | **50%**（按阶段等权估算：6/12） |

---

## 状态图例

| 状态 | 含义 |
|---|---|
| 未开始 | 尚未动工 |
| 进行中 | 正在执行 |
| 已完成 | 已通过该阶段验收标准 |
| 阻塞 | 遇到阻塞，需决策或依赖 |

---

## 主进度表

| 阶段 | 任务 | 状态 | 开始日期 | 完成日期 | 备注 |
|---|---|---|---|---|---|
| P0 | 创建目录骨架 | 已完成 | 2026-09-16 | 2026-09-16 | 38 个目录 |
| P0 | 编写 `.gitignore` | 已完成 | 2026-09-16 | 2026-09-16 | 已排除 `learning/`、`.env`、`data/`、`*.jar` |
| P0 | 编写 `.env.example` | 已完成 | 2026-09-16 | 2026-09-16 | 十一大类变量，含中文注释，无真实密钥 |
| P0 | 编写 `Makefile` | 已完成 | 2026-09-16 | 2026-09-16 | 30+ 命令，含 `help/setup/dev/test/migrate/seed/up/down` |
| P0 | 添加 `LICENSE` | 已完成 | 2026-09-16 | 2026-09-16 | MIT |
| P0 | 产出 `todolist.md` | 已完成 | 2026-09-16 | 2026-09-16 | 含 P0–P11 全量任务 + 3 个附录 |
| P0 | 产出 `项目进度.md` | 已完成 | 2026-09-16 | 2026-09-16 | 本文件 |
| P0 | 编写 `README.md` 骨架 | 已完成 | 2026-09-16 | 2026-09-16 | 含简介、四端、快速开始、架构图、技术栈 |
| P0 | 编写 `backend/requirements.txt` | 已完成 | 2026-09-16 | 2026-09-16 | FastAPI/SQLAlchemy 2.0/PyJWT/bcrypt 等 |
| P0 | 编写 `backend/requirements-dev.txt` | 已完成 | 2026-09-16 | 2026-09-16 | pytest/ruff/mypy |
| P0 | 编写 `backend/pyproject.toml` | 已完成 | 2026-09-16 | 2026-09-16 | ruff + mypy(strict) + pytest + coverage |
| P0 | 编写 `docker-compose.yml` 草案 | 已完成 | 2026-09-16 | 2026-09-16 | SQLite 默认 + PostgreSQL 可选 profile |
| P0 | 编写 `CONTRIBUTING.md` / `SECURITY.md` / `CHANGELOG.md` | 已完成 | 2026-09-16 | 2026-09-16 | CONTRIBUTING 含八条架构红线 |
| P0 | `git init` 与首次提交 | 已完成 | 2026-09-16 | 2026-09-16 | 分支 master |
| P0 | 验证：`git status` 干净、`learning/` 被忽略、`make help` 可用 | 已完成 | 2026-09-16 | 2026-09-16 | 三项全通过 |
| P1 | 后端地基（全部子任务） | 已完成 | 2026-09-16 | 2026-09-16 | 详见下方验收记录 |
| P2 | 前端地基（全部子任务） | 已完成 | 2026-09-16 | 2026-09-16 | 40 个文件、13567 行；详见下方验收记录 |
| P3 | 目录域（全部子任务） | 已完成 | 2026-09-16 | 2026-09-16 | 迁移 0005/0006、平台端 17 端点、前端 6 页；详见下方验收记录 |
| P4 | 订单与设备生成 | 已完成 | 2026-09-16 | 2026-09-16 | 迁移 0008–0010、平台端 20 端点、前端 5 页；测试 395→397；详见下方验收记录 |
| P5 | 设备/分配/绑定 | 未开始 | — | — | 依赖 P4（迁移编号 0011） |
| P6 | 烧录工厂端 | 未开始 | — | — | 依赖 P5 |
| P7 | AI 抽象与离线模拟引擎 | 已完成 | 2026-09-16 | 2026-09-16 | 迁移 0007、42 测试；详见下方验收记录 |
| P8 | 终端用户小程序端 | 未开始 | — | — | 依赖 P7 |
| P9 | AI 配置与运营看板 | 未开始 | — | — | 依赖 P7（迁移编号 0012） |
| P10 | 部署与质量保障 | 未开始 | — | — | 依赖 P8、P9 |
| P11 | 文档/学习手册/复盘 | 未开始 | — | — | 依赖 P10 |

---

## 当前进行中的工作

P4 已完成，下一步进入 **P5 设备/分配/绑定**（分配单、confirm-token 绑定、解绑、心跳与 180 秒在线窗口、租户隔离参数化矩阵）。

> **本阶段的并行开发方式**（用户明确要求评估并执行）：主会话负责 P3（目录域），另开 2 个子代理并行推进 **P7 AI 抽象层** 与 **P3 集成测试**。为避免冲突采用三条约定：① 严格文件归属；② Alembic 迁移编号预先分配（P3=0005/0006、P7=0007、P4=0008–0010、P5=0011、P9=0012）；③ 三份追踪文件只允许主会话修改，代理一律不提交。

### P1 验收记录（2026-09-16）

| 验收项 | 命令 | 结果 |
|---|---|---|
| 代码检查 | `make lint` | ✔ ruff 0 问题 |
| 类型检查 | `make typecheck` | ✔ mypy strict，36 个源文件无问题 |
| 测试 | `make test` | ✔ **118 passed**，6.5 秒 |
| 契约一致性 | `make openapi-check` | ✔ OpenAPI 契约与快照一致 |
| 一键质量门禁 | `make check` | ✔ 全部通过 |
| 服务启动 | `make dev` | ✔ 日志正常，traceId 贯穿，安全校验通过，种子数据写入 |
| 登录返回 JWT | `POST /api/v1/auth/login` | ✔ 平台超管 `permissions=["*"]`、`tenantId=null`（ADR-01） |
| 账号锁定 | 连续 5 次密码错误 | ✔ 第 6 次（含正确密码）返回 `ACCOUNT_LOCKED` |
| **P-04 修复** | 禁用租户登录 | ✔ 返回 `TENANT_DISABLED`，账号本身仍为 ACTIVE |
| 迁移可回滚 | `alembic downgrade base` → `upgrade head` | ✔ 全链可逆，可跳步，模型与迁移零漂移 |
| 密钥不泄漏 | 响应体检查 | ✔ 全量响应无 SecretKey；`.env` 与 `learning/` 均未被 git 跟踪 |

### P2 验收记录（2026-09-16）

| 验收项 | 命令 / 方式 | 结果 |
|---|---|---|
| 后端门禁未被前端改动破坏 | `make check` | ✔ ruff 0 问题 / mypy strict 36 文件 / **118 passed** / OpenAPI 契约一致 |
| ES Module 导入契约 | `make fe-check` | ✔ 30 个 JS 模块 + 9 处 HTML 引用，77 处导入的路径与具名导出全部匹配 |
| 手写二维码编码器正确性 | `make qr-verify` | ✔ 13 个载荷 × 8 个掩码 = **104 组矩阵与 `qrcode` 库逐模块一致** |
| 四端页面可打开 | `GET /`、`/login`、`/platform/`、`/merchant/`、`/factory/`、`/miniapp/` | ✔ 全部 200；JS 以 `text/javascript` 提供（ES Module 可加载），CSS 为 `text/css` |
| 登录后进入对应端工作台 | 浏览器：平台超管登录 | ✔ 落地 `/platform/#/dashboard`，渲染「欢迎回来，平台管理员」，无白屏与控制台报错 |
| 未登录直访子页被守卫拦截（P-05） | 浏览器：跨 Origin 访问 `/platform/` | ✔ 重定向 `/login?redirect=%2Fplatform%2F&reason=unauthenticated` |
| 刷新子页面状态不丢失（P-01） | 浏览器：子路由 `#/tenants` 后 `reload` | ✔ URL 与页面内容均保持，未回落首页 |
| ⌘K 命令面板 | 浏览器：工作台按 `Control+k` | ✔ 出现 `dialog "命令面板"` 浮层 + 搜索框 + 候选列表 |
| 全新视觉（不得沿用参考站） | `tokens.css` 检查 | ✔ 主色靛紫 `#4F46E5`（非 Ant 蓝 `#1677ff`）、浅色侧导轨（非深色 `#001529`）、Inter + Noto Sans SC、数值 `tabular-nums` |
| 密钥不泄漏（前端侧） | 前端源码扫描 | ✔ 无任何硬编码口令/JWT；登录页仅有「演示账号快捷填充（只填账号，不含密码）+ 提示密码见 `.env`」，且 `.env.example` 中密码字段为空占位 |

> 本轮遗留观察（不影响 P2 判定）：① 尚无页面使用路径参数（`#/tenants/t-001` 回落 `#/dashboard`），参数化能力已在 `router.js` 落地，将于 P3/P5 详情页启用；② 工作台首屏 5 个统计接口返回 404 属预期（对应域未开发，页面已降级为「建设中」）。

### P3 验收记录（2026-09-16）

**门禁（全部实测通过）**

| 验收项 | 命令 | 结果 |
|---|---|---|
| 代码检查 | `make lint` | ✔ ruff 0 问题（含 `tests/` 与 `scripts/`） |
| 类型检查 | `make typecheck` | ✔ mypy strict，**56 个源文件**无问题 |
| 测试 | `make test` | ✔ **251 passed**（P3 新增 82 条） |
| 契约一致性 | `make openapi-check` | ✔ OpenAPI 与快照一致（31 个路径） |
| 一键质量门禁 | `make check` | ✔ 全部通过 |
| 前端导入契约 | `make fe-check` | ✔ 37 个 JS 模块、**145 处**导入的路径与具名导出全部匹配 |
| 迁移可逆与零漂移 | `alembic upgrade head` → `downgrade 0004` → `upgrade head`；`alembic check` | ✔ 0005/0006 升降可逆；模型与迁移零漂移 |

**浏览器实测（真实服务 + 本机 Chromium，逐项操作）**

| 验收项 | 操作方式 | 结果 |
|---|---|---|
| 工作台统计接入真实数据 | 登录 `admin` → `#/dashboard` | ✔ `租户总数=2 个`、`产品模板=2 个`（订单/设备当时仍为「—」，已在 P4 补齐，见 P4 验收记录） |
| 租户列表字段与筛选 | `#/tenants` | ✔ 2 条租户，编码/联系人/电话/行业/状态/创建日期全部正确回显；行业下拉取自真实数据；分页「共 2 条 · 第 1/1 页」 |
| **参数化路由真正启用（P-01）** | 列表点「详情」→ 地址变 `#/tenants/t-001` → **按 F5 刷新** | ✔ 刷新后 URL 与页面内容均为该租户详情，**未回落 `#/dashboard`**；P2 遗留观察①就此解除 |
| 租户详情四标签页 | 概览 / 产品授权 / 客户产品 / 登录账号 | ✔ 均正确渲染；概览含 4 张统计卡与 11 项资料 |
| **P-03 验收视角「建后客户详情可见」** | 详情 → 客户产品标签 | ✔ 显示该租户的 `星辰小方块（Wi-Fi 智能故事机）CP-T001-CUBE`；切换到 t-002 只显示其自有产品（租户间不串台） |
| 产品授权清单 | 详情 → 产品授权标签 | ✔ 2 条授权，含「已建产品」计数（4G 故事机 0、ESP32-S3 1） |
| **一次性密码与强制改密** | 详情 → 登录账号 → 开通账号（口令留空） | ✔ `POST /tenants/t-002/accounts → 201`；弹窗显示「初始密码（仅显示一次）」+ `复制密码` 按钮；回读该账号 `mustChangePassword=true` |
| **P-06 删除级联校验** | 模板页点「删除」→ 确认 | ✔ 确认框先提示「该模板当前有 1 条授权…按设计会被拒绝」；确认后 `DELETE → 409`，响应 `code=CASCADE_CONFLICT`、`message` 说明「仍存在 1 条租户授权」、`details={authorizations:1, clientProducts:0}`；模板未被删除 |
| **ADR-07 连通性检测不伪造成功** | 云服务商页对「火山引擎智能云」点「连通性检测」 | ✔ `POST /clouds/{id}/test → 200`，`result=NOT_CONFIGURED`、`ok=false`、`lastTestMessage=尚未配置 AccessKey / SecretKey，已按安全策略跳过真实调用`、`latencyMs` 为空；行内状态同步为「未接入 / 未检测」 |
| **密钥只进不出（小程序）** | 客户产品详情 → 小程序配置 → 填哨兵 `SENTINEL_SECRET_…` → 保存 | ✔ `PUT → 200`；GET 响应仅 `appSecretHint="SENT****"`，**明文哨兵在响应中出现 0 次**；库内 `app_secret_enc` 为 Fernet 密文（`gAAAAA…`）且不含明文；`decrypt_secret()` 可正确还原 |
| 账号改名后可用 | 登录页 4 个演示账号按钮 → 分别登录 | ✔ `admin`（平台超管）登录成功，`role=PLATFORM_ADMIN`、`tenantId=null`、权限为通配；`13812345678`（平台运营）登录成功并**被强制进入改密界面**（`mustChangePassword=true`） |

**遗留观察（不影响 P3 判定）**

1. 工作台 5 个统计接口中，`/platform/orders`、`/platform/devices`、`/platform/audits` 仍返回 404（属 P4/P5 与未交付域），页面已按「—」降级，P2 已声明的降级策略继续生效。
2. 客户产品删除尚未接入「有设备则拒绝」的校验——`devices` 表属 P4，代码注释已声明延后。
3. 工厂端字段脱敏属 P6，本阶段无法验证。
4. 交互式验收在开发库留下了两条痕迹（t-002 下的账号 `13900000009`、prod-t001-cube 的小程序配置），属正常使用痕迹；`make reset-db && make seed` 可回到纯净演示态。

### P4 验收记录（2026-09-16）

**门禁（全部实测通过）**

| 验收项 | 命令 | 结果 |
|---|---|---|
| 代码检查 | `make lint` | ✔ ruff 0 问题（含 `tests/` 与 `scripts/`） |
| 类型检查 | `make typecheck` | ✔ mypy strict，**69 个源文件**无问题 |
| 测试 | `make test` | ✔ **397 passed**（P4 新增 146 条：单元 14 + 集成 132） |
| 契约一致性 | `make openapi-check` | ✔ OpenAPI 与快照一致（**51 个路径**，P4 新增 20 个端点） |
| 一键质量门禁 | `make check` | ✔ 全部通过 |
| 前端导入契约 | `make fe-check` | ✔ 42 个 JS 模块、**195 处**导入的路径与具名导出全部匹配 |
| 迁移可逆与零漂移 | `alembic upgrade head` → `downgrade 0007` → `upgrade head`；`alembic check` | ✔ 0008/0009/0010 升降可逆；模型与迁移**零漂移** |

**浏览器实测（真实服务 + 本机 Chromium，逐项操作）**

| 验收项 | 操作方式 | 结果 |
|---|---|---|
| 工作台 6 张统计卡接入真实数据 | 登录 `admin` → `#/dashboard` | ✔ `租户2 / 模板2 / 客户产品2 / 待审核订单1 / 订单2 / 设备5`；**P2 遗留观察②就此完全解除** |
| 订单列表 | `#/orders` | ✔ 2 条订单，单号/租户/产品/数量/状态标签/联网方式/设备数/时间齐全；**仅「待审核」单才渲染「审核」按钮**（状态相关动作渲染正确） |
| **订单闭环（本阶段核心）** | 待审核单 → 审核通过 → 生成设备 | ✔ `POST /orders/{id}/audit → 200`；`POST /orders/{id}/generate → 200`；订单最终 `IN_STOCK`、`generatedCount=3`、新增 3 台设备（SN 规则 `SN-20260916-XXXXXX`） |
| **页头动作随状态更新** | 审核后观察页头按钮 | ✔ 由「审核通过 / 驳回」自动变为「生成设备」，生成后再变为「导出二维码」——**修掉一个真实缺陷**（见下） |
| 设备库存与四维状态 | `#/devices` | ✔ 8 台设备；「四维（资产/激活/在线/绑定）」**分列展示**为 `已入库待生产 / 未激活 / 从未在线 / 未绑定`（ADR-03 要求不合并成单列）；统计卡 `总数8 / 已入库6 / 已激活0 / 在线0` |
| 冻结 / 解冻 | 行内操作（原因必填） | ✔ 冻结 → `FROZEN` 且 `previousAssetStatus=IN_STOCK`；解冻 → 恢复 `IN_STOCK`（**原路恢复生效**，印证冻结范围收紧后的对称性）；**空原因会被拦下且不关闭弹窗**（设计如此，非缺陷） |
| **批次导入全链路** | `#/batches` 上传 CSV → 预检 → 导入 → 错误报告 | ✔ 预检 `6 行 / 有效 3 / 无效 3 / 重复 2`（与构造的异常行完全对应）；导入后 `已导入 3` 且设备总数 8 → 11；错误报告为 `text/csv`、**BOM=`efbbbf`**、含行号与原因（第 4 行缺 SN、第 5 行文件内重复、第 6 行 SN 已存在于设备库） |
| `api.raw` 修复验证 | 浏览器内通过页面模块发起真实请求 | ✔ `HTTP 200 \| text/csv \| blob=137B \| 首行=行号,SN,错误原因`（返回未消费的 `Response`，`blob()` 可用） |
| **D2 修复验证** | 同文件 + 不同订单 / 同订单 | ✔ 不同订单 → `409 BATCH_FILE_CONFLICT` + `details{existingBatchNo,existingOrderId,requestedOrderId}`；同订单 → 幂等复用（返回同一批次号） |

**独立验证（子代理对抗性测试，产出 56 条集成用例）**

| 发现 | 级别 | 处理 |
|---|---|---|
| **D1** 4G 生成因厂商未配置失败时，审计写的是 `GENERATE_DEVICES`，`VENDOR_CALL_FAILED` 为 0；且失败审计 `resource_id=None` | 真缺陷 | ✔ 已修：按失败根因分类（厂商原因 → `VENDOR_CALL_FAILED`），并给 `record_failure()` 补 `resource_type`/`resource_id` 形参 |
| **D2** 同一份 CSV 配**另一个订单**上传时静默复用旧批次，导入的设备会挂到旧订单/旧租户 | 真缺陷（数据归属） | ✔ 已修：新增 `BATCH_FILE_CONFLICT`(409)，命中摘要后校验订单一致性，不一致显式拒绝 |
| **O1** 冻结端点 OpenAPI 文案仍写「GENERATED / IN_STOCK 可冻结」 | 观察 | ✔ 已修（改为「仅 IN_STOCK」，与收紧后的规则一致） |
| **O2** 批次的 `error-report` / `import` 读路径未走 `assert_visible`，破坏 ADR-08 收口不变式 | 观察 | ✔ 已修（与 `list_lines` 对齐） |
| **O3** `MAX_FIELD_LENGTH=128` 大于 `sn String(64)` / `imei,iccid,mac String(32)`，换 PostgreSQL 会写库报错 | 观察 | ✔ 已修（改为按字段上限校验，超长日志写明是哪个字段） |
| **O4** `duplicatedRows` 把「库内已存在」与「文件内重复」合并计数，与字段名语义不符 | 观察 | ✔ 已修（只统计文件内重复） |

**本阶段顺带修复的缺陷**

1. **状态相关的页头动作不随状态更新**（`order_detail.js`）：页头按钮只在首屏按当时状态构建一次，审核通过后仍显示「审核通过/驳回」，用户必须手动 F5 才能看到「生成设备」。已给 `createDetailPage` 增加 `setActions()`（`shared/app/page.js`），并在 `reload()` 中按新状态重算。
2. **`api.raw()` 形同虚设**（P2 遗留）：`request()` 的形参里根本没有 `raw`（只在 JSDoc 里写了），且最初版本即便补上 `raw` 也会先 `parseBody()` 消费 body，调用方拿到的 `Response` 再 `blob()` 会抛 `Body is unusable`。已修复为「`raw` 时跳过 body 消费」，并让 `api.raw()` 自身隐含 `raw: true`。
3. **「审核通过」确认框沿用了驳回流程的误导文案**（写「驳回是不可逆的」），已改为符合通过语义的说明。
4. **`FREEZABLE_ASSET_STATUSES` 与 `ASSET_TRANSITIONS[FROZEN]` 不对称**（P1 遗留）：原允许冻结 `GENERATED`，但解冻只能回到 `IN_STOCK`，「恢复原状态」无法成立。已收紧为**只允许冻结 `IN_STOCK`**，并新增 `tests/unit/test_device_state_machine.py`（14 条）把该决策钉死。

**遗留观察（不影响 P4 判定）**

1. **工厂端端点未实现**（属 P6）：`factory_orders` / `burn_reports` / `inspections` 三表已建好，端点与脱敏留给 P6。
2. **设备详情页未做**：设备列表的「详情」目前给出指引提示而非跳转页（详情页不在本阶段前端归属清单内，P5 做设备流转与时间线时一并补）。
3. **`duplicatedRows` 是预检时落库的快照**：口径修正后不会回溯重算历史批次，因此演示库里那条旧批次仍显示 `2`。
4. **`COMPLETED` 订单再次调用生成接口为幂等回放 200**（不新增设备）：这是有意的重试安全设计，不是终态泄漏。
5. 商户端「我的订单」的客户产品下拉依赖本阶段新增的 `GET /merchant/products`；P9 会把它扩成完整的「我的产品」页。

---

### P7 验收记录（2026-09-16）

| 验收项 | 命令 / 方式 | 结果 |
|---|---|---|
| 代码检查 / 类型检查 | `make lint`、`make typecheck` | ✔ ruff 0 问题；mypy strict 56 文件无问题 |
| 单元 + 集成测试 | `pytest tests/unit/test_provider_registry.py tests/integration/test_ai_mock.py tests/integration/test_vendor_unavailable.py` | ✔ **42 passed**（注册表 31 / 模拟引擎 11 / 无密钥安全失败 7） |
| 迁移与漂移 | `alembic upgrade head` → `downgrade -1` → `upgrade head`；`alembic check` | ✔ `0007` 落地且**零漂移**；升降往返可逆 |
| 离线可跑 | `AI_DEFAULT_PROVIDER=mock` 下 `/ai/chat`、`/ai/tts`、`/ai/asr` | ✔ 全通；流式分块数 > 1；会话与消息落库 |
| **ADR-07 安全失败** | `tests/integration/test_vendor_unavailable.py` | ✔ 未配置密钥的厂商 `health_check → DOWN`、端点返回 `VENDOR_UNAVAILABLE` 且写入 `VENDOR_CALL_FAILED` 审计，**绝不伪造成功** |

> **P7 已知局限（诚实记录，待真实联调消除）**：① 火山引擎的**签名算法与签名位置**仍是占位（官方《服务端 API › 调用方法》页正文不可读），已收口在 `BaseHttpProvider.sign` 一处并在代码中显式标注；② `AibotCreate` 之外的 Action Body 字段名按官方 PascalCase 惯例统一，未逐字核对（风险示例：`AibotId` vs `AgentId`）；③ `TTSConfig` / `LLMConfig` 的层级为同构推断，仅 `ASRConfig` 逐字来自官方示例；④ 实时对话的真实链路是 RTC/WebSocket 长连接，当前 `chat()` 为「HTTP 取整段 + 本地分块」，联调时替换为帧解析（对外接口 `AsyncIterator[ChatChunk]` 不变）；⑤ `dialogue_sessions.device_id` / `end_user_id` 刻意未建外键（`devices` 属 P4、`end_users` 属 P9），P4 落地后需补一次轻量迁移。

---

## 阻塞事项

| 编号 | 阻塞内容 | 影响阶段 | 提出日期 | 处理进展 |
|---|---|---|---|---|
| — | 暂无 | — | — | — |

> 遇到阻塞时按此格式登记，并在 `todolist.md` 对应任务标记为 `[!]`。**不擅自改变已定案的架构决策**（见 `todolist.md` 附录 C）。

---

## 里程碑

| 里程碑 | 交付标志 | 状态 | 达成日期 |
|---|---|---|---|
| M0 项目启动 | 脚手架、进度文件、仓库初始化就绪 | 已完成 | 2026-09-16 |
| M1 可登录骨架 | 后端可登录鉴权 + 前端可登录进工作台 | 已完成 | 2026-09-16 |
| M2 业务闭环（无 AI） | 客户开通 → 下单 → 生成设备 → 烧录 → 分配 → 扫码激活 | 未开始 | — |
| M3 AI 闭环 | 终端设备可对话（离线模拟引擎），商户可配置 AI | 未开始 | — |
| M4 可部署交付 | `docker compose up` 一键起服务 + 全端点冒烟通过 | 未开始 | — |
| M5 作品集就绪 | 专业文档集齐备 + 学习手册完成 + README 终版 | 未开始 | — |

---

## 关键架构决策（已定案，不再变更）

| # | 决策 |
|---|---|
| ADR-01 | 平台管理员 `tenant_id = NULL`（全局长），修正参考实现把它挂在 `t-001` 的矛盾 |
| ADR-02 | 烧录工厂独立 `factories` 表 + `role_type=FACTORY`，账号 `tenant_id = NULL` |
| ADR-03 | 设备状态以**四维**（`asset_status`/`activation_status`/`online_status`/`bind_status`）为权威，原型的 9 态降为前端派生展示 |
| ADR-04 | AI 业务标准以 `ai-toy` 原型与 PRD-03 为准（参考站无 AI 实现） |
| ADR-05 | 默认 SQLite，PostgreSQL 可选，Alembic 统一管控迁移 |
| ADR-06 | 前端零构建 ES Module，克隆即可打开，无需 `npm install` |
| ADR-07 | 未配置密钥的厂商一律返回 `VENDOR_UNAVAILABLE`，**绝不伪造成功** |
| ADR-08 | 多租户过滤只在 repository 层收口，禁止在 router 手写过滤 |

---

## 变更记录

| 日期 | 变更内容 |
|---|---|
| 2026-09-16 | 项目启动；完成计划制定 |
| 2026-09-16 | **P0 完成**：目录骨架、`.gitignore`（含 `learning/` 排除）、`.env.example`、`Makefile`（30 命令）、`LICENSE`、`README`、`CONTRIBUTING`/`SECURITY`/`CHANGELOG`、`requirements*.txt`、`pyproject.toml`、`docker-compose.yml`、`deploy/Dockerfile.backend`、`deploy/nginx.conf`；产出 `todolist.md` 与 `项目进度.md`；`git init` 完成并通过三项验收 |
| 2026-09-16 | **P1 完成**：后端地基全部落地——配置与启动期安全校验、统一错误码（25 个）、traceId 中间件、权限目录（6 角色）、四层 Alembic 迁移、SQLAlchemy 模型（11 张表）、bcrypt+JWT 鉴权（含登录锁定与刷新令牌重放检测）、租户作用域收口、幂等键、分页、审计服务、8 个 API 端点、应用工厂、幂等种子数据；118 个测试通过，`make check` 全绿；**修复遗留缺陷 P-04** |
| 2026-09-16 | **P2 完成**：前端地基全部落地（40 文件 / 13567 行，零构建 ES Module）——全新视觉设计系统（靛紫主色 + 浅色侧导轨）、五个 CSS 文件、core 层（config/api/auth/router/guard/store/ws/end-user）、ui 组件层（含纯 SVG 图表、Canvas 手写二维码、⌘K 命令面板）、四端入口与统一登录页；**修复 P-01（刷新状态丢失）、P-02（back 目标）、P-05（无路由守卫）**；新增 `make fe-check`（77 处导入契约）与 `make qr-verify`（104 组二维码矩阵比对）；`make check` 保持全绿；浏览器实测四端可开、可登录、守卫生效、⌘K 可用 |
| 2026-09-16 | **P3 完成**：目录域全部落地——迁移 `0005_catalog` / `0006_client_product_miniapp`（`cloud_providers` / `product_templates` / `product_authorizations` / `client_products` / `miniapp_configs`）、`app/core/crypto.py` 密钥加密（明文只进不出，响应仅回掩码）、`tenant_service` / `catalog_service`、平台端 **17 个端点**、前端 **6 个页面**（首次启用参数化路由 `#/tenants/:id`、`#/client-products/:id`）、种子数据补充 2 云服务商 / 2 模板 / 3 授权 / 2 客户产品；**修复遗留缺陷 P-03、P-06**；顺带修复 6 项缺陷（前端监听器跨导航累积、`updated_at` 过期导致 5 个 PUT 端点 500、模板详情字段缺失、编码重复错误码不一致、`utcnow` 前向引用等）；测试 118 → 251；`make check` 与 `make fe-check` 全绿；浏览器实测 12 项 |
| 2026-09-16 | **P7 完成（并行开发）**：AI 抽象层与离线模拟引擎——`ai/base.py` 抽象与 DTO、`ai/registry.py` 四层解析、`ai/mock/*` 规则对话与可复现模拟、四个厂商适配器骨架（火山引擎按官方「硬件对话智能体」文档校对 Action 与请求体结构）、迁移 `0007_ai_dialogue`（8 张表）、`/ai/*` 六个端点；42 条测试通过，迁移零漂移 |
| 2026-09-16 | **并行开发机制落地**：主会话负责 P3，另开 2 个子代理并行推进 P7 与 P3 集成测试；确立三条防冲突约定（严格文件归属 / 迁移编号预分配 / 追踪文件单写者，代理不提交） |
| 2026-09-16 | 新增 `docs/13-火山引擎硬件对话智能体配置说明.md`（控制台实测 + 官方文档取证）；**迁移编号顺延**：P3=`0005`/`0006`、P7=`0007`、P4=`0008`–`0010`、P5=`0011`、P9=`0012` |
| 2026-09-16 | **P4 完成**：订单与设备生成全部落地——迁移 `0008_orders` / `0009_devices_batches` / `0010_factory_production`（9 张表）、`qrcode_service`（JX/JD 双格式 + HMAC 签名，修正原型参数顺序缺陷）、`order_service`（状态机 + 审核 + 生成设备，4G 走厂商 / Wi-Fi 本地生成）、`device_service`（四维状态流转 + 冻结解冻报废 + 事件时间线）、`batch_service`（CSV 去重预检导入 + 错误报告 + 幂等重跑）、平台端 20 个端点、前端 5 个页面（订单 / 订单详情 / 设备 / 批次 / 商户订单）；**测试 251 → 397**；`make check` 与 `make fe-check` 全绿；浏览器实测 10 项 |
| 2026-09-16 | **P4 独立验证发现并修复 2 个真缺陷 + 4 条观察项**：D1 厂商失败审计口径不一致且缺资源标识；D2 同一 CSV 用于另一订单时静默复用旧批次（设备会挂到旧订单）；O1 冻结端点文案过期；O2 批次读路径未走 `assert_visible`（破 ADR-08 不变式）；O3 批次字段上限大于数据库列长（换 PostgreSQL 会写库报错）；O4 `duplicatedRows` 口径与字段名不符 |
| 2026-09-16 | **顺带修复 4 个缺陷**：① 状态相关页头动作不随状态更新（审核后仍显示「审核通过」，须手动 F5 才出现「生成设备」）→ `createDetailPage` 增加 `setActions()`；② **`api.raw()` 形同虚设**（P2 遗留：`raw` 形参不存在，且即便补上也会先消费 body 导致 `blob()` 抛 `Body is unusable`）；③ 「审核通过」确认框沿用驳回文案；④ `FREEZABLE_ASSET_STATUSES` 与 `ASSET_TRANSITIONS[FROZEN]` 不对称（P1 遗留）→ 收紧为只允许冻结 `IN_STOCK` 并新增 14 条单元断言 |
| 2026-09-16 | 新增 `docs/14-ESP32-S3刷机与联调步骤清单.md`：面向真机联调当天的可执行清单（云端自检 → 工具链 → 四条路径决策树 → 配网 → 首次对话三条判据 → F-01~F-10 失败速查 → 真机结果回填项） |
| 2026-09-16 | **账号中立化**：平台超级管理员账号由 `15811805314`（沿用自第三方参考站）改为 `admin`；新增平台运营账号 `13812345678`（`PLATFORM_OPERATOR`，可处理产品/订单/设备，不能改客户档案、云服务商、产品模板）；同步更新 `.env` / `.env.example` / `config.py` / 种子数据 / 测试夹具与断言 / 登录页演示账号按钮，并迁移开发库中的既有账号（**保留原密码哈希**） |

---

## 更新约定

1. **每完成一个阶段**，必须同步更新：本文件的总体进度条、主进度表、里程碑，以及 `todolist.md` 对应复选框。
2. **主进度表状态取值**：`未开始` / `进行中` / `已完成` / `阻塞`。
3. **日期格式统一** `YYYY-MM-DD`；开始与完成日期在同一天时也需完整填写。
4. **阻塞事项必须登记**，并注明影响的阶段与提出日期。
5. 新增或调整任务时，**两份文件必须同时更新**，保持一一对应。
